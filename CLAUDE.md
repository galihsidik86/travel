# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this repo is

A hybrid: a static HTML/CSS design package **+** a Node.js/Express/Prisma/MySQL backend that is being built incrementally on top of those same design files. All copy is Bahasa Indonesia (`lang="id"`). The build-out is staged:

1. ✅ Skeleton (Express + static serving + sensitive-path block)
2. ✅ Prisma schema + MySQL (MariaDB via XAMPP), seed
3. ✅ Auth + RBAC (JWT cookie + bearer, 8 roles, audit, rate-limit)
4. ✅ Public paket landing + booking (`/p/:slug?a=<slug>`) with agent lock-in
5. ✅ Agen portal `/agen` — Leads CRM (Cold/Warm/Hot/Lunas kanban) + Marketing kit + Wallet/komisi + Analytics
6. ✅ Admin/HQ dashboard `/admin` — Overview / Paket / Manifest / Bunking / Finance tabs, plus Users + Jemaah + Audit + Booking-detail pages
7. ✅ Money flow — Payment recording, status transitions, auto-Komisi on LUNAS, cancel + refund (negative-Payment), editable notes
8. ✅ Ops jobs — daily cron to auto-EXPIRE overdue `JemaahDocument` rows (CLI + HTTP trigger)
9. ✅ Jemaah self-service portal `/saya` — public HTML register + booking claim by `(bookingNo, phone)` + read-only booking detail. Claim flow soft-merges anonymous booking-profiles into the user's canonical profile. Logged-in jemaah booking new paket from `/p/:slug` auto-links to their account (no claim needed). Profile + document self-edit at `/saya/profile`, including per-doc file attachment (5mm) stored in private `private/docs/<jemaahId>/`.
10. ✅ Notifications — pluggable channel queue (`Notification` model) with console sender by default. Booking create / payment received / komisi payout auto-enqueue WA + email notifs. Admin viewer at `/admin/notifications`, processed via `npm run job:send-notifications` CLI or HTTP trigger. Jemaah-side inbox at `/saya/notifications` (5ll) scoped via `recipientUserId`; per-channel opt-out (5jj) on `JemaahProfile.notifEmail/notifWa` marks rows `SKIPPED` instead of suppressing them. Retry with exponential backoff (5nn): FAILED rows auto-retry up to 5 attempts (1m/5m/30m/2h/12h) via the same worker before going terminal.
11. ✅ Payment gateway — Midtrans Snap integration (5pp). `PaymentIntent` model decouples gateway state from realized `Payment` rows. Jemaah pays online via `/saya/bookings/:id` "Bayar online" button → Snap hosted checkout → signature-verified webhook materialises a Payment via `recordPayment` (single source of truth for money math + komisi). Fake mode when `MIDTRANS_SERVER_KEY` absent — full intent/webhook loop testable without external creds.
12. ✅ Crew portal `/crew` (5oo) — read-only muthawwif workspace. M2M `PaketCrew` join (admin-managed from paket edit page). Crew see their assigned paket dashboard + a stripped manifest (jemaah identity / phone / emergency contact / passport / room / doc-pills) with NO money fields — separation of duty.
13. ✅ Mobile experience — PWA shell (`shared/manifest.webmanifest` + `shared/sw.js`), installable `/saya` and `/crew`, mobile bottom-nav for jemaah, responsive @media baseline + per-view tightening. Offline-friendly attendance: IndexedDB queue (`shared/attendance-queue.js`) replays form submits via auto-flush on `online` event + 20 s tick. Crew SOS — `Incident` model + `/admin/incidents` queue with OPEN → ACKED → RESOLVED state machine + EMAIL+WA fan-out to ACTIVE admins on creation. Operational hardening: hot-path indexes (audit + intent compound), weekly `prune` retention job that bounds Notification/JobRun/failed-intent growth (AuditLog + Payment never pruned).
14. ✅ Per-paket × per-agent komisi matrix — `AgentPaketKomisi(agentId, paketId, rate)` join table prepended to the rate chain so admins can declare "ahmad-w gets 15% on VVIP-2026 only" without code changes. Admin UI: panel below Crew on `/admin/paket/:slug/edit`. The rate at LUNAS is still locked into `Komisi.amount` — later edits to the matrix never recompute historical rows.
15. ✅ Cached document thumbnails — uploads auto-generate a 256-px JPEG (`sharp`) at `private/docs/<jemaahId>/thumbs/<docId>.jpg`. Thumb routes `/saya/documents/:docId/thumb` + admin variant serve the cache; **graceful fallback** to the full file when no cached thumb (legacy uploads + non-image mimes still work). Doc panel `<img>` srcs swapped to `/thumb`; click-through `<a>` still hits `/file` for full quality.
16. ✅ Analytics extension — two new panels powered by `getPerPaketPerformance` + `getKomisiMonthly` in `src/services/analytics.js`. Agen Analitik tab gets per-paket leaderboard (revenue + conv% + status counts) + 6-month komisi bar chart (earned vs paid). Admin overview gets cross-agen mirror (`getPerPaketLeaderboard` + `getKomisiMonthlyAdmin`) including `agentCount` (distinct agents per paket) + `directCount` (Kantor Pusat walk-ins). Both komisi-monthly variants share `komisiMonthlyImpl` and exclude CANCELLED rows (undone work, not income).
17. ✅ Web Push for SOS / Incidents — `PushSubscription(userId, endpoint, endpointHash, p256dh, auth)` per (user × browser). `pushToAdmins` fans out to every ACTIVE OWNER/SUPERADMIN/MANAJER_OPS subscription as a third layer alongside EMAIL+WA in `notifyIncidentCreated`. Service worker `push` handler shows a Notification + `notificationclick` focuses an existing tab. SOS sets `requireInteraction:true` so the toast sticks until the admin clicks. VAPID env-driven; absent → console fake mode (mirror Fonnte/SMTP pattern). Self-cleanup: 404/410 from web-push scrubs the dead subscription row.
18. ✅ Paket clone for next season — `POST /api/paket/:slug/clone` copies Paket row + hotels + days + prices into a fresh DRAFT with `kursiTerisi=0`. Optional `includeAgentOverrides` carries the per-agen rate matrix forward. **Never** copies bookings / payments / komisi / rooms / crew / incidents — clone is a template, not a historical record. `newReturnDate` defaults to `newDeparture + source.durationDays` so trip length stays consistent. Audit row stamps `clonedFromSlug` so lineage is queryable. UI: "Clone untuk musim berikutnya" modal in paket-edit form-footer.
19. ✅ Print-friendly manifest — `GET /admin/manifest/:slug/print` renders an A4 landscape worksheet for airport check-in (`views/print-manifest.ejs`). Active-only bookings sorted by room-first (kamar groupings stay adjacent), with passport / emergency contact / doc pills columns. `@media print` sets A4 landscape + `display: table-header-group` on thead + `page-break-inside: avoid` per row. Cetak button next to Export CSV in the admin Manifest tab.
20. ✅ Per-jemaah booking voucher — pairs with stage 19 (manifest is the admin's list, voucher is the per-jemaah summary handed out at manasik). Routes `GET /admin/bookings/:id/print` (4 admin VIEW roles) + `GET /saya/bookings/:id/print` (jemaah, scoped to `jemaahUserId`, **returns 404 on cross-user access — not 403 — so existence isn't leaked**). Service `src/services/bookingVoucher.js` returns the same shape for both paths so the view doesn't branch. A4 portrait worksheet: hero + jemaah identity + perjalanan (airline + route + hotel cards) + pembayaran (3-cell money strip + history) + condensed itinerary + agen contact footer. "🖨 CETAK VOUCHER" link in the crumb bar of both detail pages.
21. ✅ Payout slip — completes the print trifecta (manifest = admin worksheet, voucher = per-jemaah handout, slip = per-payout accounting receipt). `GET /admin/payouts/:id/print` renders an A4 portrait slip with payoutNo + amount + agen identity + komisi breakdown table + 2 signature boxes (agen + yang membayar). `getPayoutSlip` wraps `getPayoutById` with computed totals + Bahasa method label. **Sanity check**: when `sumKomisi !== amountIdr` (shouldn't happen — createPayout snapshots in a transaction), the slip prints a warn-strip rather than silently masking the discrepancy. "🖨 CETAK SLIP" link in the crumb bar of `/admin/payouts/:id`.
22. ✅ Per-paket profitability — admin leaderboard answers "which paket actually makes money" not just which generates revenue. `Paket.costPerPaxIdr` + `costNotes` (both nullable) carry the fully-loaded vendor cost per pax (hotel + flight + visa + ops). `getPerPaketLeaderboard` extends with `lunasPaxCount + totalCostIdr + komisiLiabilityIdr + netMarginIdr + marginPct`. Komisi liability sums EARNED + PAID (CANCELLED + PENDING excluded). **Null cost → null margin fields** (no misleading 0% / 100% when costs aren't tracked yet). **Negative margins reported honestly** — admin needs to see losses to fix them. Form on paket edit: "Biaya per pax (Rp)" + "Rincian biaya" with 3-state preprocessor so empty input persists as null. Leaderboard card footer colors marginPct by tier (≥20% emerald / ≥5% gold / ≥0% amber / negative ruby).
23. ✅ Pre-departure readiness checklist — `GET /admin/paket/:slug/checklist` answers "who's missing what before departure" at H−30 / H−7 / H−1. 8 boolean checks per active booking: paspor present + paspor valid (`expiry ≥ departureDate + 6 months` — Saudi rule), VISA_UMROH / VACCINE_MENINGITIS / HEALTH_CERT / MANASIK_CERT each VERIFIED, kamar assigned, kontak darurat present. Score = passed/8 × 100; tier ≥100 ready / ≥60 partial / else critical. **Sort = critical → partial → ready, lowest score first within tier** so the worst-of-worst land at the top of the admin's follow-up list. Countdown badge auto-colors by H−days (green ≥30d / amber ≥7d / ruby ≤7d / ink past-departure). Default filter hides ready rows so the page opens straight to actionable items. "✓ Checklist" button in the admin Manifest tab toolbar next to 🖨 Cetak + ⇩ Export CSV.
24. ✅ Departure calendar — `GET /admin/calendar?year=&month=&date=` completes the planning trio (manifest = per-trip detail, checklist = per-trip readiness, calendar = trip-by-trip overview). UTC-aligned month grid (Su-Sa) with paket badges per cell color-coded by status + fill rate (full emerald / semi gold / low ink / draft amber / closed ruby), overflow rendered as "+ N lagi". Optional `?date=YYYY-MM-DD` opens a drill panel below the grid with kursi% + status pill + Manifest/Edit action links. Month-nav crosses the year boundary; "Hari ini" jump shown only when not already on current month. Service excludes ARCHIVED + soft-deleted paket. Sidebar "Kalender" link in Operasional group (above "Cari booking").
25. ✅ Enriched recent activity feed — `src/lib/auditFormat.js` turns raw AuditLog rows into `{sentence, url, badge, amountIdr}` for the admin overview tab. Per-entity copy: Booking CREATE → "Booking baru …", STATUS_CHANGE → CANCEL/REFUND/TRANSFER/CLAIM, KomisiPayout → "Payout … · Rp …", Incident → SOS/INCIDENT badge by type, JemaahDocument → UPLOAD/VERIFY/EXPIRE (respects `autoExpired`), Paket cloned flag → CLONE badge, Retention sweep → PRUNE. Each row clickable into the relevant detail page. Badge tokens map to status color (emerald success / ruby destructive / gold money / sapphire neutral state-change / amber alerts).
26. ✅ Paket waitlist — `PaketWaitlist(paketId, fullName, phone, status, promotedBookingId?)` with composite-unique `(paketId, phone)` so re-submission upserts. Public CTA `/p/:slug` auto-flips to "Daftar waitlist →" when `kursiTerisi ≥ kursiTotal`; POST `/api/waitlist` (rate-limited 8/min/IP). Admin queue at `/admin/paket/:slug/waitlist` with per-row Promote (kelas + paxCount + agentSlug) and Cancel actions. **promote calls `createBooking` via the canonical pipeline** — money math, komisi, notif all converge on the same path. Status machine WAITING → PROMOTED (terminal, carries `promotedBookingId` backref) | CANCELLED. Re-joining after CANCELLED reopens to WAITING (defensive upsert).
27. ✅ Daily activity digest for OWNER — `src/services/dailyDigest.js → buildDailyDigest({now})` aggregates yesterday's revenue + booking + ops state from the **same sources `/admin` reads** (no separate counters / denormalised state). Counts: new + LUNAS bookings, new jemaah, new leads, incidents created + still-open. Money: payments in (IDR-only filter — non-IDR cash rarely happens; mixing currencies would make the line confusing), refunds out, LUNAS revenue, komisi earned + paid, net revenue. 7-day rolling comparison (bookings + LUNAS). `notifyDailyDigest({digest})` fans out one EMAIL row per ACTIVE OWNER (not OWNER/SUPERADMIN/MANAJER_OPS like other admin alerts — this is strategic, not operational; admins already have real-time access via `/admin`). CLI `npm run job:send-daily-digest` + HTTP `POST /api/admin/jobs/send-daily-digest` + systemd timer at 07:00 daily. `send-daily-digest` registered in `EXPECTED_INTERVAL_MS` so `/api/health` flags a missing digest. **Idempotent**: re-running for the same wall-clock day returns the same numbers — duplicate runs just enqueue duplicate emails, no DB drift.
28. ✅ Yesterday-at-a-glance widget on `/admin` overview — surfaces the same `buildDailyDigest()` output the 07:00 email uses as a 6-cell panel ("Kemarin · {tanggal}") above the status-breakdown chart on the admin overview tab. Cells: booking baru / revenue LUNAS / pembayaran masuk / komisi earned / jemaah baru / insiden (warn-colored when OPEN > 0), plus a 7-day rolling footer. Calls `buildDailyDigest()` from `getAdminOverview` with try/catch — **a digest aggregation failure must NOT bring the overview down** (KPIs + recent activity are independently load-bearing). `yesterday` is null in that case and the view conditionally omits the panel. Same source as the email means UI and inbox never disagree.
29. ✅ Day-over-day delta pills in the yesterday widget — `buildDigestWithComparison({now})` runs `buildDailyDigest` twice (yesterday + day-before) and emits a `deltas` map keyed by metric. Each delta carries `{diff, pct, direction, good, empty}`. **Polarity-aware**: `incidentsCreated` + `refundsOutIdr` are reverse-polarity (more = worse), the rest are forward (more = better). View renders coloured arrow pills per cell: emerald when favourable, ruby when not, muted when flat or empty. Edge cases handled explicitly — `previous=0, current>0` returns `pct=null` so the view picks a `+N` label instead of a "% of zero" lie, and `both=0` flags `empty=true` for a neutral "—". Windowing rules go through `buildDailyDigest` both times so the two surfaces can't drift apart.
30. ✅ Delta arrows in the OWNER digest email — same `buildDigestWithComparison` output now feeds the 07:00 EMAIL fan-out. Each metric line carries an inline suffix (`  ·  ▲ 12%` / `  ·  ▼ Rp 200.000` / `  ·  =` / `''`) so the inbox shows the same trend the dashboard widget shows. CLI + HTTP trigger swapped from `buildDailyDigest` to `buildDigestWithComparison`. **`notifyDailyDigest` is backwards-compatible** — if a caller passes a bare digest (no `deltas` key), every suffix renders as empty string so the template stays string-stable and old smoke scripts keep working. Suffix-rendering format matches the widget polarity: `pct=null → ' · ▲ +N'`, flat → ' · =', empty → ''. Template carries one `{{trend*}}` placeholder per metric so future formatting tweaks live in one file.
31. ✅ Needs-attention panel — `src/services/needsAttention.js → getNeedsAttention()` rolls up stuck operator items in three categories: terminal-FAILED notifs (`status=FAILED AND (nextRetryAt=null OR attemptCount>=5)`), cancel-requests >24h pending (jemaah-side `cancelRequested=true` without admin action), OPEN incidents >24h old. Panel above the yesterday widget on `/admin` (ruby left-border + ageing column), and a `{{needsAttentionBlock}}` section appended to the OWNER digest email so the inbox also surfaces aged items. Block is empty (zero-cost) when nothing is stuck. `buildDigestWithAttention` wraps both into one pipeline call so CLI + HTTP trigger pick up the same payload. **All three categories use a 24h ageing cutoff** — fresh items don't crowd the panel; the SOS surface (`/admin/incidents`) and cancel-request banner (`/admin/bookings/:id`) already handle in-hour triage.
32. ✅ Per-paket weekly recap — `src/services/paketWeeklyRecap.js → getPaketWeeklyRecap({slug})` answers "what happened on THIS paket in the last 7 calendar days?" with new bookings + LUNAS + payments IDR-only + refunds + docs verified + cancelled. Dropdown on `/admin` overview (defaults to first ACTIVE paket; query param `?recapPaket=<slug>` selects another). 6-cell grid + a "Booking baru terkini" list (top 5) + footer links to edit + manifest. Window: previous 7 *complete* calendar days (today-so-far deliberately excluded — recaps that flicker as the day progresses are misleading). Soft-deleted + unknown slugs return `null` so the view conditionally hides.
33. ✅ Weekly summary email — `src/services/weeklyDigest.js → buildWeeklyDigest({now})` aggregates the previous full Mon–Sun + week-before for comparison. Monday-anchored convention (Indonesian work-week). Weekend re-runs idempotent: `resolveLastFullWeek` always returns the most-recent COMPLETED week regardless of day-of-week. Includes a `topPaket` rank (top 3 by new bookings, with paket title attached) and an inline `{{topPaketBlock}}` in the email. `WEEKLY_DIGEST_OWNER` notif type (migration applied), `notifyWeeklyDigest` fan-out, CLI `npm run job:send-weekly-digest`, HTTP `POST /api/admin/jobs/send-weekly-digest`, systemd timer Mon 07:05 (5 min after the daily so they arrive in order). Polarity-aware deltas same as daily — `cancelledBookings` joins `refundsOutIdr` + `incidentsCreated` as reverse-polarity metrics. Local-TZ `weekStart`/`weekEnd` formatting via `localYmd` helper — the ISO `slice(0,10)` UTC-truncation bug from the daily digest is *not* repeated here.
34. ✅ YoY paket comparison on leaderboard — `Paket.clonedFromId` column (migration applied) is now populated by `clonePaket` so clone lineage lives on the row (durable, queryable via FK), not just in AuditLog. `getPerPaketLeaderboard` runs a batched `summariseLifetimeTotals` for distinct parent IDs and attaches `previousSeason: {paketId, slug, title, lunasRevenue, lunasCount, marginPct, revenueDelta, revenueDeltaPct, marginDeltaPp}` per row. Leaderboard card adds a YoY strip — `▲ revenue +40% / ▼ margin −3pp` etc. — colour-coded by polarity. **Pct=null when parent has zero LUNAS revenue** (avoid divide-by-zero); the view falls back to a signed Rp delta. **MarginDeltaPp=null when either side lacks cost data** (can't honestly compare). No N+1 — one batched query per dimension regardless of how many clones are in view.
35. ✅ Refund analytics panel — `src/services/refundAnalytics.js → getRefundAnalytics({days, limit})` answers "where is money leaking?" over the last 90 days (configurable). Default 90d; top 10 per dimension. Two lenses — per paket and per agent (Kantor Pusat walk-ins bucketed under `__kp__` sentinel). Each row carries `{paid, refunded, refundCount, refundRatePct}`; ratePct = refunded ÷ paid × 100, rounded to 0.1. **IDR-only filter** to match the rest of the analytics. Sort by absolute refund amount (largest leak first) — rate-sort would surface tiny paket with a single tiny refund. Header KPI shows total refund + overall rate colour-coded by tier (≥5% ruby / ≥2% amber / >0% gold / 0% emerald). Panel renders on /admin overview between the leaderboard and komisi chart, only when total refunded > 0 (hidden on clean slates).
36. ✅ Per-agent weekly digest — mirror of stage 33 scoped per `AgentProfile`. `src/services/agentWeeklyDigest.js → buildAgentWeeklyDigest({agentId, now})` aggregates the previous full Mon–Sun for one agent: new bookings + LUNAS + cancel + leads (created/converted/lost) + conversion rate + komisi (earned/paid) + top paket sold. Returns null when agent is suspended / soft-deleted (caller skips). `listActiveAgentsForDigest` is the iterator helper. `notifyAgentWeeklyDigest` enqueues one EMAIL per call, indexed by `relatedEntity=AgentProfile` so audits can group by agent. CLI `npm run job:send-agent-weekly-digest` loops every active agent and catches per-row failures so a bad row doesn't abort the batch. New `AGENT_WEEKLY_DIGEST` enum (migration applied). Cron Mon 07:10 (after OWNER weekly at 07:05). Polarity-aware deltas include `cancelledBookings` + `leadsLost` as reverse.
37. ✅ Smart payout reminder — closes the loop on stage 36. `src/services/payoutReminder.js → getOverduePayoutCandidates({thresholdIdr})` aggregates EARNED komisi per agent and surfaces those ≥ Rp 1M (configurable). Includes `ageDays` from the oldest EARNED row so admins can see how long agent X has been waiting. `notifyPayoutReminder` fan-outs to OWNER/SUPERADMIN/MANAJER_OPS (KASIR excluded — separation of duty same as the payout workspace). **Silent on empty** — quiet weeks don't generate noise. New `PAYOUT_REMINDER_OWNER` enum (migration applied). CLI `npm run job:send-payout-reminder`, HTTP `POST /api/admin/jobs/send-payout-reminder`, cron Mon 07:15 (after agent weekly at 07:10). Body inlines top 10 candidates + "+ N agen lainnya…" for the rest so the email stays scannable.
38. ✅ Refund drill-down page — `GET /admin/refunds?paket=<slug>` or `?agent=<slug>` (sentinel `kantor-pusat` for walk-ins). `getRefundDetails` returns up to 200 REFUNDED Payment rows in the window + each row's booking + jemaah + cancel reason + agen. View shows KPI strip (total + count + average) and a table of clickable bookings. Refund analytics panel rows on `/admin` now link directly to this page. **No filter → redirect to overview** (guard against landing on an empty page from a bookmark). Window respects `?days=N` (capped 1–365).
39. ✅ Profitability gauge on paket-edit — `src/services/paketProfitability.js → getPaketProfitabilitySnapshot(paketId)` returns a lifetime snapshot mirroring the leaderboard math (so editing "Biaya per pax" here moves the leaderboard pill the same way). 4-cell panel inline below the cost input: Revenue LUNAS / Total biaya vendor / Komisi terikat / Net margin (with polarity-colored margin pct). Hidden when no LUNAS bookings exist (no point in showing zeros). Same null-fallback as stage 22 — cost missing → totalCostIdr=null → marginPct=null → cell renders "butuh biaya per pax" instead of misleading 0%. New panel renders only in edit mode (passed via opts); create form skips it cleanly.
40. ✅ Per-paket forecast — `src/services/paketForecast.js → getPaketForecasts({days, now})` computes 14-day booking velocity (mean + stddev of daily counts) and projects days-to-full per ACTIVE future-departure paket. Returns `dtfLowDays / dtfDays / dtfHighDays` (±1 SD confidence band so the UI renders a range, not false precision). Sort: most urgent first (lowest dtf), no-velocity rows last. CANCELLED bookings excluded from velocity (they represent intent that didn't stick). Edge cases explicit: `full=true, dtfDays=0` for paket already full; `noVelocity=true, dtfDays=null` for 14d-idle paket (avoid divide-by-zero). Risk tag flips to `'short'` when projected dtf > daysToDeparture (paket won't fill before takeoff). Panel on /admin overview with PENUH/PEKAN INI/<1 BULAN/AMAN badges color-coded.
41. ✅ Komisi liability aging — `src/services/komisiAging.js → getKomisiAging({now})` buckets EARNED komisi rows by age (`now − earnedAt`) into 0-30/30-60/60-90/90+ day windows. Per-agent rollup with per-bucket amount + count + `oldestDays`. Sort by oldestDays desc (90+ candidates land at the top regardless of Rupiah — agents waiting longest are the bigger trust signal). Panel on /admin overview shows 4 summary cells + a table; the 90+ column is highlighted ruby + bold to draw the eye. Status filter is strict EARNED — PAID/PENDING/CANCELLED excluded. Pairs with stage 37: that reminder says WHO crosses a Rp threshold; this says WHO has been waiting longest regardless of amount.
42. ✅ Waitlist promote nudge on cancel — when `cancelBooking` frees seats AND the paket has WAITING waitlist entries, `notifyWaitlistSlotFreed` fires an EMAIL fan-out to ACTIVE OWNER/SUPERADMIN/MANAJER_OPS (KASIR excluded — promote action lives in admin queue, not cashier). Body lists up to 5 longest-waiting candidates with `ageDays` so admin can promote without leaving the inbox. **Silent when waitlist is empty** (no signal worth sending). **Fire-and-forget posture**: nudge failure logged but never aborts the cancel (the freed seat is the load-bearing change). New `WAITLIST_SLOT_FREED` notif type (migration applied). Loop closes: jemaah joins waitlist → admin gets nudge on cancel → promotes via existing `/admin/paket/:slug/waitlist` flow.
43. ✅ Manifest close countdown — `src/services/manifestClose.js → getManifestClosing({urgentHours=72})` surfaces ACTIVE paket whose `manifestClosesAt` is either within 72h of now or already overdue. Paket with `manifestClosesAt=null` (admin chose "never close") or already at `kursiTotal` are excluded — close date is moot once full. Panel on /admin overview shows each row with a one-click "+ 24 JAM" form POST → `extendManifestClose` adds N hours (default 24, capped 1–168). **Smart base**: when paket is overdue, extension starts from `now` not from the past date, so "+24 jam" actually buys 24 future hours. Overdue rows get a ruby left-border + bold count in the panel header.
44. ✅ Smart promote deep link — the stage 42 nudge email now includes `?promoteOldest=1` deep link alongside the plain waitlist link. The waitlist route resolves to the oldest WAITING entry, passes `highlightId` to the view, which (a) adds a gold pulse to that row, (b) shows a top banner explaining the highlight, (c) auto-scrolls to and focuses the kelas dropdown so admin can tab through immediately. **Pure UI hint** — actual promotion still goes through the same POST flow, so this stays composable with manual selection. Empty-waitlist case renders a separate banner explaining the jemaah may have been promoted by another admin.
45. ✅ Per-agent sparkline — performa agen table on /admin overview gets a 30-day daily booking trend chart per row. Computed inline from the already-loaded `bookings` array (no extra query — one bucket pass over the existing data). Inline SVG path, 120×28 px, auto-scaled to per-agent max so a slow agent's small spike is still visible. Subtitle shows ▲/▼/= last-7d vs prev-7d trend. Agents with zero recent activity render "—" instead of an empty chart. Trend pill colour: emerald up / ruby down / muted flat. Table column added to header + body; colspan="9" empty state updated.
46. ✅ Stalled hot-leads agent digest — daily 08:00 per-agent EMAIL listing their COLD/WARM leads with `updatedAt > 7 days ago` (cutoff configurable). Most-stalled lead lands first. CONVERTED + LOST always excluded (terminal). New `AGENT_STALLED_LEADS` enum + migration. CLI `npm run job:send-stalled-leads`, HTTP `POST /api/admin/jobs/send-stalled-leads`, cron daily 08:00 (after the OWNER digest sequence at 07:00-07:15). Silent on agents with empty stalled list — no noise on quiet days. Same per-agent-iteration pattern as stage 36: per-agent failures caught, batch continues.
47. ✅ Doc expiry warning — per-doc visual indicator on jemaah edit page when `expiresAt` is approaching (≤60d amber, ≤14d ruby, EXPIRED ruby). Row gets a colored left-border + an "⏱ expire dalam Nh" pill + a one-click "↻ RENEW" button. Renew is a UI-only reset (status → PENDING, refNumber → empty, expiresAt → empty, focus refNumber input + flash highlight) — admin then types the new doc info and clicks SIMPAN, which goes through the existing upsert. REJECTED docs are excluded (not active credentials). No new endpoints — pure UI scaffolding around the existing `/api/jemaah/:id/documents` flow. The global "expiring soon" jemaah filter (`?expiringSoon=1`) already handles cross-jemaah triage.
48. ✅ Paket conversion funnel — new `PaketView(paketId, visitorId, dayKey, agentSlug?)` model with `@@unique([paketId, visitorId, dayKey])` so multi-refresh within the same day stays at one row (migration applied). Public `/p/:slug` route mints a non-httpOnly `rp_vis` cookie (32-hex random, 1-year max-age) on first visit; subsequent visits reuse the same id. Visit upsert is fire-and-forget — landing page render never blocks on the analytics DB write. Admin overview gets a "Konversi paket" panel: per-paket unique visits + non-CANCELLED bookings + `conversionPct = bookings ÷ visits × 100` over last 30 days. **`conversionPct=null` when visits=0 but bookings>0** (avoids divide-by-zero and misleading "∞%" — admin still needs to see those rows because they reveal traffic-blind bookings). Sort by visits desc. Walk-in agents who visit a paket page also get counted — the signal is "someone landed", role-agnostic.
49. ✅ Conversion attribution on Booking — `Booking.firstViewAt` + `viewCount` columns (migration applied) snapshot the visitor's view history at create time. `/api/booking` POST reads `rp_vis` cookie, calls `getVisitorAttribution({paketId, visitorId})` to compute first-touch + visit count from existing `PaketView` rows, and passes the result to `createBooking` via the new `visitorAttribution` param. **Best-effort lookup** — a missing cookie or no prior views just yields null, which createBooking stores as default `viewCount=0` / nulls. Admin booking-detail page renders an "Attribution" strip below the notes when any field is populated: "visit 3× sebelum booking · pertama 5h sebelum · varian hero B · via fb / cpc · ramadhan-2027". Hidden cleanly for admin walk-ins and pre-S48 bookings.
50. ✅ Hero A/B test — optional `Paket.heroTitleHtmlVariantB` column (migration applied) lets admin define a second hero copy. When set, `/p/:slug` deterministically picks A or B per visitor cookie (parity of first hex digit, so refreshes are stable). `PaketView.heroVariant` records which one was served. Paket-edit form gets a second textarea labeled "VARIAN B (opsional)" and a live A/B breakdown panel showing per-variant conversion%, with winner declared only when combined sample ≥30 visits (below that the noise dominates the signal — declaring a winner early would mislead). `pickHeroVariant` exported helper makes the bucketing testable. Booking row also snapshots which variant the converting visitor saw via `heroVariant` column — preferring the most-recent non-null in case admin paused the test mid-flight.
51. ✅ UTM attribution — `PaketView.utmSource/utmMedium/utmCampaign` columns (migration applied) capture campaign tags from `?utm_source=`/`?utm_medium=`/`?utm_campaign=` on first visit. **First-touch wins** — later visits within the same day are no-ops via the upsert update-no-op, and `getVisitorAttribution` pulls UTM from the oldest row. Booking also snapshots these so admin booking-detail and analytics queries can answer "which campaign drove this LUNAS". Admin overview gets a "Sumber kampanye (UTM)" panel grouping all paket × all visits by (source, medium, campaign) with visit / booking / conversion% columns. Direct/no-UTM bucket rendered italic to distinguish from real campaigns. Length-capped at field write (80/80/120 char) so a malformed URL can't blow up the column.
52. ✅ CTA A/B test — extends stage 50 hero A/B to the booking submit button text. New `Paket.ctaTextVariantA/B` columns (migration applied) carry the override text per variant. **Reuses heroVariant bucketing** so one A/B run tests both surfaces simultaneously per visitor — admin attribution gets cleaner read of "B converted better because of which copy". Both null → fall back to hardcoded "Kunci kursi saya →" so existing paket without test config don't suddenly read different copy. Waitlist label always wins when paket is full (variant text shouldn't override "Daftar waitlist"). Form-reset JS reads `data-cta-default` attr so error recovery keeps the bucketed variant visible.
53. ✅ Traffic anomaly alert — daily 08:30 email to OWNER/SUPERADMIN/MANAJER_OPS when a paket's yesterday visit count drops ≥50% vs 7-day rolling mean AND baseline ≥5 visits/day (so sleepy paket don't fire). New `TRAFFIC_ANOMALY_OWNER` notif type. CLI `npm run job:send-traffic-anomaly`, HTTP `POST /api/admin/jobs/send-traffic-anomaly`, cron daily 08:30 + systemd timer. **Silent when no anomalies** — quiet days don't generate noise. Email body suggests likely root causes (ads paused, landing broken, campaign expired, competitor outranked).
54. ✅ Jemaah cohort retention — `src/services/cohortRetention.js → getJemaahCohortRetention({months=12})` groups jemaah by month of first non-cancelled booking, computes % who had a 2nd non-cancelled booking within 365 days. Per-month table on admin overview shows cohort label / total jemaah / retained / retention% / mature flag. **Mature flag**: cohort is "mature" only when its end-of-month + 365 days < today (the 12-month window has fully elapsed). Immature cohorts render dimmed because their retention pct is still trending up. Header KPI averages across mature cohorts only (the only honest number). CANCELLED + REFUNDED excluded from cohort + retention math — those don't reflect a real customer relationship. PENDING/DP_PAID/LUNAS all count as a real second booking.
55. ✅ Cohort retention by acquisition channel — same service extended with `byChannel` rollup. `channelFor(booking)` resolves to `utm:<source>` when set, `agen:<slug>` when `agentSlugCap` is present, `direct` otherwise. Per-channel rollup across all cohorts in the window, filtered to channels with ≥5 jemaah (avoid showing volatile single-jemaah percentages alongside real channels). Sorted by total desc. Surfaced as a sub-table inside the cohort panel — admin sees which acquisition channel actually brings back repeat customers, not just which channel volume-wise dominates.
56. ✅ Landing speed budget — `PaketView.renderMs` column added; `/p/:slug` route measures end-to-end via `res.on('finish')` so the DB write is post-response (no added latency). `getLandingSpeed({days=7})` computes p50/p95/p99 globally + per-paket worst-5 by p95 (≥5-sample minimum for stable percentiles). Budget 800 ms — `overBudget=true` when global p95 crosses it, panel border turns ruby. `lowSample` flag when total < 50 visits in the window so admin knows the percentiles are still noisy. Repeat visits within a day overwrite `renderMs` so the metric reflects the latest experience; other fields stay first-touch. New `LANDING_SLOW_OWNER` enum (migration applied) reserved for a future alert job.
57. ✅ Auto-archive stale leads — wired into the existing weekly `prune` job (no new cron entry). `archiveStaleLeads(now, days=180)` soft-deletes COLD + LOST leads with `updatedAt < cutoff` by setting `deletedAt = now`. **WARM stays forever** (still workable — agent should follow up) and **CONVERTED stays forever** (booking history). Soft delete means rows survive in DB for retrospective queries; the agent's pipeline view already filters `deletedAt: null` so archived leads disappear from active CRM. Window tunable via `RETENTION_STALE_LEAD_DAYS` env var. Counts surface in the prune job's audit row.
58. ✅ Landing slow alert — wires the `LANDING_SLOW_OWNER` enum from S56. Daily 08:45 job `send-landing-slow` reads `getLandingSpeed` snapshot and emails ACTIVE OWNER/SUPERADMIN/MANAJER_OPS (KASIR excluded — perf is engineering, not cashier) when **p95 > 800ms AND not lowSample**. Silent on healthy days OR when the sample is too small to be meaningful (lowSample). Body shows p50/p95/p99 + worst 5 paket by p95 + suggested root causes (DB query, large hero asset, CPU saturated, connection pool). CLI `npm run job:send-landing-slow`, HTTP `POST /api/admin/jobs/send-landing-slow`, cron + systemd timer 08:45 (after traffic-anomaly at 08:30).
59. ✅ Lead reactivation hint — on `/admin/jemaah/:id/edit`, query soft-deleted leads matched by the jemaah's phone (last 8 digits, so country-code mismatches don't hide matches). Gold-bordered panel above the booking history shows up to 5 archived leads with `deletedAt` date + agent + last status + truncated notes. Surfaces prior CRM context that S57 auto-archive removed from the active pipeline. **No new endpoints** — pure read on the existing Lead table.
60. ✅ Paket-edit view sparkline — `getPaketDailyViews({paketId, days=30})` returns zero-filled per-day visit counts (so quiet days are visible dips, not x-axis collapses). Inline SVG sparkline (320×36 px) renders above the hero-title field on `/admin/paket/:slug/edit` with total + last-7d + week-over-week arrow. Hidden when total=0 (no signal to show). Admin sees traffic trend in the same field where they edit the copy that drives it — context for the A/B copy decisions in S50/S52.
61. ✅ ROI on cross-agen leaderboard — new `Paket.adsSpendIdr` + `adsNotes` columns (migration applied). `getPerPaketLeaderboard` now computes `roiX = lunasRevenue ÷ adsSpendIdr` (multiplier form, more intuitive than % for marketing teams) and `netPostAds = netMarginIdr − adsSpendIdr`. Renders as a third row below the cost/margin strip on each card, colour-coded (≥5× emerald / ≥3× gold / ≥1× amber / <1× ruby). **roiX=null when adsSpendIdr unset** — leaderboard renders "—" rather than "0×" to avoid misleading "no ads → infinite ROI" reading. Form on paket-edit uses the same 3-state preprocessor pattern as `costPerPaxIdr` so empty input persists as null.
62. ✅ Booking-detail audit timeline — replaced the flat list on `/admin/bookings/:id` with a vertical timeline grouped by category. `categorise()` view-side helper maps each audit row to `{created, money, cancel, notes, other}` with corresponding icon (●/$/✕/📝/•) and color (emerald/gold/ruby/sapphire/ink-100). Bullets sit on a connecting line; each event shows action + entity + time + actor email/role + a compact snapshot of changed fields (status, paid, method, kamar, cancel reason, merged fields, etc.). No service change — pure view reorganisation. Reads as a narrative instead of an undifferentiated list.
63. ✅ Public testimonial CRUD — new `Testimonial(paketId?, jemaahName, body, rating, photoUrl?, status, sortOrder)` model with `TestimonialStatus` enum DRAFT|PUBLISHED (migration applied). Admin CRUD at `/admin/testimonials` (list with status filter + new/edit/delete forms). Service validates min 10-char body + 2-char name. **Nullable paketId** lets admin author either paket-specific testimonials (shown on that paket's `/p/:slug` only) OR generic ones (shown on every paket). Public-side: `getPublishedTestimonialsForPaket(paketId)` returns PUBLISHED rows matching `paketId OR null`, rendered as cards between trust strip and FAQ on `/p/:slug`. Sidebar link in admin Tools group. No file upload baked in — admin pastes a path under `/uploads/testimonials/` for the photoUrl (the existing public uploads dir).
64. ✅ Testimonial KPI in conversion panel — `getPaketConversion` returns a `testimonialSplit` rollup grouping active paket into "with PUBLISHED testimonial" vs "without". Lift % shown when both buckets have ≥10 visits (sample size guard). **Generic testimonials count as "everyone has one"** — admins running a generic-only setup see no split (correct: there's no controlled variable). Two-cell strip with per-bucket conversionPct + paket count + visit/booking counts, rendered above the per-paket table on the admin conversion panel.
65. ✅ Per-crew (MUTHAWWIF) weekly digest — Monday 07:20 email per ACTIVE crew with their previous-week attendance activity (marks count / present / absent / paket touched) + upcoming paket assignments within 30 days. New `CREW_WEEKLY_DIGEST` notif type (migration applied). `recipientUserId` set so the row would scope correctly if a /crew/notifications inbox is added later. **Silent on idle weeks** — when both attendance count = 0 AND upcoming assignments = 0, the email doesn't fire. CLI `npm run job:send-crew-weekly-digest`, HTTP `POST /api/admin/jobs/send-crew-weekly-digest`, cron + jobRunner interval registered. Per-crew failures caught so a bad row doesn't abort the batch.
66. ✅ Public conversion modal — soft WA modal on `/p/:slug` fires once per visitor (24h localStorage cooldown). **Trigger**: scrolled past 80% of viewport AND idle 30s with no input (no scroll/touch/mousemove). WA-first because most Indonesian conversions land in chat first, form second. Pre-fills the message with paket title + agent name; defaults to office WA when no agent is resolved. CTA click also persists the dismiss key so a converted visitor doesn't see the modal on their next page within cooldown. Pure client-side — no backend changes, no DB writes, no analytics ping (intentional: this is a nudge, not a metric).
67. ✅ Crew weekly digest deltas — extends S65 with previous-week aggregation + delta arrows on each metric (mirrors S29/S33 for the OWNER digests). New `aggregateCrewWeek({userId, start, end})` helper used for current and previous windows in parallel. Polarity-aware: `absentCount` is reverse (more = bad), the rest forward. `fmtEmailDelta` from notifications.js reused so the visual style stays consistent across all weekly emails. Template gets `{{trend*}}` placeholders per metric.
68. ✅ Speed tier badge on leaderboard — `getPaketSpeedMap({days=7})` returns per-paket p95 latency map (single batched query). `getPerPaketLeaderboard` attaches `speedTier ∈ {fast, acceptable, slow, unknown}` per row based on the S56 800ms budget (fast ≤400 / acceptable ≤800 / slow >800). **Unknown when sample < 5** — admin sees nothing rather than a misleading badge. Renders as a small mono-font pill next to the revenue figure in each leaderboard card; title attr shows `p95 + sample` for hover details. Falls through gracefully when no PaketView rows have renderMs (pre-S56 dev DB).
69. ✅ Jemaah self-service testimonial submit — `GET/POST /saya/bookings/:id/testimonial` (jemaah only, owns booking, booking must be LUNAS — DP-only doesn't earn a public voice). Form lets jemaah pick rating (1–5 stars), write body (20–2000 chars), optional city. **Always lands DRAFT** — admin still controls what appears on /p/:slug. Re-submit policy: existing testimonial for (paketId × jemaahName) is updated in place + flipped back to DRAFT for re-review (mirrors S47 doc re-verify behaviour, prevents duplicate rows on form refresh). Audit row marks `selfSubmit: true` for grep. `/saya/bookings/:id` view shows a gold-bordered CTA when status=LUNAS and a green success banner after submit. 5 HTTP-integration tests cover the gates.
70. ✅ Testimonial published notif — new `Testimonial.submittedByUserId` FK (S69 + S70 migration) populated by the jemaah self-submit route. When admin's `updateTestimonial` flips DRAFT→PUBLISHED AND `submittedByUserId` is set AND the submitter is ACTIVE, fires a `TESTIMONIAL_PUBLISHED` EMAIL with `recipientUserId` set so the jemaah's `/saya/notifications` inbox + unread badge pick it up. **Fire-and-forget** — admin save never blocked by notif failure. Skips silently on admin-authored testimonials (null submittedByUserId) and on PUBLISHED→DRAFT (unpublish). New `TESTIMONIAL_PUBLISHED` enum.
71. ✅ Crew public profile page — `CrewProfile` extended with `slug` (unique, nullable), `titlePrefix` ("Ustadz", "H.", "KH."), `bio` (Text), `photoUrl`. Public route `/c/:slug` renders crew bio + upcoming-12-months assigned paket + last-4-months past paket. **Opt-out by default** — crew with no slug, no bio AND no photoUrl returns 404, so a muthawif who hasn't filled their profile isn't accidentally surfaced. `/p/:slug` landing pulls assigned crew with public slugs and shows a "Ditemani tim berpengalaman" strip with circular photo cards linking to the profile. Strip hidden when no crew has a public profile.
72. ✅ ICS calendar export — `GET /saya/bookings/:id/calendar.ics` returns a RFC 5545 VCALENDAR with one VEVENT (all-day DTSTART/DTEND covering departure→return, +1 day on DTEND per spec). DESCRIPTION carries booking ref + kelas + maskapai + route + deep link back to `/saya/bookings/:id`. Stable UID = `<bookingId>@religio-pro` so re-imports update the calendar event rather than duplicating. VALARM 3 days before departure for prep reminder. Special chars escaped per spec (`\;` `\,` `\\` `\n`); long lines folded at 70 chars with `CRLF + space`. Ownership enforced via `getMyBooking`'s `jemaahUserId` filter (404 on cross-user). Status-gated: cancelled/refunded bookings hide the CTA. "📅 + KALENDER" link sits next to "🖨 CETAK VOUCHER" in the booking detail crumb bar.
73. ✅ Admin crew profile editor — extends the existing /admin/users/:id/edit MUTHAWWIF panel with `crewSlug` + `crewTitlePrefix` + `crewBio` + `crewPhotoUrl` inputs. UserAdmin schema + service handle 3-state (undefined = no change, "" = clear → null, value = stored) so admin can wipe a public profile by clearing the slug. Slug lowercased on write; whitespace stripped. Hooks into the S71 `getCrewPublicProfile` opt-out rule — clearing all fields takes the crew off the public `/c/:slug` directory in one form save.
74. ✅ Public agen profile — `/a/:slug` mirror of S71 for `AgentProfile`. Added `AgentProfile.photoUrl` column (migration). Form panel extended with `agentPhotoUrl` + `igHandle` inputs. Public route + `agent-public.ejs` view renders bio + verified badge + stats (lifetime LUNAS count, active paket count, years with Religio Pro) + upcoming paket cards. **Three-condition opt-in**: profile must have `isVerified=true` AND active user AND (bio OR photoUrl). All paket links carry `?a=<slug>` so referral attribution flows back. WA + IG action buttons in hero.
75. ✅ First payment thanks — fires once per booking when the first PAID Payment row lands (counts prior PAID rows excluding the current one, == 0 means this is the first). Onboarding tone — "thanks + here's what's next" (doc checklist, manasik schedule, agen contact) — distinct from the neutral kuitansi `notifyPaymentReceived`. New `FIRST_PAYMENT_THANKS` enum (migration applied). `recipientUserId` set when booking is linked so the notif also shows in `/saya/notifications` with unread badge. Anonymous bookings still get the email but skip the inbox. Subsequent partial payments DP→PARTIAL→LUNAS don't re-trigger.
76. ✅ Public agen leaderboard — `GET /agen-leaderboard` lists top 10 verified agents by lifetime LUNAS count. Three-condition opt-in (`isVerified=true` AND active user AND bio OR photoUrl) so suspended/private agents don't surface. Stable sort by lunasCount desc then displayName. Rendered with rank badges (🥇🥈🥉 for top 3) + photo + tier + years active + lunas counter. Social proof for the network: links to /a/:slug for each row. Empty state shows when no verified agents qualify yet.
77. ✅ Email click tracking — new `EmailClick` model (`@@unique([notificationId, targetUrl])` so repeat clicks bump `clickCount` instead of duplicating rows). `lib/emailClickToken.js` signs URLs via HMAC-SHA256 of `(notifId, url)` truncated to 16 hex chars; `/r/<notifId>.<b64>.<sig>` redirects via constant-time-compare token verify. **Wrap happens inside `dispatchNotification`** for EMAIL channel only — body is mutated in-flight so the DB row stays unchanged (admin viewer shows original URLs). Conservative URL whitelist: absolute http(s) + root-absolute paths under known prefixes. mailto/tel/anchors pass through unwrapped. Open-redirect proof via the signature requirement. Pruning excluded — admin needs full history for arbitrary CTR windows.
78. ✅ WA fallback on email bounce — when EMAIL channel hits terminal FAILED (`nextRetryAt=null` after max 5 attempts) AND notif type is in the critical set (BOOKING_CREATED, PAYMENT_RECEIVED, BOOKING_LUNAS, REFUND_ISSUED, FIRST_PAYMENT_THANKS, PAYOUT_CREATED) AND `recipientPhone` is set, auto-enqueues a WA duplicate. Admin digests (DAILY/WEEKLY/CREW/etc.) deliberately excluded — those land in the queue dashboard if email bounces; bulk fallback would worsen UX. **Idempotent**: enqueue checks for an existing WA row with the same (recipientPhone, type, relatedEntity, relatedEntityId) to avoid double-firing on retried terminal failures. Payload carries `fallbackFromEmail: <emailNotifId>` for the audit trail. Best-effort — fallback failure never blocks the original failure recording.
79. ✅ Email CTR dashboard — `getEmailCtrByType()` in `src/services/emailCtr.js` joins Notification with the S77 EmailClick rows (`clicks: { some: {} }` predicate) and groups by notif type to report sent / clicked / CTR% per type. **`lowSample` flag when sent < 5** so the panel can dim noisy rows (one click on three sent = 33% CTR isn't actionable). Sorted by sent desc — types with the most volume bubble to top. Surfaced on `/admin` overview between needs-attention and S51 UTM panel. Read-only — admin uses it to spot dead templates (DAILY_DIGEST_OWNER with 0% CTR over 90 days = nobody opens the email link).
80. ✅ SOS auto-escalate — `Incident.escalatedAt` column + new `escalateStaleIncidents({olderThanMin=60})` service. Cron `*/15 * * * *` finds OPEN incidents older than 60min with `escalatedAt=null`, EMAILs only OWNER tier (tighter than stage 13's initial OWNER/SUPERADMIN/MANAJER_OPS fan-out — assumes admin desk missed the first alert), then stamps `escalatedAt` so the next run skips the row. **`escalatedAt` is set-once** — ACK/RESOLVE never clear it; re-escalation isn't a thing. New `INCIDENT_ESCALATED` enum + template. CLI `npm run job:send-incident-escalate` + HTTP `POST /api/admin/jobs/send-incident-escalate` + systemd timer + jobRunner interval (15 min). The state machine still flows OPEN → ACKED → RESOLVED — escalation is orthogonal, just a louder ping.
81. ✅ Booking note @-mentions — `updateBookingNotes` parses `@email@domain` tokens out of the notes text and fires `BOOKING_NOTE_MENTION` EMAIL to matching ACTIVE users. **Diff by-email, not by-token-position** — adding context around an existing mention doesn't re-fire ("@cs@religio.pro" already in old notes → present in new notes → no notif). New mentions only. Self-mentions filtered (actor's own email skipped). Unknown emails silently no-op (no enqueue for typos). `recipientUserId` set so if the mentioned user has a `/saya` inbox they see it. Mention parser exported as `extractMentionEmails` for future inline-highlight surfaces. Hint text added to the admin notes textarea: `@email@domain untuk mention`.
82. ✅ Mention autocomplete — typing `@` in the booking notes textarea triggers a dropdown of matching ACTIVE staff. `GET /admin/bookings/mention-search?q=<2+ chars>` returns up to 10 (OWNER/SUPERADMIN/MANAJER_OPS/KASIR/SALES) — AGEN/MUTHAWWIF/JEMAAH excluded since the @-mention surface is staff-only. **Min 2 chars** to prevent full-table scans from backspace hammering. Same RBAC as notes-edit (CANCEL_ROLES — KASIR can't edit notes so they don't get the dropdown either). Pure browser JS, no framework: ArrowUp/Down to navigate, Enter/Tab to commit, Escape closes. Inserts `@email@domain ` at the trigger position so the existing S81 mention parser picks it up unchanged.
83. ✅ Incident response SLA report — `src/services/incidentSla.js → getIncidentSlaReport({weeks=8})` aggregates Incident rows per Mon-Sun cohort (created in that week) with ack/resolve p50+p95 latency + escalation rate. Panel above the queue on `/admin/incidents`. **Cohorting is by `createdAt` week**, not `resolvedAt` — the question is "how responsive were we to incidents that arrived in week X". Open-but-aging rows count toward `created` (denominator) but NOT toward the percentile pool — mixing nulls would distort latencies. **`escalationRatePct=null` when created=0** (no divide-by-zero). Current week excluded — it's still accumulating. Uses `localYmd` helper (not `toISOString().slice(0,10)`) to avoid the TZ-truncation bug that bit the daily digest.
84. ✅ Email click heatmap — `getEmailClickHeatmap({type, days=30})` joins EmailClick → Notification (filtered by `type` + `sentAt`) and aggregates per **normalised URL** (scheme + host stripped via `new URL()`, path+query kept). Lets `https://prod/admin?tab=ops` and `http://localhost:3001/admin?tab=ops` collapse to one row so dev + prod don't fragment the data. Sorted by clicks desc + `sharePct` per URL. Drill-down view at `/admin/email-ctr/:type` — accessed by clicking a type row in the S79 panel (link only rendered when `clicked > 0`). **Defensive against unknown enum**: bookmarked-but-since-renamed type returns empty rows instead of 500 (Prisma's enum rejection caught + downgraded).
85. ✅ WA click tracking — S77's `/r/<token>` redirect was always channel-agnostic; the gap was that `dispatchNotification` only wrapped EMAIL bodies. S85 extends the wrap to WA. `wrapUrl` now prepends `PUBLIC_BASE_URL` when configured (required for WA — chat clients only auto-detect absolute http(s) URLs); without the env it falls back to a path which is fine for HTML email but silently no-op for WA. `getEmailCtrByType` groups by `(type, channel)` so the CTR panel shows EMAIL + WA side-by-side. `getEmailClickHeatmap` accepts optional `channel` query so the drill-down can lens to one channel or combine both. EmailClick model name kept (not renamed to MessageClick) — the click semantics are channel-agnostic and a rename would invalidate every existing row without functional benefit.
86. ✅ Notes mention history — `BookingMention(bookingId, userEmail, userId, mentionedById, mentionedByEmail, createdAt)` denormalised table (migration applied) populated by `notifyBookingNoteMention` alongside the EMAIL fan-out. Indexed by `(userEmail, createdAt)` for fast per-user lookups. Surfaced on `/admin` overview as "Anda di-mention" panel showing the viewer's last 30 days of mentions with deep links to each booking + age + actor email. **Best-effort insertion** — failure to write the BookingMention row never blocks the notif enqueue (the notif is the load-bearing signal; the index is convenience). Why denormalised over querying Notification queue: S14 retention prunes notif rows, the inbox-style surface needs a separate persistence layer.
87. ✅ Incident SLA targets + alerts — `src/services/incidentSlaAlert.js → SLA_BUDGETS` constants per IncidentType (SOS ack 5min/resolve 30min, MEDICAL 15min/1h, LOST_JEMAAH 30min/4h, SECURITY 1h/24h, LOGISTICAL+OTHER 2h/48h). `getIncidentSlaBreaches({minSample=3})` runs over the previous full Mon-Sun week, returns one row per (type, metric) where p95 > budget. **Skips low-sample types** — fewer than 3 incidents can't make a statistical claim. **Silent on healthy weeks** — no breach = no email. `notifyIncidentSlaBreach` fans out to OWNER/SUPERADMIN/MANAJER_OPS (KASIR excluded). New `INCIDENT_SLA_BREACH_OWNER` enum (migration applied). Cron Mon 07:25 (after the OWNER digest sequence). Budgets are code constants (not DB) — there's one correct set per role/operation, not a per-admin tweak surface.
88. ✅ Mention shortcodes — `MentionShortcode(code, userId)` table (migration applied) maps short tokens like `:cs` → a staff user. Expansion happens at save in `updateBookingNotes` BEFORE the S81 parser runs, so stored notes carry the full `@user.email` and the existing diff-by-email + audit flow is unchanged. **Unknown codes left as-is** (admin typo visible, not silently dropped). **Codes targeting suspended/deleted users skip expansion** — the code stays as `:cs` so admins notice the stale link instead of mentioning the wrong person. Admin CRUD at `/admin/users/shortcodes` (OWNER+SUPERADMIN). Autocomplete extended: `:` triggers `/admin/bookings/shortcode-search` (min 1 char), `@` continues to fetch staff (min 2 chars). Codes lowercased + validated `[a-z0-9_-]{1,40}`.
89. ✅ Incident timeline UI — view-only enhancement on `/admin/incidents/:id`. Each leg shows elapsed time (`+10m`) and the budget reference (`/ budget 5m`); breached legs flip ruby + bold. Added visual SLA bar above the timeline: emerald/amber/ruby track shows current age vs `slaBudget.resolveMs`, with gold marker lines at the ack budget and resolve budget. Includes pending-state rows ("⏱ belum di-ack — sudah 20m melewati budget 5m") when an OPEN/ACKED incident is past its budget without the next state transition. Escalation event (S80) gets its own timeline row. Route resolves `SLA_BUDGETS[incident.type]` from S87 and passes to the view.
90. ✅ WA consent audit trail — `JemaahProfile.notifWaConsentAt` + `notifWaWithdrawnAt` columns (migration applied; **no backfill** — old profiles keep null because stamping NOW() would be a dishonest "consented just now" claim). Register flow stamps `consentAt = now` so new accounts have an authoritative first-opt-in date. `updateJemaah` watches for `notifWa` flips: opt-out → stamps `withdrawnAt`; opt-in → stamps `consentAt` AND keeps the last `withdrawnAt` (durable history). No-op + omitted-field paths leave both timestamps alone. Surfaced on jemaah self-profile + admin jemaah-form. Useful when a jemaah complains "I never agreed to WhatsApp" — the timestamp + register-flow tie shows otherwise.
91. ✅ Mention-driven follow-up tasks — `Task(bookingId, assigneeEmail, body, dueAt, status)` + `TaskStatus` enum (migration applied). Parser `extractTodos` reads `@email TODO body [by YYYY-MM-DD]` from notes and `upsertTodosForBooking` creates Task rows alongside the S81 mention fan-out. **Idempotent on (bookingId, email, body)** — re-saving same notes creates no duplicates. Date parsing limited to explicit `by YYYY-MM-DD` (NLP is brittle); malformed dates → dueAt null. "Tugas saya" panel on `/admin` overview lists OPEN tasks for the viewer's email, overdue first, with one-click DONE/CANCEL forms. POST `/admin/tasks/:id/complete` + `/cancel` — any 4 admin roles can clear queues for each other; `completedByEmail` records who did it.
92. ✅ Per-paket cost vendor breakdown — `PaketCostLine(category, amountIdr, vendorNote, sortOrder)` + `CostCategory` enum (HOTEL/FLIGHT/VISA/MEALS/GROUND_OPS/GUIDE/INSURANCE/OTHER; migration applied). When at least one line exists, the auto-sync trigger writes `SUM(lines)` back to `Paket.costPerPaxIdr` so the leaderboard margin math (S22) keeps working unchanged. **Deleting the LAST line does NOT clear the column** — preserves the prior sum as a manual estimate fallback (admin who deletes all lines didn't necessarily want margin to disappear). JSON CRUD at `/api/paket/:slug/cost-lines`. Cross-paket per-category rollup helper for future leaderboard insight. Inline JSON-driven panel on paket-edit lets admin add/delete lines without leaving the page.
93. ✅ PWA push for jemaah — extends S17 admin web-push to JEMAAH role. New `pushToUser(userId, payload)` mirrors `pushToAdmins` but scoped by userId. Jemaah subscribe surface at `/api/saya/push/{config,subscribe,unsubscribe}` (parallel to `/api/admin/push`). `notifyBookingCreated` + `notifyPaymentReceived` now fire push to the jemaah's installed `/saya` PWA when subscribed. **Best-effort** — push failure never blocks the EMAIL/WA queue writes (those remain load-bearing). **`pushToAdmins` still excludes jemaah** via the role filter in the WHERE clause — same PushSubscription table, role-based selection per fan-out kind. Silent when jemaah hasn't installed the PWA / disabled notifications.
94. ✅ Jemaah PWA subscribe UI — `shared/push-jemaah.js` mirrors `push-admin.js` against the `/api/saya/push/*` endpoints. Toggle bar on `/saya` dashboard (icon + status text + AKTIFKAN/MATIKAN buttons) auto-paints based on `Notification.permission` + current `PushManager` subscription. Browser-not-supported / permission-denied paths render an explanatory line + hide the buttons (no broken-button confusion). Same DOM contract as admin (`#rp-push-{bar,status,enable,disable}`) so styling can be reused. HTTP integration tests cover auth gating + payload validation.
95. ✅ Per-category cost benchmarks — `getCostBenchmarks({paketId})` compares this paket's per-category total spend (from S92's lines) against the network median across non-archived paket. Flags 'high' when amount ≥ 2× median, 'low' when ≤ 0.5×, null when within range. **Sample guard**: skips flag when fewer than 3 peer paket in the category — too few to make a statistical claim. **Aggregates per (paketId × category)** before computing median so multi-line paket (Hotel Madinah/Mekkah/Aqsa as 3 rows) compare apples-to-apples with single-line paket. Surfaced as a gold-bordered warning strip above the lines panel on paket-edit; only renders when ≥1 flag exists. Median (not mean) so a single outlier doesn't drag the benchmark for everyone.
96. ✅ Task SLA escalation — `getOverdueTasks({graceHours=48})` finds OPEN tasks past `dueAt + grace`. Daily cron at 09:00 fans `TASK_OVERDUE_ESCALATION` EMAIL to OWNER/SUPERADMIN/MANAJER_OPS (KASIR excluded — task workflow is ops, not cashier). **7-day per-recipient cooldown**: notif dedup query checks if same admin email got the same notif type within last 7 days; if so, skip enqueue. Avoids inbox spam on long-stuck tasks while still surfacing them weekly. Silent on healthy days (zero overdue → zero email). New `TASK_OVERDUE_ESCALATION` enum (migration applied). CLI `npm run job:send-task-overdue` + HTTP `POST /api/admin/jobs/send-task-overdue` + systemd timer + jobRunner interval.
97. ✅ Push debug page — `/admin/push-debug` lists every PushSubscription (admin + jemaah) with user, role, user-agent, endpoint preview, lastUsedAt, and a per-row "TES KIRIM" button. `listAllPushSubscriptionsForDebug` flags subs owned by suspended/deleted users as stale (dimmed in the table). `sendTestPushToSubscription(subId)` reuses the same `sendOne` path as production fan-outs so a successful test confirms VAPID config + the SW push handler end-to-end. Page header shows current push mode (web-push vs fake) + whether `VAPID_PUBLIC` is configured. Closes the observability gap: previously a stale subscription was invisible until "nobody got the alert" was reported.
98. ✅ Booking activity feed — `getBookingActivityFeed(bookingId, {limit=150})` merges AuditLog, Payment, Komisi, BookingMention, Task, Notification, JemaahDocument, AttendanceMark rows into one chronological feed (newest first). Renders as a unified vertical timeline above the audit-only timeline on `/admin/bookings/:id`. Per-row kind badge color-codes the source. Each kind has a one-line `label` (e.g. "Pembayaran PAID · Rp 5jt · TRANSFER", "Task → @cs: verify passport"). **Bounded at 150 rows** so the page doesn't blow up on long-lived bookings; deep history still lives in `/admin/audit`. Best-effort render — feed failure is logged but the booking detail still loads from the existing panels.
99. ✅ Agent commission forecast — `getAgentCommissionForecast({agentId, windowDays=90})` projects expected komisi from non-LUNAS active bookings using `total × rate × statusProbability` where probability is PENDING 30% / BOOKED 50% / DP_PAID 70% / PARTIAL 85% (LUNAS excluded — has a real Komisi row). Rate chain mirrors `payment.js` S22 (matrix > agent override > paket rate > DEFAULT). Returns per-month buckets + per-status breakdown + total. Surfaces as a gold-bordered "Proyeksi komisi" panel on `/agen` Wallet tab when an agent has any non-LUNAS active booking. Helps agents plan; pairs with S37 payout reminder for the admin side. CANCELLED/REFUNDED bookings excluded — undone commitment, not pipeline.
100. ✅ Cross-agent komisi forecast — `getAllAgentsCommissionForecast({windowDays=90})` rolls up S99 across every ACTIVE agent. Returns `perAgent` (sorted by expected desc), `perMonth` (chronological + unscheduled last), and grand totals. Panel on `/admin` overview shows top 10 agents + per-month buckets side-by-side so OWNER can plan cash-flow outflow. Agents with zero active bookings skipped (only those with pipeline contribution appear). Sequential per-agent fetch — fine at typical scale (≤50 agents), revisit if it grows.
101. ✅ Voucher PDF download — `pdfkit` (~100KB pure-JS, no Chromium) renders a programmatic A4 voucher with header / jemaah / perjalanan / pembayaran / agen / itinerary blocks. `streamVoucherPdf(voucher, res)` pipes directly to the HTTP response with `Content-Disposition: attachment; filename="voucher_<bookingNo>.pdf"`. Filename sanitised — `[^A-Za-z0-9_-]` stripped so no path traversal even if `bookingNo` is corrupted. Routes: `GET /admin/bookings/:id/voucher.pdf` (4 admin VIEW roles) + `GET /saya/bookings/:id/voucher.pdf` (jemaah, scoped via `jemaahUserId`). CTA: "⇩ PDF" button next to existing "🖨 CETAK" on both detail pages. **Why pdfkit not playwright/puppeteer**: no native Chromium binary, no manual `playwright install`, no 2-3s cold start.
102. ✅ Inactive jemaah prune — `pruneInactiveJemaah` (wired into the weekly S14 prune) soft-deletes JEMAAH users that meet ALL: role=JEMAAH, deletedAt=null, createdAt < cutoff, lastLoginAt < cutoff OR null, AND `bookings: { none: { status: notIn [CANCELLED, REFUNDED] } }`. Bookings FK is SetNull on User delete so any historical paid bookings keep their `JemaahProfile` link (the profile is the customer record; the user row is just the login). Default 365 days; tunable via `RETENTION_INACTIVE_JEMAAH_DAYS`. CANCELLED-only history doesn't save the account (no real customer relationship). OWNER/SUPERADMIN/MANAJER_OPS/KASIR/AGEN/MUTHAWWIF excluded by role gate.
103. ✅ Voucher PDF branding + i18n — header is now a centered ornamental brand mark with gold rule + diamond divider; footer has gold-rule + per-locale blessing line + fineprint. `?lang=id|en|ar` query (default `id`) flips section labels + footer through a `LANGS` table. **Arabic is romanised** ('AL-HAAJ', 'Baarakallahu fiikum…') — embedding Amiri TTF (~400KB) would dwarf the rest of the repo for marginal voucher benefit; if real Arabic glyphs become hard-required later, register a font via `doc.registerFont`. Unknown lang falls back to `id`. Filename suffix `_en` / `_ar` so admin can save multiple variants without overwrite. Booking detail page gets an `ID/EN/AR` dropdown next to the PDF button.
104. ✅ Soft-deleted users viewer — `listUsers` extended with `deleted: 'ACTIVE'|'DELETED'|'ALL'` (default ACTIVE — no behaviour change for existing callers). `/admin/users?deleted=DELETED` surfaces what S102 (and admin manual deletes) removed. Rows render dimmed with ruby `[DELETED]` tag; the action column shows the `deletedAt` date + a `RESTORE` form that POSTs to `/admin/users/:id/restore`. `restoreUser` is idempotent (no-op + no audit on already-active rows) and writes a labelled audit row (`after.restored = true`) when it does flip.
105. ✅ Booking export bundle — `GET /admin/bookings/:id/bundle.zip` packages `voucher.pdf` (S101) + `calendar.ics` (S72) + each jemaah document under `docs/<doc-id>__<sanitized-name>.<ext>` + a human-readable `MANIFEST.txt` listing what's inside. Streamed directly via `archiver` v8 (ZipArchive) — no temp file on disk; pdfkit pipes into a PassThrough shim that archiver consumes. **Missing files on disk are logged in MANIFEST under "NOT INCLUDED" rather than aborting the zip** — partial bundles are more useful than no bundle. Filename sanitised against path traversal. New `archiver` dep (pure ESM). "📦 BUNDLE" CTA on admin booking detail crumb.
106. ✅ CSV export in bundle — `?format=csv` query on `bundle.zip` swaps `voucher.pdf` for three CSVs: `booking.csv` (1 row of booking + jemaah + paket + totals), `payments.csv` (full payment history), `docs.csv` (doc metadata regardless of file presence). All CSVs are UTF-8 BOM-prefixed so Excel auto-detects encoding; RFC 4180 quoting. `calendar.ics` is dropped in CSV mode (accounting flow ≠ calendar import). Filename suffix `_csv.zip` so admin can keep both variants side-by-side. MANIFEST.txt reflects format in its title.
107. ✅ Bulk dossier export from manifest — `POST /admin/manifest/:slug/bundles.zip` with form-encoded `bookingIds[]` checkboxes packages each selected booking under `bookings/<bookingNo>/` (voucher.pdf + calendar.ics + docs/ — or CSVs when `?format=csv`). Cross-booking `BUNDLES_MANIFEST.txt` at the zip root lists who's included. **Per-booking shape mirrors single-bundle exactly** so a vendor handed one subfolder gets a standalone dossier. Capped at 200 bookings per request to avoid runaway zips. Per-booking lookup failure is logged + skipped, not fatal.
108. ✅ Outbound webhooks — `Webhook(url, secret, events[], status)` model (migration applied) + `dispatchEvent(name, payload)` service. Six canonical events: `booking.created`, `booking.lunas`, `booking.cancelled`, `payment.received`, `refund.issued`, `komisi.payout`. Each subscription gets a 32-byte hex secret; payload is HMAC-SHA256 signed and sent via `X-Religio-Signature: sha256=<hex>` header so partners can verify authenticity. **Per-row 8s timeout via AbortController** so a slow partner doesn't tie up the caller. Admin CRUD at `/admin/webhooks` (OWNER+SUPERADMIN). Wired into `createBooking`, `recordPayment` (payment.received + booking.lunas on transition), `issueRefund`.
109. ✅ Webhook delivery log + retry — `WebhookDelivery(webhookId, event, payload, signature, status, attemptCount, nextRetryAt)` model (migration applied). Each `dispatchEvent` call now inserts a pre-attempt row, fires once, then either marks SUCCEEDED or queues PENDING with backoff 1m/5m/30m/2h/12h (mirrors notif retry S77). **Payload + signature stored verbatim** so a retry re-uses the SAME signature the original used — receivers comparing a timestamp inside the body don't see drift on retry. `processPendingDeliveries` (CLI `npm run job:retry-webhooks` + HTTP `POST /api/admin/jobs/retry-webhooks` + cron every 2 min) picks PENDING rows whose nextRetryAt elapsed and re-fires. **SUSPENDED subs are skipped without burning a retry**. After 5 attempts the row is terminal FAILED. Per-webhook `LOG` link opens `/admin/webhooks/:id/deliveries`.
110. ✅ Manifest bulk-select UI — wires S107's `POST /admin/manifest/:slug/bundles.zip` into the Manifest tab. Checkbox column + select-all master (indeterminate when partial) + floating gold-bordered action bar at the bottom (hidden until ≥1 row checked) with "⇩ PDF dossier" / "⇩ CSV dossier" / "×" clear. Pure browser JS — no service change. Today the bulk endpoint goes from "exists but unreachable from UI" to "one click per dossier set".
111. ✅ Inbound webhooks — `POST /api/webhook-in/:source` accepts signed partner payloads (Fonnte delivery receipts, Zapier triggers, generic). `InboundWebhook(source, headers, payload, signatureValid, status)` model (migration applied) records every hit regardless of validity — failed-signature attempts stored as REJECTED for audit. Per-source verifier registry reads secrets from env (`WEBHOOK_IN_<SOURCE>_SECRET`) so rotation is ops-only. **No verifier configured = accept-anything dev mode** (signatureValid=null, status=RECEIVED). Per-source handler registry runs AFTER persistence; handler failure stamps `HANDLER_ERROR` for replay. Only `x-*` + content-type + user-agent headers kept (cookies/auth stripped). Admin viewer at `/admin/inbound-webhooks` with source+status filter + per-row REPLAY button. CSRF skip added for `/api/webhook-in/`.
112. ✅ Fonnte delivery handler — wired into S111's HANDLERS map. Match-by-phone strategy (no provider message id is stored): latest WA Notification with same `recipientPhone` (normalised — strip non-digits, leading 0 → 62), status=SENT, sentAt within last 24h. On status=failed → flip Notification.status to FAILED + stamp reason + clear nextRetryAt (message left our side; downstream failure, not worth re-queuing). On status=delivered/read → leave SENT + annotate `payload.fonnteDelivery` so the admin viewer can see receiver-side state. Silent on no match (late receipt for an older message → harmless).
113. ✅ API key auth — `ApiKey(name, hashedKey, scopes JSON, status)` model (migration applied) for partner-facing read APIs. Token format `rp_<id>.<secret>` where id is the cuid (non-secret, drives O(1) lookup) and secret is 32-byte hex bcrypt-hashed. Plaintext shown ONCE at creation. `requireApiScope('read:bookings')` middleware: 401 on bad/missing token, 403 on scope miss, attaches `req.apiKey` on success + stamps `lastUsedAt` fire-and-forget. SUSPENDED keys fail auth without deletion (rotation-friendly). Admin CRUD at `/admin/api-keys` (OWNER+SUPERADMIN). Three scopes today: `read:bookings`, `read:paket`, `read:notifs`.
114. ✅ Partner booking export API — `/api/v1/bookings`, `/api/v1/bookings/:id`, `/api/v1/paket`. Gated by S113 API key with respective scopes. List endpoint supports `?from=&to=&status=&page=&limit=` (cap 100, default 50). Returns paginated `{data, pagination, filters}` envelope with same field naming as the CSV bundle (S106). Single-booking endpoint adds `payments[]` inline. Versioned `/v1/` prefix so future breaking changes ship under `/v2`. CSRF skip added for `/api/v1/` (Bearer auth, no cookies).
115. ✅ API key rate limiting — `ApiKey.rateLimitPerMin` column (default 60, clamped 1..6000). `apiKeyRateLimit` middleware (mounted AFTER `requireApiScope`) reuses the shared S2 rate-limit store (in-memory or Redis depending on `REDIS_URL`) so per-key buckets share infra with the human-side limiter. Per-key bucket key = `apikey:<id>`. 429 with `Retry-After` + `X-RateLimit-Limit`/`-Remaining`/`-Reset` headers; under-limit calls populate the same headers so well-behaved clients can pace. **Fail-open on store errors** (Redis blip doesn't lock partners out). New `getRateLimitStore()` export from the middleware lets other limiters share the singleton.
116. ✅ OpenAPI spec for /api/v1 — `GET /api/v1/openapi.json` returns a hand-written OpenAPI 3.1 spec covering all three v1 endpoints (paths, parameters, response schemas, security with Bearer + per-endpoint scopes). `GET /api/v1/docs` serves a Swagger-UI HTML bootstrapper that pulls the bundle from a CDN. Both unauthenticated — discovery surface; partners need the spec to know how to authenticate. Spec auto-fills `servers[]` from `PUBLIC_BASE_URL` env or the request's `X-Forwarded-Host` / `Host`. Hand-written (not reflected from code) so accuracy stays a public contract — three endpoints make this the right cost/benefit.
117. ✅ Webhook test-fire — admin button on `/admin/webhooks` row + `POST /admin/webhooks/:id/test-fire` posts a synthetic signed event payload (`test.ping` by default; six sample shapes registered per canonical event name) to the partner URL. Renders HTTP response (status + first 500 chars of body + duration) inline as a result page. **Does NOT insert a WebhookDelivery row + does NOT bump lastFiredAt** — manual probe kept out of diagnostic surfaces. Adds `X-Religio-Test: true` header so partners can short-circuit logging. Closes the "did the partner actually receive my webhook?" gap during onboarding.
118. ✅ Webhook secret rotation — `POST /admin/webhooks/:id/rotate-secret` mints a fresh secret + archives the previous one as `prevSecret` with a `prevSecretExpiresAt` (default 24h, clamp 1..168). During the grace window every outbound POST carries BOTH `X-Religio-Signature` (new) and `X-Religio-Signature-Prev` (old) so partners can verify against either while they roll their config. New plaintext shown ONCE via query-flash; admin row shows an amber ROTATING badge with the grace expiry. Cleaner than "delete + recreate" which would break the integration mid-flight. Migration applied: `Webhook.prevSecret + prevSecretExpiresAt`.
119. ✅ Idempotency-Key on webhooks — `attemptDelivery` now sets `Idempotency-Key: <WebhookDelivery.id>` on every POST. Retries reuse the SAME header value (since the delivery row id is stable across attempts), so partners can dedupe replays that arrive after their endpoint actually succeeded but timed out responding. Industry-standard pattern from Stripe. No schema change — leverages S109's existing delivery row id.
120. ✅ Partner audit log API — `GET /api/v1/audit?entity=&entityId=&page=&limit=` returns paginated AuditLog rows scoped to one entity. New `read:audit` scope (added to `KNOWN_SCOPES`). 400 when entity/entityId missing — keeping payloads bounded by requiring caller to name the resource. Lets partner CRMs surface "who changed what" inline without raw DB access. OpenAPI spec updated.
121. ✅ API request log + per-key analytics — `ApiRequestLog(apiKeyId, path, method, statusCode, durationMs, scope, ip, ts)` model (migration applied). `apiRequestLog` middleware mounted at TOP of `/api/v1` (before auth) so failing-auth requests also leave a trail. Fire-and-forget via `res.on('finish')` — log write failures never block the request. `getApiKeyAnalytics({days=7})` rolls up per-key: requests / 5xx count + rate / p50+p95 duration / top 5 paths. Surfaces on `/admin/api-keys` as the top panel for keys with traffic. Pruned by weekly retention (60d default; tunable via `RETENTION_API_REQUEST_LOG_DAYS`).
122. ✅ Scope-use audit on token use — `ApiRequestLog.scope` column captures the scope each route required (`requireApiScope` stashes it as `req.apiUsedScope`). Analytics panel splits per-key usage into "scope used" (emerald pills with count) + "granted but unused" (ruby pills marked ⤬). Compliance + scope-down workflow: admin can see partner X has `read:audit` permission but only ever calls `read:bookings` → safe to revoke the unused scope without breaking their integration.
123. ✅ OpenAPI codegen check — `npm run check:openapi-sdk` runs `buildOpenApiSpec()` through `openapi-typescript` to generate a `.d.ts` of operation + schema types, then `tsc --noEmit` against a smoke `.ts` that references the key types (Booking shape, Pagination, all 4 paths). Any spec drift that breaks a partner-side codegen (renamed field, dropped endpoint) → tsc error → exit 1 → catches breaking change pre-merge. **Programmatic codegen via JS API** rather than the CLI binary — the CLI choked on spaces in our local dev path. tsc runs with `cwd=tmpDir` + relative project arg for the same reason. Two devDeps added: `openapi-typescript`, `typescript`.
124. ✅ API key scope-down digest — weekly Monday 07:23 cron mails OWNER+SUPERADMIN listing ACTIVE API keys that have ≥1 granted scope which produced ZERO ApiRequestLog rows in the last 30d (AND ≥1 request total — abandoned keys are a separate problem). Silent on healthy weeks. New `API_KEY_SCOPE_DOWN_OWNER` enum (migration applied). CLI `npm run job:send-api-scope-down`, HTTP `POST /api/admin/jobs/send-api-scope-down`, jobRunner interval registered.
125. ✅ Rate-limit headers on human routes — the shared `rateLimit` middleware now sets `X-RateLimit-Limit/Remaining/Reset` on every response (under-limit AND 429) + `Retry-After` on 429. Same header set as the partner API (S115) so docs + monitoring share one convention. Login/register get the headers for free. Defensive `setHeader` so test fixtures with plain-object res don't crash production-shaped code.
126. ✅ Webhook delivery replay — `POST /admin/webhooks/:id/deliveries/:deliveryId/replay` re-fires a stored delivery immediately via `attemptDelivery`. Re-uses the persisted payload + signature so partner sees identical bytes (no clock drift on retry). Bumps attemptCount like cron retries do — manual replay still counts toward the 5-attempt cap. Refuses on SUSPENDED webhook + max-attempts-reached + missing-row with friendly reason codes. Per-row `↻ REPLAY` button on `/admin/webhooks/:id/deliveries`; row replaces button with "max attempts" pill when terminal.
127. ✅ Webhook event expansion (ops events) — added 4 finer-grained events to `EVENT_NAMES` so partners can subscribe to operational lifecycle without re-deriving it from the money events: `booking.status_changed` (every forward transition — fires from both `cancelBooking` and `recordPayment` on any partial → DP_PAID/PARTIAL/LUNAS leg), `booking.notes_updated` (with first-400-char `notesPreview` + `actorEmail`), `incident.created` (with crew identity + paket + location), `incident.resolved` (with `resolution` + `resolvedByEmail`). **`booking.cancelled` still fires alongside `booking.status_changed` on cancel** — partners on legacy subscriptions don't break, new partners can pick the generic one and skip the specific. The no-op short-circuit in `updateBookingNotes` means re-saving identical notes doesn't fire `booking.notes_updated` (mirrors the audit-log skip-on-no-op rule). Test-fire `samples` extended with shapes for all four so admin can probe them from `/admin/webhooks`.
128. ✅ Per-paket webhook subscription filter — `Webhook.paketId` nullable FK (migration applied) lets admin scope a subscription to ONE paket. `dispatchEvent` filters subs by `(s.paketId == null OR s.paketId === payload.paketId)`. **Global subs (paketId=null) keep receiving every event** — fully back-compat with pre-S128 rows. **Paket-scoped subs skip events that lack `paketId` in the payload** — the dispatcher can't infer scope from "untagged" events (e.g. `test.ping`), so refusing to deliver is safer than guessing. Every existing dispatch site updated to include `paketId` in the payload: `booking.created` (booking.js), `payment.received`/`booking.lunas` (payment.js), `refund.issued` (refund.js); `booking.cancelled`/`booking.status_changed` already had it. Admin form gets a "Filter per paket (opsional)" dropdown listing non-archived non-soft-deleted paket; row table shows a gold `🎯 PAKET: <slug>` pill when scoped. `createWebhook` validates the paketId exists before insert (BAD_PAKET 400) rather than letting FK opaque-fail.
129. ✅ Webhook delivery health digest — `src/services/webhookHealthDigest.js → getWebhookHealthDigest({days=7})` builds per-webhook health snapshot from `WebhookDelivery` rows in the window: `{total, succeeded, failed, pending, stuckPending, successRatePct, topError, attemptInflation}`. **Success rate only counts terminal rows** (SUCCEEDED+FAILED) — pending is "we don't know yet" and counting it as either would skew the read. **Stuck pending = PENDING > 1h old** (most retry cycles complete by then; lingering rows usually mean partner endpoint is down). **attemptInflation = total attempts / row count** — values >1 flag partners that need retries to succeed. Rows sorted worst-first. Weekly cron Monday 07:30 fans `WEBHOOK_HEALTH_OWNER` EMAIL to ACTIVE OWNER+SUPERADMIN (MANAJER_OPS/KASIR excluded — webhook config is a partner-integration concern, same RBAC as `/admin/webhooks`). **Silent when nothing is unhealthy** — quiet weeks generate no email. **Latency tracking deferred** — `WebhookDelivery` doesn't carry `durationMs` today; the simpler "X% delivered + top error" signal covers the actionable cases without a migration. New `WEBHOOK_HEALTH_OWNER` enum (migration applied). CLI `npm run job:send-webhook-health`, HTTP `POST /api/admin/jobs/send-webhook-health`, jobRunner interval registered so `/api/health` flags a missing run.
130. ✅ Inbound webhook replay gate — replay was previously offered on every row regardless of state, which double-fired side effects (HANDLED) or pretended to "revalidate" bad-signature rows (REJECTED). New `canReplayInbound({status, source})` only allows replay when status=HANDLER_ERROR AND a handler is currently registered. Refusal reasons surface explicitly: `already_succeeded` (HANDLED), `bad_signature` (REJECTED), `no_action_needed` (RECEIVED — passive source with nothing to run), `no_handler_for_source` (handler was removed since the failure — degenerate but reachable). Admin view computes the gate per-row server-side: actionable rows get the `↻ REPLAY` button, non-actionable rows render a muted reason hint (`✓ done` / `bad sig` / `passive` / `no handler`) so admin understands why no button. Flash messages localised to Bahasa Indonesia per refusal code. **No new endpoint** — same `POST /admin/inbound-webhooks/:id/replay`, just tighter pre-conditions. The S111 `no_handler_for_source` reason is now reachable only when status=HANDLER_ERROR (status gate runs first); the existing test for that path updated to assert the new `no_action_needed` behavior on RECEIVED rows.
131. ✅ Per-subscription webhook burst rate-limit — `Webhook.rateLimitPerMin` column (default 30, clamp 1..600; migration applied). `dispatchEvent` runs `checkRateLimit` per matched sub against the shared S2/S115 rate-limit store (`webhook:<id>` bucket key, 60s window). Over-quota events DON'T fire the HTTP POST — instead the `WebhookDelivery` row is created with `status=PENDING`, `nextRetryAt = end-of-window`, and `lastError='rate_limited (>N/min)'`. The retry job (S109 `processPendingDeliveries`, every 2 min) picks them up naturally once the window resets — no separate replay path needed. **Fail-open on store errors** (Redis blip shouldn't drop legitimate traffic): a thrown `.hit()` is caught, the dispatch proceeds normally. Per-row admin update at `POST /admin/webhooks/:id/rate-limit`; admin form has a "Rate limit (req/menit)" field on create and inline number-input on each row. Other webhook fields stay non-editable post-create (URL/events/paket are partner contracts) — only the throttle is tunable.
132. ✅ PaketView referrer attribution — `PaketView.referrerHost VARCHAR(120)` column (migration applied) captures `req.headers.referer` host on first visit (same first-touch semantics as `utm*`). `parseReferrerHost(referer, ownHost)` strips `www.` so `fb.com` and `www.fb.com` collapse to one bucket, drops same-origin URLs (in-site nav isn't useful attribution), and caps at 120 chars defensively. `recordPaketView` threads the value through; the route reads `req.get('x-forwarded-host') || req.get('host')` so a proxy-fronted deployment correctly identifies same-origin. New `getReferrerBreakdown({days=30})` groups visits by `referrerHost` (null → `(no referrer / direct)` bucket); renders as a sub-section inside the existing "Sumber kampanye (UTM)" panel on `/admin` overview so admin sees both lenses together. **Visits with UTM tags also have referrerHost** — the sub-section deliberately overlaps with the UTM table (the question "where did the audience come from?" wants the union, not the disjoint set). Conversion% deferred to S133.
133. ✅ Booking referrer + conversion% — closes the S132 deferral. `Booking.referrerHost VARCHAR(120)` column (migration applied) snapshots the first-touch referrer at create time via the same `visitorAttribution` plumb that already carries `utm*` (S51). `getVisitorAttribution` returns `referrerHost` from the oldest matching `PaketView`. `getReferrerBreakdown` now joins visits + bookings per host with `conversionPct`, mirroring the UTM panel exactly. **conversionPct=null when visits=0 but bookings>0** — avoids divide-by-zero and the misleading "∞%" reading; admin still sees those rows because they reveal traffic-blind conversions (e.g. pre-S132 bookings, direct API calls). Admin panel rendering adds Booking + Konv % columns; colour-coded by tier same as UTM (≥5% emerald / ≥2% gold / ≥1% amber / <1% ruby).
134. ✅ Webhook delivery latency p95 — closes the S129 deferral. `WebhookDelivery.durationMs` column (migration applied) captured by `attemptDelivery` on every fire (incl. timeouts — they take exactly `DEFAULT_TIMEOUT_MS` and that signal is what admin needs to see). Rate-limited PENDING rows (S131) have `durationMs=null` and naturally skip the percentile pool. `webhookHealthDigest` adds per-sub `{p50, p95, sample, overBudget}` where overBudget = `p95 > 2000ms`. **Sample guard ≥3** so one slow outlier on a quiet webhook doesn't generate a misleading percentile. **Slow-but-working flags row unhealthy too** — a partner doing too much synchronous work in the handler is a signal worth surfacing even at 100% success rate. Digest email body inlines `latency: p50 Xms · p95 Yms ⚠ over 2s budget (n=Z)` per unhealthy sub.
135. ✅ API key IP allowlist — `ApiKey.allowedIps Json?` column (migration applied) carries a CIDR/IP allowlist. `requireApiScope` rejects 403 `IP_NOT_ALLOWED` (NOT 401 — the token IS valid, just the source isn't authorised) when the request IP doesn't match any entry. **Empty/null = any IP** (back-compat with every pre-S135 row; no migration data write needed). Supports IPv4 CIDR (`192.168.1.0/24`, `0.0.0.0/0`) + bare IP exact match. IPv6 CIDR deferred — partners with IPv6 egress can list each address explicitly. `clientIpFrom(req)` honours `X-Forwarded-For` first entry then `req.ip`, strips `::ffff:` IPv4-mapped-IPv6 prefix so partners can list a plain IPv4 entry and have it match regardless of node's mapping. `normaliseAllowedIps` accepts comma/newline-separated string OR array → trimmed array (cap 50 entries — paste-bomb guard); malformed entries are NOT rejected at save (silently ignored at lookup) so a typo surfaces as "I keep getting 403s" during onboarding rather than blocking the save. Admin form has IP allowlist textarea on create + per-row inline edit (`POST /admin/api-keys/:id/allowed-ips`). Defense-in-depth alongside the bearer secret — a leaked token is useless without the partner's static egress IP.
136. ✅ Waitlist auto-promote on cancel — extends S42's nudge into a hands-off action when the oldest WAITING entry is from a "verified" jemaah (active JEMAAH user account AND ≥1 prior LUNAS booking). `findVerifiedWaitlistForPaket({paketId})` filters via cheap DB endsWith(last-4-digits) then JS-normalises the full phone for exact match (handles dashes/spaces/+62 prefix variants the same way S59 lead-reactivation does). Prior LUNAS counted via BOTH the direct `jemaahUserId` link AND the profile's user (covers older pre-S5p.2 anonymous-then-claimed bookings). When verified candidate found, `cancelBooking` calls `promoteWaitlist` with the freed booking's `kelas + paxCount` (we just made room for that shape; if jemaah wanted something different they refuse via existing manual cancel + re-promote flow) AND skips the S42 nudge (admin doesn't need "freed seats, here are candidates" email when we already handled it). Decorator audit row stamps `autoPromoted: true` + `trigger: 'cancel.auto_promote'` + `verifiedSignal: {userEmail, priorLunasCount}` so admin can distinguish from a hand-clicked promote. **Fire-and-forget** — auto-promote failure logs but doesn't abort cancel + falls through to the nudge fallback so the queue isn't silently dropped.
137. ✅ Crew SOS rate-limit — `createIncident` caps non-SOS incidents at 5 per 10 min per crew (HttpError 429 `SOS_THROTTLED`). Uses the shared rate-limit store with `incident:<crewUserId>` bucket key. **SOS type bypasses the limit entirely** — life-safety alerts always land regardless of how many MEDICAL/LOGISTICAL/etc. the same crew has filed recently. **Fail-open on store errors** — a Redis blip must NOT silently drop a lifeline; we'd rather a duplicate page than a missed real one. Error message in Bahasa surfaces seconds-until-retry + reassurance that "SOS tetap bisa dikirim kapan saja". Per-crew bucket isolation means one panicking crew can't lock out another's queue.
138. ✅ Audit CSV export — `GET /admin/audit/export.csv?from=&to=&entity=&action=&actorEmail=` (OWNER+SUPERADMIN, same gate as audit list). Streaming export — service pulls rows in 1000-row cursor batches and writes directly to the HTTP response, so a 50k-row download never holds the table in memory. **7-day default, 90-day cap, 50k row hard ceiling** (capped flag exposed in self-audit so investigator knows result was truncated). Window clamp keeps the recent end — "last 90 of requested" is more useful than "first 90" for compliance asks. UTF-8 BOM + CRLF (Excel-compatible, mirrors S106 convention). Filter inputs match the audit-list page so the "Export ⇩ CSV" button preserves whatever filter the admin already narrowed to. **Export is itself audited** (`action=EXPORT, entity=AuditLog`) with `{rowsWritten, capped, rangeFrom, rangeTo, filters}` snapshot — investigator never disputes "did anyone pull the log?". Day-count uses `floor((to - from)/msPerDay) + 1` so a same-day request resolves to 1 inclusive day, not 2.
139. ✅ Audit retention archive — `archiveAuditLog({retentionDays=730})` streams AuditLog rows older than 2 years to a gzipped CSV under `private/audit-archive/audit_<timestamp>_pre_<cutoff>.csv.gz`, then deletes them from DB. **Reverses the historic "AuditLog kept forever" rule** — operators grep the `.gz` files via `zcat` for long-tail compliance asks; bounded growth on the live table. CSV format is identical to S138 export so an investigator opening the archive gets a consistent shape. **One file per run** named with timestamp + cutoff date — no overwrites, no append; partial-failure recovery is just "next run picks up what's still in DB" (acceptable duplicate across files if DB delete failed mid-way). Wired into the weekly prune job (env `RETENTION_AUDIT_ARCHIVE_DAYS`; set to `0` to disable archival for deployments that prefer KEPT-FOREVER). `private/` is in SENSITIVE_PREFIXES so archive files aren't publicly served. Best-effort in the prune wrapper — a failing archive run doesn't abort the rest of the sweep.
140. ✅ Per-paket attendance trend sparkline — `buildAttendanceTrend(days, totalActive)` rolls up the existing day-level `presentCount + markedCount` into `{ticks, avgPct, markedDayCount}`. **Days with zero marks naturally render as 0%** with `hasData=false` so the view dims them (an unmarked day is "we have no data", not "0% present"); avgPct deliberately excludes them so the rolled-up number stays honest. Both crew `/crew/paket/:slug/attendance` (`listAttendanceDays`) and admin `/admin/paket/:slug/attendance` (`getPaketAttendanceReport`) return the same `trend` shape. Inline SVG sparkline partial `views/partials/attendance-sparkline.ejs` renders 320×40px bars colour-coded by tier (≥80% emerald / ≥60% gold / <60% ruby; no-data ink at 0.25 opacity). Per-bar `<title>` tooltips carry the raw counts ("H3 · 12/18 hadir (67%)") so admin can hover for detail without scrolling the table. Average pct rendered next to the chart with the same colour tier — crew + admin spot collapsing attendance at a glance.
141. ✅ Manifest close jemaah nudge — `Booking.manifestCloseNotifiedAt DateTime?` column + `MANIFEST_CLOSE_NUDGE` enum (migration applied). `getManifestCloseNudgeCandidates({windowHours=72})` finds active bookings on paket whose `manifestClosesAt` is within 72h (or overdue) AND whose jemaah has ≥1 required item missing (passport / visa umroh / vaksin meningitis VERIFIED / kontak darurat present). Health cert / manasik cert / room assignment are nice-to-haves and don't trigger a nudge alone — keeps the signal actionable, not nagging. **Idempotent** via `manifestCloseNotifiedAt` stamp: query already excludes bookings where the field is set, and `notifyManifestCloseNudge` stamps `now` after enqueue so the daily cron doesn't pile up duplicates. Fires both EMAIL + WA (per-channel opt-out via `JemaahProfile.notifEmail/notifWa` respected via existing `enqueueNotification` mechanism). `recipientUserId` set when booking is linked so `/saya/notifications` inbox + unread badge pick it up. Stamp fires even when the jemaah has NO email/phone — prevents the cron re-evaluating contact-less rows every run. Daily cron + CLI `npm run job:send-manifest-close` + HTTP `POST /api/admin/jobs/send-manifest-close` + jobRunner interval registered. Silent on quiet days — empty candidates → no email.
142. ✅ Admin manifest close nudge mirror — `getManifestCloseNudgeAdminSummary({windowHours=24})` reads recent S141 `MANIFEST_CLOSE_NUDGE` notif rows and aggregates by paket so admin can do manual WA follow-up without grep'ing the queue. Per-paket row carries the bookings that got nudged (deduped by bookingId so EMAIL+WA collapse into one entry with `channels: ['EMAIL','WA']`) + `missingSummary` rollup (`{label, count}` desc — "8 jemaah missing Visa umroh, 3 missing Kontak darurat"). Sorted by `manifestClosesAt` asc so the soonest-closing paket lands at the top. Panel on `/admin` overview between manifest-closing countdown and paket forecast. Each booking row gets a `wa.me/<phone>` link generated from the existing jemaah phone (digits-only + 0→62 normalisation, same convention as the Fonnte adapter S5kk). Collapsed by default via `<details>` so a paket with 20 nudged jemaah doesn't dominate the overview.
143. ✅ Webhook signature replay protection (timestamped HMAC) — `hmacVerifier` extended with `requireTimestamp:true` opt-in. Reads `X-Webhook-Timestamp` header (epoch seconds), rejects requests outside ±5min skew window (`REPLAY_SKEW_SEC=300`, Stripe-style). **Signed body is `<ts>.<rawBody>`** (Stripe-style), so an attacker swapping the header timestamp to dodge the skew check fails the HMAC verify — both checks have to pass together. New `generic-ts` source registered as the model integration for partners that support it; legacy `fonnte`/`zapier`/`generic` keep no-ts behavior (per-source opt-in so existing partners aren't suddenly broken). **Outbound side mirrored** — `signWithTimestamp(secret, body, tsSec)` + every `attemptDelivery` POST now sends `X-Webhook-Timestamp` + `X-Religio-Signature-V2` (timestamped) alongside the legacy `X-Religio-Signature` (body-only), so partners can opt into replay protection on their receive side without breaking existing partners. **Per-attempt fresh timestamp** — retries get a new ts so a partner's narrow skew window doesn't cause our cron retries to be rejected. Secret rotation grace (S118) covers both signature variants — `X-Religio-Signature-V2-Prev` ships during the grace window.
144. ✅ Booking no-show detection — `Booking.noShowAt DateTime?` + index (migration applied). Daily `detectNoShows({now, dryRun})` finds active bookings (PENDING/BOOKED/DP_PAID/PARTIAL/LUNAS) on paket whose `departureDate` has passed AND zero `AttendanceMark` exists for `dayNumber=1`, then stamps `noShowAt=now`. **Marked-absent ≠ no-show** — any mark (present OR absent) means crew was tracking the jemaah; the no-show signal is specifically "we never knew if you showed". Paket without an itinerary day 1 skip (we'd be guessing). CANCELLED/REFUNDED exclude. Idempotent via the stamp itself — query filters `noShowAt: null` so re-runs are no-ops; admin clears the field manually if a late correction reverses the verdict. Per-row audit row (`STATUS_CHANGE entity=Booking after={noShowAt, trigger:'noshow.daily_detection'}`) so the timeline shows when the system flipped a particular booking. `/admin/no-shows` queue page (`listNoShows`, paginated 50/page, OWNER+SUPERADMIN+MANAJER_OPS gate via `/admin` mount) with 3-cell KPI (total, LUNAS no-show count, sum paid) + per-row `wa.me/<phone>` link for follow-up. `⚠ NO-SHOW` badge in the header of `/admin/bookings/:id`. CLI `npm run job:detect-no-shows` + HTTP `POST /api/admin/jobs/detect-no-shows` + jobRunner daily interval registered.
145. ✅ No-show refund guard rail — `issueRefund` accepts `acknowledgeNoShow:boolean`. When `booking.noShowAt` is set AND `amount >= currentPaid` (full refund) AND `!acknowledgeNoShow` → throws 409 `NOSHOW_REFUND_NEEDS_ACK` with a Bahasa explanation ("Mungkin jemaah terbang dengan operator lain"). **Partial refunds bypass the guard** — goodwill 30%/50% rebates don't carry the "they may have flown with someone else" risk, only full refund does. Audit row carries `wasNoShow:true` + `noShowAcknowledged:bool` for every refund on a stamped booking (durable trail for compliance — "did we knowingly refund all the no-shows last quarter?"). `cancelBooking` mirrors the audit annotation (`wasNoShow:true` + `noShowFlaggedAt:<iso>` when present), so a no-show being formally closed out is also traceable. Refund modal in `/admin/bookings/:id` shows a ruby warning banner + an explicit checkbox when `b.noShowAt` is set; checkbox value flows through the existing FormData → JSON path so no JS change beyond the markup. Zod preprocessor on the refund route accepts `true`/`"true"`/`"1"`/`"on"` so form-encoded + JSON callers both work.
146. ✅ No-show rate analytics — `getNoShowAnalytics({days=90})` returns per-paket + per-agent rollup mirroring the S35 refund analytics shape. **Numerator** = bookings with `noShowAt` stamped within window. **Denominator** = bookings on paket whose `departureDate` falls within window AND status NOT IN (CANCELLED, REFUNDED) — only "resolved-active" bookings count (we don't dilute the rate with future bookings whose verdict is still pending). `ratePct` is null when denominator=0 so a no-show on a future-departing paket (theoretical, shouldn't happen post-S144 but defensive) renders as "—" not "∞%". Walk-in bookings bucket under `__kp__` sentinel with `displayName='Kantor Pusat'`, mirroring S35 convention. Sort: `noShowCount desc` so the heaviest leakers land at the top. Panel on `/admin` overview between refund analytics and komisi monthly with 2-cell KPI strip + two columns (per-paket + per-agent, top 10 each). Colour-coded by tier (≥10% ruby / ≥5% amber / ≥2% gold / <2% emerald). Only renders when `totalNoShow > 0` so quiet weeks don't waste real estate.
147. ✅ Jemaah cancel deadline lock — `requestCancelByJemaah` accepts `now` param + checks `booking.paket.manifestClosesAt < now`. Past the deadline, throws 409 `CANCEL_DEADLINE_PASSED` with a Bahasa explanation directing the jemaah to admin/agen. **Paket without `manifestClosesAt` never locks** — admin chose "never close" or hasn't set the date yet; jemaah can still cancel anytime. **Admin flow (`cancelBooking`) bypasses the guard entirely** — admin can still cancel post-deadline (e.g. as a manual override after commercial dispute). `/saya/bookings/:id` view surfaces three distinct states: (a) `cancelRequested` (existing pending request banner — unchanged), (b) `deadlinePassed && active` (new ruby-bordered "Deadline pembatalan sudah lewat" panel directing to admin), (c) the existing request-cancel CTA — now decorated with a `⏱ Batas pengajuan: <date> (Nh lagi)` countdown amber-flagged when ≤72h. `getMyBooking` paket select extended with `manifestClosesAt`. Prevents the "I want to cancel" coming in 2 hours before departure when the seat can't be resold.
148. ✅ Unread notif badge on `/agen` + `/crew` — extends the S5rr jemaah inbox pattern to the other portals. The existing `countUnreadForUser` / `markAllReadForUser` / `listMyNotifications` helpers from `jemaahPortal.js` are **role-agnostic** (queries filter by `recipientUserId` only), so reusing them across portals required zero service-layer changes — just import + add the routes. New routes: `GET /agen/notifications` + `GET /crew/notifications` (same pattern as `/saya/notifications`: render list then `markAllReadForUser` so the badge clears on next paint while the current paint still shows the gold left-border + dot on unread rows). Topbar badges on `/agen` (next to saldo wallet) + `/crew` (next to logout button) render a gold pill with the unread count when `unreadCount > 0`, hidden otherwise. Shared `views/partials/notification-list.ejs` extracted from the original `jemaah-notifications.ejs` so the three inbox views (`jemaah-notifications`, `agen-notifications`, `crew-notifications`) stay in sync without copy-paste drift on the row markup. The function names stay slightly jemaah-flavored (`countUnreadForUser`) because renaming would touch the existing `/saya` paths without functional benefit.
149. ✅ Booking voucher PDF cache — content-addressed cache under `private/voucher-cache/<bookingId>__<lang>__<hash>.pdf`. **Hash** is a SHA-256 truncated to 16 hex chars over every displayed field of the voucher (bookingNo / status / total / paid / kelas / paxCount / paket times+title+days+route / jemaah identity / room / agent / payments). Any change to a displayed field → new hash → cache miss → fresh render → old hash file cleaned up for the same `(bookingId, lang)`. **Per-language variants stored separately** (id / en / ar / `_<lang>` suffix). Refactor extracts `renderVoucherIntoDoc(doc, voucher, lang)` from `streamVoucherPdf` so the layout serves both the stream-to-res path and a new `renderVoucherPdfBuffer` returning a Promise<Buffer>. Routes (`/admin/bookings/:id/voucher.pdf`, `/saya/bookings/:id/voucher.pdf`) + the bundle paths (`streamBookingBundle` single + `streamBookingBundles` bulk) all route through `getOrRenderVoucherPdf`. **Best-effort** — a cache write failure logs but still returns the freshly-rendered buffer to the caller. Bundle paths keep an inline-stream fallback when the cache helper throws so a broken cache disk never breaks zip exports. `X-Voucher-Cache: HIT|MISS` response header on the standalone routes lets ops observe hit rate without instrumentation. Big bulk-dossier flows (100+ vouchers) reuse PDFs already rendered by individual admin downloads.
150. ✅ Per-agent monthly komisi statement PDF — new `KomisiStatement(agentId, periodYM, totalEarnedIdr, totalPaidIdr, lineCount, pdfPath, generatedAt)` model (migration applied) with composite-unique `(agentId, periodYM)`. `generateAgentStatement({agentId, periodYM})` queries EARNED + PAID komisi where `earnedAt` OR `paidAt` falls in the period, renders a programmatic A4 PDF (pdfkit, same pattern as S101 voucher), persists under `private/komisi-statements/<agentId>__<YYYY-MM>.pdf`, and records the row. **Idempotent**: re-running for the same period returns the existing row + PDF without re-rendering (the statement is supposed to be an immutable monthly artifact — changes to underlying data don't retroactively update old statements). CANCELLED komisi excluded (undone work, not income). `generateAllAgentStatements` iterates every ACTIVE agent + catches per-agent failures so a bad row doesn't abort the batch. CLI `npm run job:generate-komisi-statements` + HTTP `POST /api/admin/jobs/generate-komisi-statements` + jobRunner monthly interval. Cron Mon 1st generates the previous-month statement per active agent. Agent download route `/agen/statements/:id.pdf` enforces ownership via `agentProfile.userId` chain (cross-agent access 404s). Wallet tab on `/agen` shows up to 24 most-recent statements as compact cards with EARNED + PAID totals + line count.
151. ✅ Admin statement regenerate override — escape hatch for the S150 immutability rule when a late komisi adjustment lands after the original cron pass. `regenerateAgentStatement({agentId, periodYM})` captures the prior `{totalEarnedIdr, totalPaidIdr, lineCount, pdfPath}` for audit, deletes the existing row + PDF (best-effort `unlink`), then re-invokes `generateAgentStatement`. Audit row carries `UPDATE entity=KomisiStatement before=<prior> after={regenerated:true, ...}` so a compliance scan can answer "did the statement amount change retroactively?". POST `/admin/agents/:slug/statements/regenerate` (OWNER+SUPERADMIN), form lives on `/admin/users/:id/edit` as a panel below the komisi rate override field in the AGEN profile section. Empty `periodYM` defaults to the agent's latest existing statement (409 `NO_EXISTING_STATEMENT` when none — direct admin to run generate/backfill first). Flash banner on `/admin/users` surfaces the diff inline ("Total lama: EARNED Rp X · PAID Rp Y") so the admin sees what changed without leaving the page. PDF filename pattern `<agentId>__<period>.pdf` stays stable across regenerate so the agent's download URL keeps working.
152. ✅ Statement email delivery — new `KOMISI_STATEMENT_READY` enum (migration applied) + `notifyKomisiStatementReady` helper. Fires one EMAIL per `generateAgentStatement` create with a download link to `/agen/statements/:id.pdf` + the period's earned/paid totals. **Silent on three cases**: zero-line statement (no point notifying about a quiet month), agent has no email, agent user suspended/deleted. **Idempotent re-runs don't duplicate** — the early-return on existing statement short-circuits before the notif fire. `recipientUserId` set so the S148 `/agen/notifications` inbox + unread badge surface the message. **Fire-and-forget** — notif failure logs but never aborts statement creation (the PDF is the load-bearing artifact). Regenerate flow (S151) also benefits — re-creating after delete fires a fresh notif (admin presumably wants the agent to know the numbers changed).
153. ✅ Komisi statement backfill CLI — `backfillKomisiStatements({months=6})` walks backwards from `previousMonthYM()` for N periods and calls `generateAllAgentStatements` per period. **Idempotent** via the existing skip-if-exists in `generateAgentStatement` — re-running a backfill is safe + usually a no-op after the first pass. **Cap 24 months** (beyond two years is a one-off backfill that should be done deliberately; floor 1 month). CLI `npm run job:backfill-komisi-statements -- --months=6` (the `--` is required so npm forwards the arg to node) parses `--months=N` from argv. Per-month breakdown + grand totals logged to stdout. **NOT registered as a recurring cron** — this is a deliberate first-install / new-deployment seeding tool, not a scheduled job. Existing S152 email fires for newly-created statements during backfill but skips for the ones already there.
154. ✅ Statement zero-line opt-out — env-gated `KOMISI_STATEMENT_SKIP_ZERO_LINES=true` makes `generateAllAgentStatements` skip agents with zero lines for the period (no DB row, no PDF). Keeps the agent wallet tab tidy for agents who didn't sell anything that month — the previous behavior generated "Rp 0 / 0 baris" PDFs they never wanted. **Check is at the cron/batch layer**, not inside `generateAgentStatement` — the direct service call (used by admin regenerate S151) **always produces** regardless of flag, since explicit admin intent overrides the quiet-month policy. Pre-check via `getStatementLines` avoids rendering the PDF + creating the row when we'd skip anyway. **Existing rows are preserved** even when flag is on (idempotency wins over the policy — once a row exists the SKIP gate stops mattering). Reads env at call time (not module load) so runtime toggles + tests work without restart. New `zeroSkipped` counter in the result shape so admin can see how many quiet months were elided per run.
155. ✅ Statement YTD running totals — `computeYtdTotals({agentId, periodYM})` returns three slices over the calendar year: **before** (Jan 1 → period start, exclusive), **during** (the period itself), **after** (Jan 1 → period end, inclusive). Each carries `{earnedIdr, paidIdr, count}`. **Suppressed on January with no prior signal** — empty before-window in January means no rollover from last year, so the YTD block adds no signal; February onwards always renders even with zero before-totals so the agent sees the running tape start fresh. PDF gets a new "YTD <year> (komulatif)" block below the period totals: 3-row table with Window / EARNED / PAID columns, "Setelah periode ini" row bolded + green-tinted so the cumulative-so-far number stands out. `generateAgentStatement` wraps `computeYtdTotals` in try/catch so a compute failure just drops the block without aborting PDF render. Lets agents see "I earned 300k this year so far" at a glance without flipping through 12 monthly PDFs.
156. ✅ Admin note on statement — `KomisiStatement.adminNote Text?` (migration applied). `generateAgentStatement` + `regenerateAgentStatement` accept an `adminNote` arg, trim + cap at 2000 chars (empty → null), persist on the row, and render as a gold-bordered "CATATAN DARI RELIGIO PRO" block at the end of the PDF body. **Three-state semantics** on `regenerateAgentStatement`: `undefined` (omitted arg) **preserves** the prior note; `''` or `null` **clears** it; non-empty string **replaces**. Achieved by dropping the destructuring default — `adminNote` arg with no value passed gets `undefined`. Route `POST /admin/agents/:slug/statements/regenerate` reads `req.body?.adminNote ?? null` so an empty textarea clears (form behaviour) while an omitted field would preserve (API behaviour). Audit row carries both prior + new `adminNote` so retroactive note edits show up in compliance scans. Wallet-tab card on `/agen` gets a gold pill (`✎ ada catatan`) when set, with the full note in `title` for hover-tooltip; the agent then opens the PDF for the full bordered block.
157. ✅ Agent statement prefs self-service — `AgentProfile.notifKomisiStatement Boolean @default(true)` (migration applied). `notifyKomisiStatementReady` checks the flag (default `true` for back-compat — legacy callers omitting the field still fire) and returns `{skipped:true, reason:'opted_out'}` when off. `generateAgentStatement` select includes the field + passes it to the notif helper so the opt-out is honoured without an extra round-trip. **Statement row + PDF still created when opted out** — opt-out only suppresses the email; the artifact stays available via `/agen` wallet tab download. New `/agen/profile` page with identity strip + preferences panel (currently just the statement toggle, but the page is the natural place to add more agent-level prefs). `POST /agen/profile/prefs` normalises unchecked HTML checkbox to explicit `false` (mirrors S5jj jemaah-side pattern — checkbox absent from `req.body` would otherwise leave the flag unchanged). Topbar link added to `agen-crm.ejs` so agents can find the page.
158. ✅ Statement annual recap email — `AGENT_ANNUAL_RECAP` enum (migration applied) + `buildAgentAnnualRecap({agentId, year})` aggregates KomisiStatement rows where `periodYM startsWith <year>-` into totals + sorted-asc per-month breakdown. Returns null when agent has zero statements in scope so caller silently skips. `notifyAgentAnnualRecap` fires one EMAIL with month-by-month table inline. **Three skip reasons**: `no_email`, `no_statements`, `opted_out` (same flag as S152 — conceptually the same kind of message, no separate toggle). `sendAgentAnnualRecaps({year})` batch entry loops `listAgentsWithStatementsForYear` (excludes suspended/deleted agents — they don't get retro emails). **No idempotency** — expected cadence is once-per-year so accidental double-run sends duplicates; operators pre-purge by year before re-runs. Jan 5 cron + CLI `npm run job:send-agent-annual-recap` + HTTP `POST /api/admin/jobs/send-agent-annual-recap` (accepts `year` body param for re-runs) + jobRunner yearly interval registered.
159. ✅ Per-paket komisi statement (dispute resolution) — `getStatementLines` accepts optional `paketId` arg that adds `where.booking.paketId = paketId` to the komisi query. New `renderPaketScopedStatementBuffer({agentId, periodYM, paketId})` builds a transient PDF for a single (agent × paket × period) tuple. **Does NOT persist a KomisiStatement row** — purely read-only snapshot for admin to share during back-and-forth like "I think you under-counted my Ramadhan-2026 komisi, show me only that paket's lines". Header subtitle decorated with `· scope: <paket title>` so it's obvious this isn't the canonical monthly artifact; PDF carries a transient `adminNote` explaining the scope. Admin route `GET /admin/agents/:slug/statement-paket.pdf?periodYM=YYYY-MM&paketSlug=X` (OWNER+SUPERADMIN) streams the buffer inline. Query-param URL is shareable. Filename pattern `komisi_<agent-slug>_<period>_<paket-slug>.pdf` sanitised against path traversal.
160. ✅ PDF watermark for preview statements — `renderStatementPdfBuffer` accepts a `watermark` arg that renders a 72pt diagonal text overlay at -30° across the page at 10% ruby opacity. Applied AFTER all body content so it sits on top of every block. **S159 `renderPaketScopedStatementBuffer` passes `'PREVIEW · TIDAK KANONIK'`** so dispute-resolution PDFs look visually distinct from the canonical S150 monthly artifact even at a glance. Canonical S150 path **never** sets the watermark (no visual ambiguity for the official statement). Tests use buffer-size delta rather than literal-byte-search because pdfkit zlib-compresses content streams — bytes of "PREVIEW" don't survive the compression, but the watermark draw call adds ≥200 bytes of compressed content reliably. Pattern is ready to reuse for future preview surfaces (e.g. backfill dry-run, draft statements before admin approval).
161. ✅ Cross-paket range statement — `renderRangeStatementBuffer({agentId, from, to})` aggregates komisi across all paket within a date window. Same transient + watermarked posture as S159 (no `KomisiStatement` row, S160 PREVIEW overlay). **Window capped at 90 days** (`RANGE_TOO_WIDE` 409) — beyond a quarter, agent should be using the canonical monthly statements. **Validation chain**: `BAD_AGENT` → `BAD_RANGE` (from/to missing) → `BAD_DATE` (unparseable) → `BAD_RANGE_ORDER` (to<from) → `RANGE_TOO_WIDE` (>90d) → `AGENT_NOT_FOUND`, so a malformed request returns the most specific error possible. Synthetic `periodYM = '<from> → <to>'` reuses the existing PDF render's "Periode" line without misleadingly anchoring to a single month. Admin route `GET /admin/agents/:slug/statement-range.pdf?from=YYYY-MM-DD&to=YYYY-MM-DD` (OWNER+SUPERADMIN) streams the buffer inline; filename `komisi_<agent-slug>_<from>_to_<to>.pdf` sanitised against path traversal. Answers "show me all komisi for agent X for Q1 2026" without forcing admin to open three monthly PDFs.
162. ✅ Statement download counter — `KomisiStatement.{agentDownloadCount, agentLastDownloadAt, adminDownloadCount, adminLastDownloadAt}` columns (migration applied; defaults 0/null). `recordStatementDownload({statementId, surface})` increments the matching counter pair atomically via Prisma `{increment:1}` + stamps `now()`. **Per-surface tracking** so admin can distinguish "agent never opened the PDF" from "admin pulled it for an audit" — different signals, different follow-up. `/agen/statements/:id.pdf` bumps `agent*` via `res.on('finish')` (fire-and-forget AFTER the response so latency isn't affected). New admin route `GET /admin/agents/:slug/statements/:id.pdf` streams the same canonical PDF + bumps `admin*` — lets admin pull a copy without impersonating the agent. Counter writes are best-effort (try/catch + log) so a DB hiccup never breaks the actual download. Surfaced on `/admin/users/:id/edit` as a 6-row table showing recent periods + per-surface counts; agent zero counts highlighted amber so "agent hasn't opened the last 3 months" jumps out for follow-up. Invalid surface arg → no-op; missing statementId → no-op; non-existent row → silently swallowed.
163. ✅ Stale statement WA nudge — daily cron + CLI `npm run job:send-statement-unread-nudge` + HTTP `POST /api/admin/jobs/send-statement-unread-nudge` finds ACTIVE agents who have at least one KomisiStatement in the last 3 months with `agentDownloadCount=0` AND haven't been nudged within `cooldownDays=14`. Sends a soft WA "Anda punya N statement belum dibuka (terlama: YYYY-MM)" via existing `enqueueNotification`. New `STATEMENT_UNREAD_NUDGE` enum (migration applied). **Per-agent cooldown via the Notification table itself** — no extra state; query checks for a prior STATEMENT_UNREAD_NUDGE within `cooldownCutoff`. Skips zero-line statements (no point nudging about a quiet month) + respects the S157 `notifKomisiStatement` opt-out (statement WA and statement EMAIL are conceptually the same kind of message — no separate WA toggle). 14-day cooldown means monthly statements get at most two nudges before silence. Sort: oldest-unread-first so the most-stale agents bubble to the top of a future per-run quota. `notifyStatementUnreadNudge` is the per-row helper; `sendStatementUnreadNudges` is the batch entry that loops candidates with per-agent failure isolation. **Silent on empty candidate list** — quiet days produce zero enqueues.
164. ✅ Agent statement history page — `/agen/statements` paginated list (20/page) of the agent's full statement history beyond the 24-month wallet-tab cap. `listAgentStatementsPaginated({agentId, page, pageSize})` returns rows + `lifetime: {earnedIdr, paidIdr, lineCount, statementCount}` (a single `aggregate(_sum)` call for the totals — no per-row math in JS). Page size clamped to [1..100], page clamped to ≥1 (negative values floor to 1). 4-cell lifetime KPI strip + table of (periode, EARNED, PAID, lines, opened/unread badge, generated date, PDF+CSV download). The wallet-tab card-grid still shows the last 24 months at a glance; this page is the "all the way back" surface for long-tail review. Linked from a "RIWAYAT LENGKAP →" link in the wallet-tab panel header.
165. ✅ Statement CSV export — `buildStatementCsv({agentId, periodYM})` reuses the same `getStatementLines` source as the PDF so the spreadsheet and the printable both reflect identical numbers. UTF-8 BOM + RFC 4180 quoting + CRLF row separators (Excel mojibake-proof, matches the S138 audit export convention). Columns: bookingNo / jemaahName / paketTitle / paketSlug / earnedAt / paidAt / amountIdr / status / currency, plus a TOTAL footer row carrying separate EARNED + PAID sums so the CSV is self-contained as an audit artifact. Routes: `/agen/statements/:id.csv` (agent) + `/admin/agents/:slug/statements/:id.csv` (admin) — both honour the existing ownership chains and bump the matching surface download counter on `res.on('finish')`. CSV downloads count toward the same `agentDownloadCount`/`adminDownloadCount` as the PDF — opening the CSV is just as much "looked at the content" as the PDF would be.
166. ✅ Agent payout banking details — `AgentProfile.{preferredPayoutMethod, bankName, bankAccountNumber, bankAccountName}` columns (migration applied, all nullable so pre-S166 agents stay valid). Self-service via `POST /agen/profile/payout-details` (3-state preprocessor: `undefined` preserves, `''` clears, value stores). `updateAgentPayoutDetails` enforces a Zod schema + skip-audit-on-no-op so re-saving identical values doesn't pollute the log. **Account number is masked in audit snapshots** (first 2 + last 2 chars only, rest `*`) — the live DB row keeps the full value, but the audit table is a secondary surface admins grep through, so partial masking limits damage if logs leak. `/admin/payouts/new?agentId=<id>` reads the stored details and pre-fills `method` + injects a "Bank: BCA · No 1234567 · a/n Ahmad" snippet into the `reference` field, plus renders an amber banner above the form so KASIR sees who they're paying. KASIR still has full edit control — pre-fill is convenience, not a lock. Closes the loop on the S37 payout reminder: admin sees WHO needs payment AND how to pay them in one workflow.
167. ✅ Booking duplicate phone warning — `findRecentBookingsByPhone({phone, windowDays=90})` returns active bookings (NOT IN CANCELLED/REFUNDED) matching the phone within window. **Normalisation** mirrors S59 (lead reactivation): digits-only, leading `0` → `62`, so `+62 822-3399`, `082233 99`, and `622339911` collapse to one key. **Cheap DB prefilter via `endsWith` on last 8 digits**, then JS re-checks the full normalised form to avoid tail-match false positives (`082155667788` doesn't trigger for `081155667788`). On `POST /admin/bookings`, when admin hasn't ticked `confirmDuplicate`, re-renders the form with an amber warning panel listing each match (bookingNo + jemaah + paket + status + agent + date) with deep links. Admin ticks "Lanjut tetap" → second POST bypasses the check entirely. `findRecentLeadsByPhone({phone, agentId})` mirrors the same logic for the agen CRM lead-create flow (excludes terminal CONVERTED/LOST). Empty/short phone → empty result (no point asking the DB for 3-digit prefixes).
168. ✅ Lifetime komisi CSV — `buildAgentLifetimeKomisiCsv({agentId})` exports every Komisi row across the agent's entire history (PENDING/EARNED/PAID/CANCELLED, all paket, all periods). Distinct from S165 (per-period statement CSV) — that's scoped to one periodYM and matches the PDF; this is the full lifetime ledger for personal accounting / tax prep. Sorted oldest-first so spreadsheet running-total formulas read naturally. Columns: createdAt / earnedAt / paidAt / bookingNo / jemaahName / paketTitle / paketSlug / amountIdr / status / currency / payoutNo. TOTAL footer carries EARNED + PAID sums + rowCount. UTF-8 BOM + RFC 4180 quoting + CRLF, matching the S138/S165 convention. Route `/agen/komisi-lifetime.csv` (agent ownership via `userId → AgentProfile.id` chain); filename `komisi_lifetime_<slug>.csv` sanitised against path traversal. Linked from the Wallet tab below the KPI strip, only when `komisi.total > 0` (no point on empty wallets).
169. ✅ Lead duplicate phone warning — mirrors S167's booking dup check for the agen CRM lead-create flow. `POST /api/leads` runs `findRecentLeadsByPhone` + `findRecentBookingsByPhone` upfront when phone is set; returns 409 `DUPLICATE_LEAD` with `{duplicates: {leads[], bookings[]}}` envelope when either fires. Client (agen-crm modal) shows inline warning panel + sets `leadConfirmDuplicate=true` so the next submit carries `confirmDuplicate: true` in the JSON body to bypass the check. Confirmed-state resets on modal close so re-opening the modal triggers a fresh check. **JSON 409 + client-side warning** rather than re-render (S167 pattern) because the lead flow is fetch-driven, not form-POST. Both lead AND booking dupes surface — an agent who sees "this jemaah already has a booking" might decide the lead is unnecessary entirely. Excludes terminal CONVERTED/LOST lead statuses + CANCELLED/REFUNDED bookings — past closed signals shouldn't block re-engagement.
170. ✅ Agent payout history CSV — `buildAgentPayoutHistoryCsv({agentId})` exports every `KomisiPayout` disbursed to the agent (one row each, ordered oldest-first). Columns: paidAt / payoutNo / amountIdr / currency / method / reference / komisiLines (bundled count from `_count.komisi`) / paidByEmail / notes. TOTAL footer with sum + count. UTF-8 BOM + RFC 4180 / CRLF matching the S168 lifetime ledger convention. Distinct from S168 — that's per-Komisi-row, this is per-payout-receipt for bank-statement reconciliation. Route `/agen/payout-history.csv` (agent ownership chain); filename `payout_history_<slug>.csv` sanitised against path traversal. "⇩ CSV LENGKAP" link in the Wallet tab "Riwayat payout" panel header, only when `payouts.length > 0`.
171. ✅ Agent KPI scorecard — `getAgentKpiScorecard({agentId})` aggregates the agent's lifetime state from Booking + Komisi + Lead in three parallel queries (no N+1). Returns `{counts: {total, active, lunas, cancelled}, conversionPct, revenue, lastBookingAt, komisi: {pending, earned, paid, cancelled, lifetime}, leads: {cold, warm, converted, lost, total}, topPaket[3]}`. **conversionPct = lunas / active × 100** — denominator EXCLUDES CANCELLED/REFUNDED (undone work, not a real outcome). **conversionPct=null when active=0** to avoid divide-by-zero / misleading "no bookings → 0% conversion" reading. **komisi.lifetime sums PENDING + EARNED + PAID, NOT CANCELLED** — cancelled is undone, not part of lifetime earnings. Top 3 paket by LUNAS revenue (the agent's specialty / over-reliance signal). Surfaced on `/admin/users/:id/edit` as a 4-cell KPI panel above the statement counters table; hidden when agent has zero bookings AND zero leads (new agent, nothing to summarize). Best-effort compute — exception logs but doesn't 500 the edit page.
172. ✅ Payment reminder cron — daily nudge to jemaah with unpaid balance on bookings whose paket departs within `windowDays=14`. `getPaymentReminderCandidates` filters active bookings (PENDING/BOOKED/DP_PAID/PARTIAL — LUNAS owes nothing, CANCELLED/REFUNDED are dead), then JS-filters those with `paidAmount < totalAmount` (Prisma can't compare two columns in `where` without raw SQL). **Per-booking cooldown** via the Notification table — checks for prior `PAYMENT_REMINDER` notif within `cooldownDays=5`, so jemaah don't get the same nudge every single day until takeoff. Sort by soonest-departing first so urgent rows surface to any per-run quota. `notifyPaymentReminder` fires BOTH EMAIL + WA when both are present (each respects the jemaah's per-channel opt-out via `enqueueNotification`). New `PAYMENT_REMINDER` enum (migration applied). CLI `npm run job:send-payment-reminder` + HTTP `POST /api/admin/jobs/send-payment-reminder` + jobRunner daily interval. Silent on quiet days.
173. ✅ Document expiring soon email — daily cron emails jemaah whose tracked `JemaahDocument` rows have `expiresAt ≤ now + 30d`. Pairs with the S47 admin-side visual signal (per-doc colored border + RENEW button) so jemaah see the same warning in their inbox without admin manually nudging. **Groups all expiring docs into ONE email per jemaah per run** (rather than N separate inbox hits when a passport + visa + vaksin expire close together). **Excludes REJECTED + EXPIRED status** — REJECTED needs full resubmission not renewal, EXPIRED is covered by a different surface (admin manual follow-up). Excludes docs with no `expiresAt` (e.g. marriage cert). Per-jemaah `cooldownDays=7` via the Notification table. Soonest-expiring jemaah surface first; docs within each jemaah row also sorted soonest-first. New `DOC_EXPIRING_SOON` enum (migration applied). CLI `npm run job:send-doc-expiring` + HTTP `POST /api/admin/jobs/send-doc-expiring` + jobRunner daily interval. Per-channel opt-out via `JemaahProfile.notifEmail` (DB filter, not just enqueue-time skip — saves a roundtrip).
174. ✅ Waitlist CSV export — `buildWaitlistCsv(paketSlug)` exports every `PaketWaitlist` row for offline outreach campaigns (admin pulls the CSV, works through phone calls in a spreadsheet, marks promote/cancel via admin UI as they reach each jemaah). **Includes ALL statuses** (WAITING/PROMOTED/CANCELLED) — historical context "we already tried this person" is useful, the view's status filter is UI hygiene not data hygiene. Columns: createdAt / status / fullName / phone / kelas / paxCount / notes / promotedBookingId / promotedAt / cancelledAt. Footer carries per-status counts. UTF-8 BOM + RFC 4180 / CRLF matching the convention. Route `/admin/paket/:slug/waitlist.csv` (inherits the existing `paketAdminRouter` RBAC gate). "⇩ CSV Outreach" button in the waitlist page header, only when `rows.length > 0`.
175. ✅ Cancel reason categorization — new `BookingCancelReason` enum + `Booking.cancelReasonCode` nullable column (migration applied + composite index for the analytics scan). 7 categories: JEMAAH_REQUEST / PAKET_CANCELLED / PAYMENT_NOT_RECEIVED / DOCUMENT_INCOMPLETE / NO_SHOW / GOODWILL / OTHER. Admin picks from dropdown in the cancel modal; `cancelBooking({reason, reasonCode})` validates against the enum (case-insensitive normalisation, BAD_CANCEL_REASON_CODE 400 on typo) and persists alongside the free-text `cancelReason`. **Nullable** → historical cancels (pre-S175) stay valid; `null` codes bucket under `__UNSET__` in the analytics so admin sees the categorisation backlog. `getCancelReasonBreakdown({days=90})` aggregates CANCELLED + REFUNDED bookings by code with count + sharePct + paidIdr. **Sort by count desc, __UNSET__ always last** regardless of size so the categorised data dominates the visual ranking. Panel on `/admin` overview between no-show analytics and komisi monthly; hidden when `total === 0`. Audit row also carries `cancelReasonCode` when present so compliance scans can answer "how many goodwill cancellations did we issue last quarter?".
176. ✅ Per-paket break-even tracker — `getPaketBreakEven({paketId})` projects "N more LUNAS bookings until cost is covered" using existing S22/S39 cost + komisi math. **Per-pax formula**: `marginPerPax = avgRevenuePerPax − costPerPaxIdr − avgKomisiPerPax`; `booksNeeded = ceil(−netSoFarIdr / marginPerPax)` when net is negative. Uses LUNAS-bookings averages so the projection reflects the agent mix actually selling the trip (high-komisi-rate agents pull marginPerPax down). **Day-one fallback** — when zero LUNAS yet, falls back to featured `PaketHarga.priceIdr` + paket `komisiRate` so the widget still surfaces on a fresh paket. Returns `hasCost=false` when `costPerPaxIdr` is unset (no break-even target exists). **`booksNeeded=null` when marginPerPax ≤ 0** — surfaces a "tidak mungkin: naikkan harga / turunkan biaya" warning instead of a misleading positive number. `feasible` flag compares against `seatsLeft` so admin sees "need 20 more but only 5 kursi left → won't fill". Panel on `/admin/paket/:slug/edit` below the existing profitability gauge.
177. ✅ Last-login sort on user list — `listUsers({sortBy='lastLogin'})` orders by `lastLoginAt asc nulls:last` (oldest-logged-in first, never-logged-in lands at the very bottom). `/admin/users?sortBy=lastLogin` toggle in the table header preserves the existing q/role/status/deleted filter state. Column shows the date + relative `Nh lalu` subtitle; color tier (>30d amber, >90d ruby). **Admin roles get an extra ⚠ warning glyph** when login >60d (OWNER/SUPERADMIN/MANAJER_OPS/KASIR/SALES — these accounts shouldn't be dormant). Never-logged-in admin gets a ruby "admin tdk pernah login" tag. Pairs with S102/S104 (inactive jemaah prune + soft-deleted viewer) — same observability theme, different angle (sort vs filter).
178. ✅ Bunking room swap — `swapBookingRooms({bookingIdA, bookingIdB})` swaps two bookings' room assignments in one transaction; saves the 4-step manual flow (unassign A, unassign B, reassign each). **Cross-paket pair rejected** (PAKET_MISMATCH) since "operationally meaningless and would muddy the audit trail". **Symmetric validation**: kelas check on each destination + capacity check that excludes the two swap participants (they cancel each other in occupancy math). **Same-room → no-op return** (no audit pollution). **Three-phase transaction** (detach A → attach B → attach A) avoids per-row constraint hits mid-swap. Two-click UI in admin Bunking tab: per-slot ⇄ button selects source (gold-highlighted), click another booking's ⇄ swaps. Floating bottom banner shows current source + cancel link. Each booking gets its own audit row stamping `swap: true` + `swappedWith: <other-bookingNo>` so the booking-detail timeline shows both legs.
179. ✅ Admin team sticky note — single-row `AdminTeamNote(id='singleton', body, updatedById, updatedByEmail, updatedAt)` (migration applied) for cross-admin team communication. Visible to all admin sessions on `/admin` overview header; collapsible read view + inline edit form (any admin role can edit). **Singleton pattern** — `id='singleton'` lets us always upsert by known PK without tracking row ids. **Skip-audit-on-no-op** (mirrors `updateBookingNotes` convention) so re-saving identical text doesn't pollute the audit log. `getAdminTeamNote()` returns null when empty (view shows "belum ada catatan" placeholder). `updateAdminTeamNote()` cap 4000 chars + clears to null on empty string. POST `/admin/team-note` (any of the 4 admin roles).
180. ✅ Booking note templates — `BookingNoteTemplate(name UNIQUE, body, sortOrder)` (migration applied) for reusable common phrases (lansia / mahram / diet khusus). Quick-insert `<select>` dropdown above the notes textarea on `/admin/bookings/:id` — picking an option appends the template body to the existing text (with `\n` separator when needed) so admin can stack multiple templates + handwrite. **Listing readable by 4 admin VIEW roles** so the dropdown surfaces for MANAJER_OPS + KASIR too; **mutations OWNER+SUPERADMIN only** via `/admin/bookings/note-templates` CRUD page. P2002 unique-name collision → 409 `TEMPLATE_NAME_TAKEN` (more actionable than the opaque Prisma error). Each CRUD operation writes an audit row with before/after diff. Dropdown hidden when `noteTemplates.length === 0` so first-time admins don't see a confusing empty control.
181. ✅ Notification inbox pagination — `listMyNotificationsPaginated(userId, {page, pageSize=50})` returns `{rows, total, pagination: {page, pageSize, pageCount}}`. Page clamped to ≥1 (negatives floor to 1), pageSize clamped to [1..100]. Wired into `/saya/notifications` + `/agen/notifications` + `/crew/notifications` — all three routes pass `?page=N` through. **mark-all-read still runs on every visit regardless of page** so the unread badge clears even when viewing older history. Shared `views/partials/notification-list.ejs` gets a footer with prev/next pagination links + "Halaman N dari M · total notif" copy; hidden when `pagination.pageCount === 1`. `jemaah-notifications.ejs` has its own inlined row markup (predates the partial) — got a matching pagination footer block. The legacy unpaginated `listMyNotifications` export is kept for callers that just want the recent slice (KPI count, badge, etc.).
182. ✅ Bookings cancel reason filter — `searchBookings({cancelReasonCode})` accepts the S175 BookingCancelReason enum values, `'__UNSET__'` sentinel (for cancelled rows without a category yet — admin's categorisation backlog), or `'ALL'` (default, no filter). `__UNSET__` implicitly scopes to `status IN (CANCELLED, REFUNDED)` since `cancelReasonCode` is only set on those. **Unknown codes silently fall through to no filter** (defensive — a bookmark with a renamed enum value shouldn't crash the page). New dropdown in the `/admin/bookings` filter bar between Agen and Dari; pagination URL preserves the cancel reason just like other filters. Pairs with S175 panel on `/admin` overview: admin sees "5 GOODWILL cancels last quarter" in the analytics, clicks through, lands on filtered list.
183. ✅ Refund preset percentages — quick-action buttons (25% / 50% / 75% / BALANCE 100%) above the refund amount input on `/admin/bookings/:id` refund modal. Click computes `Math.floor(paid * pct / 100)` and fills the amount field. **`Math.floor`** avoids fractional Rupiah (refund Decimal(15,2) is whole-cent strict). BALANCE button visually emphasised (gold border) since 100% is the full-refund signal that interacts with the S145 no-show ack checkbox. Pure UI — no service or schema changes. Pinned the percentage math in a test so any future server-side preset API stays consistent.
184. ✅ Booking notes text search — `searchBookings({notes})` adds a substring filter on `Booking.notes`. **Minimum 3 chars** required (silently ignored below) so the filter doesn't quietly match every row when admin types 1-2 chars in the input. Whitespace trimmed before matching. New "Cari di catatan" input in the `/admin/bookings` filter bar between bookingNo search and Status. Pagination preserves the filter across pages. Useful for "show me all bookings with 'wheelchair' note" type follow-up questions; pairs naturally with the S180 note templates (templates emit the standard phrases, search finds them later).
185. ✅ Inactive AGEN dormant flag — `AgentProfile.dormantSince DateTime?` column (migration applied). Daily cron `scan-agent-dormancy` flags ACTIVE agents with no booking + no lead activity in last `inactiveDays=60`. Auto-clears (back to null) on the next run when fresh activity is detected — agent doesn't have to manually un-flag themselves by closing a deal. **Activity = booking via the agent in window OR lead with `updatedAt >= cutoff`** (covers both new leads and follow-ups on stale ones). SUSPENDED/soft-deleted users excluded from the scan (already inactive by definition). Walk-in / Kantor-Pusat agents (no agent FK) not considered — signal is per-salesperson, not per-channel. One audit row per transition (flagged OR cleared) so compliance scans can answer "when did agent X go dormant?". 💤 red badge on `/admin/users` agent profile column when set. CLI `npm run job:scan-agent-dormancy` + HTTP `POST /api/admin/jobs/scan-agent-dormancy` + jobRunner daily interval.
186. ✅ Lead bulk mark-as-LOST — `bulkMarkLeadsLost({agentId, leadIds})` flips selected COLD/WARM leads to LOST in one shot. **Per-row ownership enforced** but cross-agent IDs **silently skipped** (not 403) — the kanban filter already scopes by agent, so a cross-agent ID arriving here is a stale browser tab or someone fuzzing; aborting the whole batch would be hostile UX for the legitimate part. Already-LOST and CONVERTED rows skipped (terminal — no transition to write). **Payload capped at 500 IDs** defensively against runaway clients. One audit row per actually-flipped lead (matches `updateLead` single-row shape so the timeline reads consistently). Floating bottom action bar in `/agen` kanban appears when ≥1 checkbox checked; "Tandai LOST" POSTs to `/api/leads/bulk-lost`. Checkbox per-card via `lead-card.ejs` partial.
187. ✅ Crew per-jemaah notes — `CrewJemaahNote(paketId, jemaahId, crewUserId, body)` model (migration applied) for crew to leave private notes per jemaah on the assigned manifest (e.g. "lansia perlu pendamping", "alergi laut"). **Composite unique on `(paketId, jemaahId, crewUserId)`** so re-save upserts in place rather than stacking rows — two different muthawwif on the same paket can each leave their own note. **Empty body deletes** the row (treated as "remove my note"). `assertJemaahOnPaket` tuple-guard: jemaah must be on this paket via a non-CANCELLED booking (anti-enumeration). Unassigned crew gets 404 NOT_ASSIGNED (mirrors the manifest access pattern). Inline `<details>` editor per row on `/crew/paket/:slug`; read-only roll-up grouped by jemaah on the admin manifest tab with "📝 from <muthawwif name>" attribution. Best-effort load on the admin side — failure to fetch crew notes doesn't 500 the whole manifest page.
188. ✅ User list AGEN tier filter — `listUsers({tier='BRONZE'|'SILVER'|'GOLD'|'PLATINUM'|'ALL'})` filters AGEN by `AgentProfile.tier`. **Implicitly narrows `role` to AGEN** when tier is set (otherwise the join would silently return nothing for non-AGEN). **Case-insensitive normalisation** so a stored lowercase tier still matches uppercase dropdown selection. **No canonical-validation** — unknown tiers (e.g. future "DIAMOND") still filter; no rows match, but the query doesn't error. New dropdown next to the existing deleted-filter on `/admin/users` filter bar; RESET link appears when any non-default filter is applied. Pairs with the S185 dormant badge so OWNER can scan "GOLD agents who went dormant" in one query.
189. ✅ Lead reactivation button — `reactivateLead({leadId})` un-archives a soft-deleted lead from the S59 hint panel on `/admin/jemaah/:id/edit`. Clears `deletedAt` + **flips status back to COLD** (re-engagement is fresh contact, not continuation of WARM heat). Already-active leads return `{reactivated:false, reason:'already_active'}` idempotently — no audit pollution. CONVERTED leads blocked with 409 `LEAD_TERMINAL` even when soft-deleted (already became a booking). **`agentId` stays the same** — reactivation keeps the original ownership; admin acts on behalf of the agent, doesn't re-attribute. Audit row stamps `reactivated: true` so timeline scans can answer "which leads came back from the dead?". Per-row "↻ Aktifkan" button in the archived-leads table; CONVERTED rows show "terkunci" placeholder instead of the button.
190. ✅ Per-paket FAQ — `PaketFaq(paketId, question, answer, sortOrder)` model (migration applied) for admin-curated Q&A per paket. JSON CRUD via `/api/paket/:slug/faqs` (3-role gate). Inline table editor on paket-edit form with per-row Save/Delete + footer add-row; rendered as collapsible `<details>` accordion on `/p/:slug` below the hero. **Falls back to hardcoded defaults** when no curated FAQs exist (so empty fresh paket still gets a sensible "paspor harus aktif berapa bulan?" et al.). Sort by `sortOrder asc, createdAt asc` so admin controls priority + ties broken by creation order. First-entry auto-open via `i === 0` so jemaah sees one answered question without clicking. CRUD operations write audit rows with before/after diffs.
191. ✅ Manifest age-bracket counts — `computeAgeBuckets({bookings, departureDate})` pure-JS pass over already-loaded bookings; counts pax into Anak (<12y) / Dewasa (12-59y) / Lansia (≥60y) / Unknown (missing birthDate) buckets. **paxCount-aware**: a 3-pax family booking with adult lead jemaah counts 3 toward dewasa (matches "one row per booking" manifest convention). **Excludes CANCELLED + REFUNDED** so the strip reflects "who's actually going". **Age computed at paket `departureDate`**, not "now" — a 59-year-old today departing next year shows as 60 (lansia) which matches what crew needs to plan for at the time of the trip. Same-day birthday counts as that year. Lansia cell gold-bordered + bold (operationally significant — wheelchair / ihram help / slower walks). Loaded inside `getManifestForPaket` as a best-effort attached field so a compute error doesn't 500 the manifest page.
192. ✅ Per-paket announcement banner — `PaketAnnouncement(paketId, title, body, publishedAt, expiresAt, authorId)` model (migration applied) for admin pinned messages to jemaah on this paket. Multiple announcements per paket allowed. `listActiveAnnouncements({paketId, now})` filters `publishedAt <= now AND (expiresAt == null OR expiresAt > now)` — admin can schedule future announcements + set expiry to auto-hide stale ones. JSON CRUD via `/api/paket/:slug/announcements` (3-role gate). Banner renders on `/saya/bookings/:id` above the info panel as gold-bordered card with 📢 prefix; multiple banners stack newest-first. `null` expiresAt means "never expires" (sticky pinned). Per-CRUD audit rows; `authorId` FK uses SetNull on User delete so deleting an admin doesn't cascade-delete their announcements.
193. ✅ Push notif for paket announcements — `createAnnouncement` fires a push notif to every active jemaah on the paket via the S93 `pushToUser` fan-out. **Gated on `publishedAt <= now`** — scheduled-future announcements don't push until they go live (admin would need a separate cron to handle that handoff, deferred). Distinct `userIds` deduplicated (one jemaah with multiple bookings on the same paket gets one push, not N). Anonymous bookings (`jemaahUserId=null`) skipped — no push target. **Fire-and-forget** posture: a push failure logs but doesn't abort the announcement create transaction; the on-page banner is the load-bearing surface and push is purely additive. Body preview truncated to 140 chars (browser notification body limit). Payload includes `data.announcementId` so the SW handler can wire deep-link to `/saya` on click.
194. ✅ Public paket SEO/OG meta tags — `buildPaketSeo(paket, agent, {baseUrl, dateFormatter})` returns `{url, title, description, image, locale}` for the `<head>` block of `/p/:slug`. **Description sanitisation**: strips HTML tags, collapses whitespace, truncates to 160 chars (Twitter cuts around 200, Facebook 300 — 160 keeps the preview clean across all surfaces). Falls back to a generic "Paket X bersama Religio Pro. Berangkat YYYY-MM-DD, N hari." line when `heroDescription` is unset. URL uses `PUBLIC_BASE_URL` env when set (absolute), otherwise relative `/p/<slug>` (still valid for crawlers landing through a direct link). Agent slug URL-encoded into `?a=` param for referral attribution. Includes Open Graph (`og:type`, `og:url`, `og:title`, `og:description`, `og:image`, `og:site_name`, `og:locale=id_ID`) + Twitter Card (`summary_large_image`). Default OG image at `/shared/og-default.jpg` (admin uploads to override).
195. ✅ Booking voucher QR code — `drawQrCode(doc, text, {x, y, size, errorCorrectionLevel='M', dark, light=null})` renders a QR code into a pdfkit document using the `qrcode` package's synchronous `create()` API + filled rectangles per module. `errorCorrectionLevel='M'` (~15% recovery) is the sweet spot for printed vouchers — survives small coffee spills and folds. `light=null` keeps the QR background transparent so the voucher's cream background shows through. Wired into `bookingVoucherPdf.js` at top-right (460,140) as an 80pt square encoding `${PUBLIC_BASE_URL}/admin/bookings/<id>` so crew/admin scans at airport check-in to pull up the admin booking detail. Caption "SCAN UNTUK CEK-IN" below. **Best-effort** — QR generation failure logs but doesn't abort the voucher PDF (the voucher is the load-bearing artifact). New `qrcode` dep (~50KB pure-JS, no native bins). **S197 updated** the QR URL to point at the public verification page.
196. ✅ Per-paket pickup points — `PaketPickup(paketId, label, address, departTime, notes, sortOrder)` model (migration applied) for multiple bus pickup locations per paket (Bekasi/Bogor/Tangerang). JSON CRUD via `/api/paket/:slug/pickups` (3-role gate). Inline table editor on paket-edit; rendered as 240px-wide card grid on `/saya/bookings/:id` with ⏰ time + 📍 address + optional italic notes. **`departTime` is a freeform `VarChar(5)`** rather than strict TIME — admin might type "05:00" or "5:30" and we accept both; tighter validation can come later. Sort by `sortOrder asc, createdAt asc` so admin controls display order, ties broken by creation. Empty `departTime` stored as null (not empty string). Per-CRUD audit rows.
197. ✅ Public QR verification page — `/v/<bookingId>?sig=<hex16>` route. `buildVerifyUrl(bookingId)` signs the booking ID via HMAC-SHA256 (key=`env.JWT_SECRET`, payload=`voucher:<id>`, truncated to 16 hex chars matching the S77 emailClickToken convention). `verifyVoucherSig` uses `timingSafeEqual` to prevent timing attacks. View shows MINIMAL info on valid scans: bookingNo + paket title + **first-name only** + departureDate + kelas/pax + status (no PII reveal even on a leaked URL). Invalid signature → "VOUCHER PALSU" page explaining the likely causes (edited photo, manual URL entry, deleted). `<meta name="robots" content="noindex,nofollow">` so verify URLs don't pollute search results. NO auth required — the QR is the credential. S195's voucher PDF QR now encodes this URL instead of the admin booking URL, so anyone (airport staff, crew without app, jemaah confirming their own voucher) can verify authenticity.
198. ✅ Voucher print history — `Booking.voucherPrintCount` + `lastVoucherPrintedAt` + `lastVoucherPrintedByEmail` columns (migration applied). `recordVoucherPrint({bookingId, actorEmail})` bumps the counter + stamps `now()` + records the actor's email on each voucher download. Wired into both `/admin/bookings/:id/voucher.pdf` (admin email) and `/saya/bookings/:id/voucher.pdf` (jemaah email) via `res.on('finish')` (fire-and-forget AFTER the response so latency isn't affected). **Counter pattern** chosen over a full history table — the count is enough signal for the support workflow ("voucher printed 3× already, ask before reissuing"). Best-effort: missing booking row → swallowed by the helper's try/catch (defensive). Surfaced on `/admin/bookings/:id` as a small `Nx` badge on the PDF download button + `title` hover with timestamp + actor email.
199. ✅ Print manifest age column — `getPrintManifest` now attaches `jemaah.age` + `jemaah.ageBracket` (`ANAK`/`DEWASA`/`LANSIA`/`UNKNOWN`) per row using S191's `computeAge` helper. Age computed at `paket.departureDate` (not "now") — matches what crew needs to plan for at the time of the trip. New "Umur" column in the print worksheet shows `{age}y` + bracket label; **Lansia rows gold-bordered + gold background** for operational planning (wheelchair, ihram help). Anak rows get subtle blue tint. Header KPI strip now includes `Nlansia · Manak` when either is >0; hidden when only dewasa. UNKNOWN bracket (null birthDate) renders as "—" plain. CANCELLED/REFUNDED already excluded by the existing query so they don't pollute the age math either.
200. ✅ Bulk room assignment by floor — `bulkAssignRoomsByFloor({paketId, floor})` greedy first-fit: for each unassigned booking (createdAt asc — earliest jemaah get first dibs), find the first room on `floor` with matching kelas + remaining capacity ≥ paxCount, assign. **Whole call non-transactional** — each assign is its own DB write + audit row; partial success is fine (admin re-runs to pick up freed rooms). Returns `{assigned[], skipped[], errors[]}` per-row so the UI shows "5 assigned, 2 skipped (capacity), 1 error". CANCELLED/REFUNDED bookings ignored (closed). Per-assigned audit row stamps `bulkAssignByFloor: true` + `floor: N` so timeline scans can answer "which bookings did bulk-assign touch?". Per-floor `⇣ Bulk-assign` button in the bunking-tab `<h3>` — only rendered when there are unassigned bookings AND the floor is numeric.
201. ✅ Audit log content search — `listAudits({q})` adds substring matching across `entityId` + `before` JSON + `after` JSON. **Two-phase query**: raw `$queryRaw` finds matching audit IDs via `CAST(\`before\` AS CHAR(8000)) LIKE pattern` + same for after (backticked since `before`/`after` are MariaDB reserved keywords), then the regular Prisma `where: { id: { in } }` applies the rest of the filters (entity/action/actorEmail/date) over that bounded set. **≥3 chars required** (silently ignored below) to avoid scanning the whole table on stray keystrokes. LIKE wildcards (`%`/`_`/`\\`) escaped so literal underscores in IDs/UUIDs match correctly. **Hard cap 5000 candidate IDs** so a too-broad query can't OOM. New "Cari konten (≥3 char)" input in the `/admin/audit` filter bar; preserved across pagination + export CSV. Useful for compliance ("find every audit mentioning UUID X" / "find every booking note containing 'wheelchair'").
202. ✅ Booking pickup choice — `Booking.pickupId` FK to `PaketPickup` (S196), `onDelete: SetNull` so removing an obsolete pickup falls back to "TBD" rather than cascading the booking away. `setMyBookingPickup({userId, bookingId, pickupId})` validates: booking owned by user, pickup belongs to booking's paket (PICKUP_MISMATCH on cross-paket), CANCELLED/REFUNDED rejected. **Idempotent re-pick** returns `{updated:false}` so no audit pollution from double-tap. **null clears** the choice (also idempotent). Per-pick audit row stamps `pickupChosen: true` + label so the timeline reads "jemaah chose Bekasi". Jemaah-side UI: click-a-card on `/saya/bookings/:id` posts to `/api/saya/bookings/:id/pickup`; current selection gold-bordered; "batalkan pilihan" link clears with confirm.
203. ✅ Passport renewal reminder — daily cron emails + WAs jemaah whose `JemaahProfile.passportExpiry` is within 90 days (Saudi rule: passport must be valid ≥6 months after departure, so 90 days early gives queue time). **Distinct from S173 `DOC_EXPIRING_SOON`** — that one walks `JemaahDocument.expiresAt`, but many jemaah carry passport metadata on the profile (`passportNo`+`passportExpiry`) without a `JemaahDocument` row, so doc-expiring sweep misses them. This service walks the profile directly. Per-jemaah 30-day cooldown via the Notification table — 90d window with 30d cooldown means jemaah gets at most 3 nudges before actual expiry. **Walks JemaahProfile not Booking** so re-bookings don't generate duplicate nudges per jemaah. Respects `notifEmail` opt-out at the DB filter (saves a roundtrip). Suspended/soft-deleted users excluded. Soonest-expiring first. New `PASSPORT_RENEWAL_REMINDER` enum (migration applied). CLI `npm run job:send-passport-renewal` + HTTP `POST /api/admin/jobs/send-passport-renewal` + jobRunner daily interval.
204. ✅ Per-user audit activity timeline — `getUserActivityFeed({userId, limit=50})` returns recent `AuditLog` entries where this user was the actor. Rendered as a compact table panel on `/admin/users/:id/edit` (Owner asks "what has this kasir been doing?" without leaving the page). Shows action / entity / target ID / IP / timestamp. **Action color-coded** (CREATE emerald, UPDATE gold, DELETE ruby). Last-8 chars of `entityId` shown to keep the column compact; Booking entityIds get a deep link to `/admin/bookings/<id>` for one-click drill-down. Limit capped at 200 (default 20 on edit page); negative or zero limit floors to 1. Footer link to `/admin/audit?actorEmail=<email>` for full pagination of older history. Empty state when user has no audited actions. Best-effort load — a query failure dims the panel via null, doesn't 500 the edit page.
205. ✅ Pickup summary + manifest filter — `getManifestForPaket` now attaches a `pickupCounts` array (one row per pickup label + `paxCount` + `count`) computed in JS from already-loaded bookings (no extra query). CANCELLED/REFUNDED excluded. **TBD bucket always rendered last** regardless of size so admin always sees the "who hasn't picked yet" backlog. `filterManifestByPickup(result, pickupId)` returns a shallow copy with `bookings` narrowed; `'__TBD__'` sentinel targets unchosen. Admin manifest tab gets a clickable count strip (per-pickup card + reset link); URL preserves `?manifestPickup=` so paging/exports stay scoped.
206. ✅ Booking note pin/unpin — `Booking.notesPinned` boolean column (migration applied, default false). `toggleBookingNotesPinned({bookingId, pinned})` flips the flag; **idempotent re-pin** returns `{updated:false}` (no audit pollution from double-tap); **EMPTY_NOTES 409** when trying to pin a booking with no notes (no point pinning empty content). Per-flip audit row stamps `field: 'notesPinned'`. Pinned notes render at the very top of `/admin/bookings/:id` as a gold-bordered banner with 📌 prefix + "Unpin" button; above all other panels so urgent context (VIP, prior dispute warnings, special handling) isn't buried. "📌 Pin catatan ini" button next to SIMPAN on the notes form when notes exist + not yet pinned. Same RBAC as note editing (CANCEL_ROLES).
207. ✅ Crew portal task list — crew's `/crew` dashboard now shows their own open `Task` rows (S91) via `getMyOpenTasks({assigneeEmail: crew.email})`. Read-only on this surface — admin still owns create/complete/cancel; crew sees the work assigned to them at a glance. Sorted by `dueAt asc, createdAt asc` so overdue/imminent bubble to the top. **Overdue rows ruby-bordered**; count surfaced in panel header ("8 tugas terbuka · 3 overdue"). Each row shows bookingNo + jemaah name + paket title + due date + createdByEmail. Best-effort load — task query failure dims the panel via null, doesn't 500 the dashboard.
208. ✅ Pickup roster CSV — `buildPickupRosterCsv(paketSlug, {pickupId})` per-paket export grouped by pickup point so bus drivers download a roster before departure to verify who they're picking up. Reuses S205's pickup grouping + same `__TBD__` sentinel for jemaah without a choice. Rows sorted by pickup `sortOrder asc` (admin-controlled display order), `TBD bucket always last` regardless of size. CANCELLED/REFUNDED excluded. UTF-8 BOM + RFC 4180 quoting + CRLF (matches the convention). Per-pickup footer summary line (e.g. `Bekasi=2; Bogor=1; TBD=3`). Optional `pickupId` query (or `__TBD__`) narrows to one bus route so each driver only downloads their own list. Filename `pickup_roster_<slug>[_<pickup>].csv` sanitised against path traversal. Route `/admin/manifest/:slug/pickup-roster.csv` inherits the existing 3-role gate via `/admin` mount. "🚌 Roster pickup" button in the manifest tab toolbar preserves the active pickup filter from the URL.
209. ✅ Booking dup-check by NIK — extends S167's phone-based dup warning to also check `JemaahProfile.nik` so walk-in admins catch duplicate bookings even when the jemaah uses a different phone (family-shared phones make phone-only matching noisy). New `findRecentBookingsByNik({nik, windowDays=90})` does exact equality match (NIK is a 16-digit national ID, no fuzzy/endsWith needed). Returns empty when NIK <10 digits (avoids partial-NIK scans matching too much). `POST /admin/bookings` runs parallel phone + NIK lookups, merges results via a `flagsById` Map (booking flagged 'phone', 'nik', or both). **Severity coloring**: NIK matches get ruby border ("kemungkinan jemaah yang sama") since NIK is a unique person ID; phone-only stays gold (family share is plausible). Per-row badge shows TEL / NIK / TEL+NIK flag chips. `NewBookingSchema` accepts optional `nik` field with 3-state preprocessor (whitespace cleanup, 16-digit pattern validation).
210. ✅ Jemaah dietary preferences — `JemaahProfile.dietary` enum (REGULAR / VEGETARIAN / HALAL_STRICT / SOFT_TEXTURE / DIABETIC / OTHER, migration applied) + `dietaryNotes VARCHAR(500)` for catering / hotel meal planning. **REGULAR is default** (back-compat — existing rows + new anonymous bookings stay neutral). SOFT_TEXTURE for elderly jemaah who can't chew well; DIABETIC for sugar control; OTHER + notes for everything else (allergies, vegan-strict, kosher-equivalent). Wired into both admin (`/admin/jemaah/:id/edit`) and jemaah self-service (`/saya/profile`) form panels. **3-state preprocessor** for the schema: `undefined` (form omitted) → no change; explicit empty → clear (dietary resets to REGULAR, notes to NULL); value → set. Manifest table on admin overview renders a colored dietary pill **only when non-REGULAR** (REGULAR is the default — catering doesn't need a callout for the silent majority) with hover tooltip carrying the free-text `dietaryNotes`. Pill colors: emerald VEG / gold HALAL+ / amber SOFT / ruby DIAB / sapphire other. Crew manifest also includes the fields so muthawwif can flag special meals to hotels on arrival. Voucher PDF deliberately omits dietary — not part of the customer-facing handout, lives only in operational surfaces.
211. ✅ Dietary roll-up CSV — `buildDietaryRollupCsv(paketSlug)` per-paket export for catering / hotel kitchen brief. Two sections in one file: per-jemaah list of non-REGULAR diets (bookingNo / name / kelas / room / dietary / notes), and a footer line with per-category pax tally (`REGULAR=120; VEGETARIAN=3; DIABETIC=1`). **REGULAR rows excluded from per-jemaah list** — kitchen only cares about the exceptions; the tally still counts them so the standard-meal volume is visible. **Sort by dietary code then jemaah name** so all DIABETIC rows group together (kitchen processes one category at a time). CANCELLED/REFUNDED excluded (already off the trip). UTF-8 BOM + RFC 4180 + CRLF matches the convention. Route `/admin/manifest/:slug/dietary.csv` inherits the 3-role gate. "🍽 Diet CSV" button in the manifest tab toolbar alongside the export + pickup roster + cetak buttons.
212. ✅ Pickup point capacity — `PaketPickup.maxCapacity` column (migration applied) caps total pax per bus. **NULL = no cap** (back-compat default — existing rows + admin who hasn't set a limit work unchanged). Clamped 1..200 at schema validation. S202 `setMyBookingPickup` adds an aggregation check: sum of `paxCount` on the target pickup (excluding the current booking when re-picking) + current booking's pax must not exceed cap, else 409 `PICKUP_FULL` with the occupied/cap numbers in the message. **Excludes current booking from the sum** so re-picking the same pickup at cap is a clean no-op (idempotent) and switching FROM a full pickup TO another is allowed (jemaah's own seat doesn't count against the destination). CANCELLED/REFUNDED don't occupy a seat. New `listPickupsWithOccupancy(paketId)` decorates each row with `occupiedPax` + `isFull` via one `groupBy` aggregation (no N+1). Jemaah-side: `/saya/bookings/:id` renders a "PENUH" ruby pill on full pickups, dimmed + non-clickable; capacity `N/M pax` line shows under the address when cap is set. Paket-edit pickup table gets a "Cap" number input column for inline edit.
213. ✅ Crew dietary brief email — Monday morning email per (assigned MUTHAWWIF × paket departing within `windowDays=14`) listing every non-REGULAR jemaah on the paket so muthawwif can brief the hotel kitchen on arrival. New `CREW_DIETARY_BRIEF` enum (migration applied). Body groups specials by dietary code (DIABETIC section, VEGETARIAN section, etc.) with one row per jemaah carrying full name + room + free-text dietary notes. Header summary: total pax / special pax / per-category tally. **Silent on three cases**: crew has no email (no point queueing), zero specials on the paket (all-REGULAR brief carries no signal — kitchen doesn't need an empty list), enqueue failure. SUSPENDED + soft-deleted crew excluded; ARCHIVED + soft-deleted paket excluded. CANCELLED/REFUNDED bookings not in specials. Crew with multiple near-departure paket get one email per paket — keeps each brief focused on one hotel. CLI `npm run job:send-crew-dietary-brief` + HTTP `POST /api/admin/jobs/send-crew-dietary-brief` + jobRunner weekly interval. Pattern: per-crew failure caught in batch so a bad row doesn't abort the run.
214. ✅ Crew portal dietary panel — `getAssignedManifest` decorates the return value with `dietarySummary: {tally, specials, totalPax, specialPax}` so muthawwif sees the S213 brief inline on `/crew/paket/:slug`, not just in the Monday email. Tally counts ALL pax including REGULAR (gives standard-meal volume); specials list excludes REGULAR (kitchen brief only cares about exceptions). Panel renders gold-bordered above the booking table, grouped by dietary code with one row per jemaah carrying full name + room + free-text notes. **Hidden when zero specials** so all-REGULAR trips don't waste real estate. Mirrors S211 CSV / S213 email layout so crew + admin + kitchen see the same numbers in three places.
215. ✅ Manifest dietary filter — `filterManifestByDietary(result, dietary)` narrows the admin manifest table by `?manifestDietary=` query param. Supports exact codes (REGULAR / VEGETARIAN / etc.), `__SPECIAL__` aggregate (all non-REGULAR — useful for "show me everyone the kitchen needs to know about"), and `ALL` / empty to clear. **Unknown codes silently fall through to unfiltered** (defensive against bookmark with renamed enum). **Null/missing dietary counts as REGULAR** — pre-S210 jemaah rows show up under the REGULAR filter, not vanish into a separate bucket. Composes on top of the S205 pickup filter (both shallow-copy the bookings array, so filter order doesn't matter). Dropdown in the manifest tab toolbar; active filter renders a gold chip with reset link next to the pickup reset.
216. ✅ Announcement EMAIL broadcast — `createAnnouncement` now also enqueues one EMAIL per distinct active jemaah on the paket alongside the existing S193 push fan-out, so jemaah who haven't installed the `/saya` PWA still get the announcement in their inbox. Belt+suspenders for critical messages (visa updates, schedule changes). Uses **GENERIC** notif type (admin body is final text — no template variables to interpolate). **Dedupe by (userId or jemaahProfileId)** so one jemaah with multiple bookings on the same paket gets one email, not N. Anonymous bookings without email skipped. CANCELLED/REFUNDED bookings excluded. `recipientUserId` set when booking is linked so `/saya/notifications` inbox + unread badge surfaces the row. **Gated on `publishedAt <= now`** (same condition as the push) so scheduled-future announcements don't fan out until they go live. **Fire-and-forget** — email failure logged but doesn't abort the announcement create transaction (announcement row is the load-bearing artifact). `relatedEntity='PaketAnnouncement'` + `relatedEntityId` so admin can grep notif queue per-announcement.
217. ✅ Dietary in manifest CSV — `exportManifestCsv` extended with two new columns at the end: `Dietary` (enum code) + `Dietary Notes` (free-text). Pairs with S210/S211 — the dedicated dietary CSV (S211) is a tightly-filtered kitchen brief; this exposes the same data on the full manifest export so admin can grep/pivot a single file. **`null` dietary renders as REGULAR** (pre-S210 jemaah default — keeps the column non-blank and honest about the default). **`null` notes render as empty cell** (not literal `"null"` string). Notes containing commas are quoted per RFC 4180 (same convention as the rest of the export).
218. ✅ Crew portal: paket announcements visible — `/crew/paket/:slug` route loads admin-posted announcements (S192 `listActiveAnnouncements`) and renders them gold-bordered above the dietary panel so crew gets the same heads-up jemaah do ("visa delayed", "pickup time moved"). Crew often needs the same operational news — no role-specific surface for it before. Active-only filter (publishedAt ≤ now AND (expiresAt null OR > now)) matches the jemaah-side `/saya/bookings/:id` panel. Best-effort load — query failure dims the panel via empty array, doesn't 500 the manifest page. **No write surface** — admin still owns CRUD via paket-edit panel.
219. ✅ Pickup choice reminder cron — daily email + WA reminder for active bookings on a soon-departing paket (≤14d default) where the jemaah hasn't picked a pickup yet. New `PICKUP_REMINDER` enum (migration applied). Per-booking 5-day cooldown via the Notification table — 14-day window with 5-day cooldown means max ~3 nudges before takeoff. **Silent on three cases**: paket has zero curated pickup points (admin hasn't set up routes yet — can't remind about something that doesn't exist), jemaah has neither email nor phone, recently-nudged within cooldown. Fires BOTH EMAIL + WA when contacts present (each respects per-channel opt-out via `enqueueNotification`). Soonest-departing first so a future per-run quota hits the urgent ones. `recipientUserId` set when booking is linked so `/saya/notifications` inbox surfaces it. CLI `npm run job:send-pickup-reminder` + HTTP `POST /api/admin/jobs/send-pickup-reminder` + jobRunner daily interval. Walks Booking (not JemaahProfile) since pickup choice is per-booking.
220. ✅ Per-pickup driver info — `PaketPickup.driverName + driverPhone + plateNumber` columns (migration applied; all nullable, admin may not know driver at create). Jemaah sees driver contact below the address on their **chosen** pickup card only (other pickups in the picker don't leak the driver phone to non-passengers). S208 pickup roster CSV gains 3 columns at the start so each driver self-identifies on the roster spreadsheet they download. PickupSchema **uppercases plate** at write (canonical `B 1234 CDE` format). Empty string at all three preprocessors → `null` clear. Paket-edit pickup table gains a sub-row beneath each main row with `driverName / driverPhone / plateNumber` inputs; the Save button reads both rows via shared `data-pickup-id`.
221. ✅ Admin sets pickup on booking — `adminSetBookingPickup({bookingId, pickupId})` mirrors the S202 jemaah self-pick but **without the `jemaahUserId` ownership requirement** (admin route enforces RBAC via `CANCEL_ROLES`). Walk-in / phone bookings can now get a pickup assigned without the jemaah needing a `/saya` account. Same guards: cross-paket pickup rejected (`PICKUP_MISMATCH`), capacity full rejected (`PICKUP_FULL` — S212), CANCELLED/REFUNDED rejected (`BOOKING_CLOSED`). Audit row stamps `adminSet: true` (vs S202's `pickupChosen: true`) so the timeline can distinguish admin assignment from jemaah self-pick. `POST /admin/bookings/:id/pickup` route + form on booking-detail with dropdown showing each pickup's occupancy `(N/M)` and `· PENUH` tag (disabled option) so admin sees the cap state inline. Idempotent re-set + null-clear are both no-ops (no audit pollution).
222. ✅ Per-paket WhatsApp group link — `Paket.waGroupUrl VARCHAR(500)` column (migration applied; nullable). Admin curates the invite URL on paket-edit (validated as http(s) URL via Zod). **Jemaah sees a "Gabung grup WA" emerald CTA on `/saya/bookings/:id` only when LUNAS** — speculative bookings that might cancel don't get group access. Crew sees a "Grup WA" button on `/crew/paket/:slug` regardless of attendance state (they need it for trip coordination). Both surfaces open in a new tab with `rel="noopener nofollow"`. Same 3-state preprocessor as `costPerPaxIdr` / `adsSpendIdr` — undefined preserves, empty clears, value sets.
223. ✅ Per-paket required documents — `Paket.requiredDocs Json` column (migration applied; nullable JSON array). Different paket types need different visas (Saudi umroh needs VISA_UMROH + VACCINE_MENINGITIS; Turkey/Aqsa doesn't). S23 `getPreDepartureChecklist` now resolves the per-paket list via `resolveRequiredDocs(paket.requiredDocs)` instead of hardcoding 4 doc checks. **Always-present checks** (passport identity + room + emergency contact = 4) survive regardless; **variable checks** (one boolean per required doc) extend the total. **Score weights all checks equally** so trimming docs lifts the score — admin who configures a Turkey paket without manasik doesn't see jemaah penalised for a missing cert that doesn't apply. NULL / empty array → fall back to `DEFAULT_REQUIRED_DOCS` (`['VISA_UMROH','VACCINE_MENINGITIS','HEALTH_CERT','MANASIK_CERT']`) for back-compat with pre-S223 paket. Schema preprocessor accepts CSV string OR array; empty array → null (clear); unknown enum strings + PASSPORT (handled as identity check, not doc upload) filtered out at read. Paket-edit form gets a checkbox grid + helper text explaining what each doc is for.
224. ✅ Decline jemaah cancel request — admin can explicitly decline a pending S5ff cancel request without cancelling the booking. `declineCancelRequest({bookingId, reason})` clears the 3 `cancelRequest*` fields, writes an audit row with `cancelRequestDeclined: true` + admin's reason, and fires a GENERIC EMAIL+WA notif to the jemaah explaining the decline. **Closes the loop on hanging requests** — pre-S224 the only outcomes were approve (cancelBooking) or leave indefinite, which confused jemaah (their /saya kept showing "pending") and polluted the needs-attention surface. Refuses on no pending request (`NO_PENDING_REQUEST` 409), already-closed booking (`ALREADY_CLOSED` 409), and missing reason <3 chars (`DECLINE_REASON_REQUIRED` 400 — admin must justify the decline). `POST /admin/bookings/:id/cancel-request/decline` route (`CANCEL_ROLES` gate). Inline `<details>` form on the cancel-request banner in `/admin/bookings/:id`. Notif failure logged but doesn't abort the decline.
225. ✅ Bulk retry FAILED notifications — admin selects multiple terminal-FAILED rows on `/admin/notifications` and one click retries them all via the new floating bottom action bar. `bulkRetryFailedNotifications({ids, limit=200})` resets `attemptCount=0` + clears `nextRetryAt` + dispatches each. **Per-row failure caught** so a bad row doesn't abort the batch. **Non-FAILED rows in the input silently skip** (no error) — admin who clicks retry on a row another admin just handled doesn't see a confusing error. Returns counters `{requested, eligible, retried, failed, skipped}` so the redirect flash shows real numbers. UI uses an outer `<form id="notif-bulk-form">` + `form="..."` attribute on each row's checkbox (HTML disallows nested forms; the per-row Send buttons inside `<td>` still work). Select-all header checkbox supports indeterminate state. Caps at 200 ids per request to avoid runaway.
226. ✅ Booking tags — `Booking.tags Json` column (migration applied; nullable JSON array). Short uppercase labels (VIP / LANSIA / HONEYMOON / KELUARGA / KESEHATAN / PERTAMA / DIFABEL / PRIORITAS — preset chips) for fast visual identification + future manifest filter. `setBookingTags` writes via `normaliseBookingTags` which uppercases, trims, dedupes, validates `/^[A-Z0-9_-]+$/`, caps each tag at 24 chars + array at 8 entries. **Empty list clears to NULL** (back-compat read path stays simple). **Idempotent re-save → no audit pollution** (skip-when-same convention). Refuses on CANCELLED/REFUNDED (`BOOKING_CLOSED` — tags are an active-booking concern, history is frozen). `POST /admin/bookings/:id/tags` route (`CANCEL_ROLES`). Booking-detail page renders gold pills + a CSV-input form with preset chip buttons that append to the input.
227. ✅ Auto-publish scheduled paket — daily cron flips DRAFT paket to ACTIVE when `publishedAt <= now`. Admin pre-schedules a launch by setting publishedAt to a future date while keeping status=DRAFT; this cron handles the handoff on the day so admin doesn't have to remember to flip the status. **Sanity guards** at query level: departure must still be in the future (no point activating a paket whose trip already happened), `kursiTotal > 0` (can't sell a 0-seat paket), `deletedAt: null`. Per-paket failure caught so a bad row doesn't abort the batch. Silent on quiet days (no candidates → no audit row). System actor uses `id=null` + `email='system'` for the audit row; the durable distinguisher from admin manual activation is `after.autoPublished: true`. CLI `npm run job:auto-publish-paket` + HTTP `POST /api/admin/jobs/auto-publish-paket` + jobRunner daily interval.
228. ✅ Pickup info on crew manifest — `/crew/paket/:slug` extends the per-row table with a Pickup column showing each jemaah's chosen pickup label + departure time (TBD amber when unchosen). Adds a `pickupSummary` rollup panel at the top: per-pickup card with pax count + departTime + colour-coded left border (gold for chosen, amber for TBD). **TBD bucket always last** regardless of size (mirrors S205 admin convention) so crew sees the unfinished work clearly. CANCELLED/REFUNDED bookings excluded — the manifest is "who's actually going", same money-stripped + non-leaking pattern as the rest of the crew view. **Key fix**: `pickupId` scalar must be in the Prisma select alongside the `pickup` relation; missing the scalar collapses all bookings into one bucket with the first row's label (Prisma populates the relation but not the FK column by default).
229. ✅ Manifest tag filter — `filterManifestByTag(result, tag)` narrows the admin manifest table by `?manifestTag=` query param. **Case-insensitive** (lowercase input → uppercase match). Composes with the existing pickup + dietary filters (each shallow-copies the bookings array; filter order doesn't matter). Unknown tags silently return zero-row result rather than falling through to unfiltered — admin who bookmarks `?manifestTag=NONEXISTENT` sees an honest empty list, not a misleading full manifest. Tag dropdown options derived from the **raw** manifest (before filtering) so admin still sees every available tag even when narrowed. Clickable tag pills on each manifest row deep-link to `?manifestTag=<tag>`. Active filter chip + reset link mirror the S215 dietary pattern.
230. ✅ Booking tag aggregate KPI — `getBookingTagRollup()` aggregates active (non-CANCELLED/REFUNDED) bookings across non-archived paket and emits per-tag rows with `{tag, bookings, paxCount, paketCount}`. Sort by `paxCount desc` then `tag asc` so the operationally heaviest labels bubble to the top. **`paketCount` = distinct paket carrying the tag** (a VIP across 3 trips signals different operational planning than a VIP concentrated on one paket). DB-level `tags: { not: null }` filter cuts the result set on the typical case (most bookings untagged). Panel on `/admin` overview between cancel-reason and komisi monthly; renders one card per tag with pax/booking/paket counts. Hidden when nothing is tagged (no real-estate waste on quiet weeks). Best-effort load — query failure dims the panel via null, doesn't 500 the overview.
231. ✅ Booking tags on crew manifest — `/crew/paket/:slug` per-row table renders gold-bordered tag pills below the jemaah name when `Booking.tags` is non-empty. Read-only (admin still owns CRUD via S226 booking-detail) — muthawwif sees VIP/LANSIA/etc. at the curb without leaving the portal. **`tags: true` added to both `select` blocks in crewPortal.js** — `getAssignedManifest` and `buildCrewManifestCsv` (the latter for offline-friendly CSV export). CANCELLED/REFUNDED bookings already filtered upstream so they're not visible regardless of tags.
232-234. ✅ Booking tag autopilot — `bookingAutoTag.js` computes 3 tags from data already on the booking: **LANSIA** (S232 — jemaah age ≥ 60 at paket departureDate; birthday-aware), **PERTAMA** (S233 — jemaah has zero prior LUNAS bookings on any paket), **KELUARGA** (S234 — ≥3 active bookings on the same paket share an `emergencyContact`). New `Booking.autoTaggedSeen Json` column (migration applied) tracks which auto-tags have already been computed for a booking. **Additive only** — auto-tag never replaces admin's S226 manual tags, only appends. **"No re-add" rule**: if admin removes an auto-tag, `autoTaggedSeen` still records it; next pass sees it's been computed before and missing now, and respects admin's choice (no fight loop). **KELUARGA retro-tag**: when the Nth booking trips the threshold, `retroTagKeluargaCohort` fans out and tags the prior cohort members with the same EC on the same paket (additive + "no re-add" applies to them too). Auto-tag fires from `createBooking` (fire-and-forget — booking is load-bearing, tag failure logged but doesn't abort). Daily `backfill-auto-tags` cron + CLI `npm run job:backfill-auto-tags` + HTTP `POST /api/admin/jobs/backfill-auto-tags` covers missed firings + new signals (e.g. jemaah's first LUNAS retroactively activates PERTAMA on their NEXT booking, not this one). CANCELLED/REFUNDED bookings refused (frozen history). Per-row failure caught in batch.
235. ✅ Refund reason code — `Payment.refundReasonCode VARCHAR(40)` column (migration applied; nullable) on the negative-amount refund row. Service-side allowlist (REFUND_REASON_CODES): `JEMAAH_REQUEST / PAKET_CANCELLED / VISA_REJECTED / JEMAAH_ILL / DOCUMENT_INCOMPLETE / NO_SHOW_REFUND / GOODWILL / DUPLICATE_PAYMENT / FRAUD_CHARGEBACK / OTHER`. **VARCHAR not enum** so adding codes later doesn't need a migration. Case-insensitive normalisation; empty string → NULL. `issueRefund` accepts optional `reasonCode` arg, validates against the allowlist (`BAD_REFUND_REASON_CODE` 400 on typo), persists on the Payment row + propagates into the audit row. Refund modal on `/admin/bookings/:id` gets a dropdown above the free-text reason; admin who picks "OTHER" gives the free-text below. Pre-S235 refund rows keep `refundReasonCode=NULL` — back-compat for analytics that bucket NULL as "(tanpa kode)".
236. ✅ Refund analytics by reason code — `getRefundAnalytics` extended with `perReasonCode` rollup: per-code refund total + count + share% of total refunded in the window. **NULL `refundReasonCode` buckets under `__UNSET__` sentinel** (mirrors S175 cancel reason convention) so admin sees the categorisation backlog — pre-S235 rows + skipped-dropdown rows surface as "(tanpa kode)". Sort by refunded desc with `__UNSET__` always last regardless of size, so categorised data dominates the visual ranking. Panel renders below the per-paket + per-agent columns on the existing refund analytics card on `/admin` overview; hidden when nothing refunded (parent panel already handles that gate).
237. ✅ Auto-cancel stale unpaid PENDING — daily cron walks PENDING bookings older than `staleDays=14` with `paidAmount=0` and flips them to CANCELLED via the canonical `cancelBooking` path. Frees kursi for future jemaah without admin intervention. **Sanity guards** at query level: paket must still have future `departureDate` (no point auto-cancelling a booking on a paket that already departed — that's S144 no-show territory), `status` not ARCHIVED, `deletedAt` null. **`reasonCode='PAYMENT_NOT_RECEIVED'`** + reason "Auto-cancel: tidak ada pembayaran setelah Nh" written by the system actor (`id=null`, email='system') so compliance scans can grep `cancelReasonCode + actorEmail='system'` for the auto-cancel cohort. Per-row failure caught so a bad row doesn't abort the batch. Silent on quiet days. Cap 200 candidates per run to avoid runaway. CLI `npm run job:auto-cancel-stale-pending` + HTTP `POST /api/admin/jobs/auto-cancel-stale-pending` + jobRunner daily interval.

## Running locally

Requires **Node ≥ 20.6.0** — every script uses the built-in `--env-file-if-exists=.env` flag, which is only available from that version.

```bash
npm install
npm run db:migrate          # apply Prisma migrations
npm run db:seed             # seed users + paket + bookings + rooms + leads + payments
npm run dev                 # http://localhost:3001 (3000 collides, see .env)
```

Default port is `3001` (set in `.env`) because something else owns `3000` on this machine. `npm run db:studio` opens Prisma Studio.

`npm run db:reset` runs `prisma migrate reset` — **destructive**: drops the schema, re-runs every migration, and re-seeds. Never invoke without confirming with the user.

Tests run via Node's built-in `node:test` (zero deps, ships with Node ≥ 18). `npm test` runs `tests/*.test.js`; `npm run test:watch` re-runs on file change. New tests go in `tests/` as `<topic>.test.js`. Shared fixtures + cleanup hooks live in `tests/_helpers.js` — use `tempJemaah(t, tag)` / `tempPaket(t, tag, {dayCount})` / `tempBooking({...})`, all registered with `t.after()` so they're scoped per-test. Tests share the dev DB but isolate by unique `makeTag()` prefixes — never assert on global counts (seed rows exist), always filter by tag.

Older `scripts/smoke-*.js` are ad-hoc smoke runners predating `node:test` — they still work standalone (`node scripts/smoke-XX.js`) but new coverage should be written as `tests/*.test.js`. Migrating the legacy smokes is incremental, not a flag-day.

The static design package is still served — visit `/`, `/screens/paket-detail.html`, etc. — but Express now sits in front of it and blocks sensitive files (`.env`, `package.json`, `src/`, `prisma/`, `.claude/`, `memory/`). The `uploads/` directory is intentionally **public** (it's part of the static design package — currently holds `Proposal_Religio_Pro.docx`), so never drop secrets or unredacted user data there.

**Windows file-lock gotcha**: `prisma generate` will fail with `EPERM rename query_engine-windows.dll.node` if the dev server is running (`node --watch` holds the file open). Stop the dev process before running migrate/generate, then restart. Killing one npm/node PID isn't enough — the watch parent spawns a child worker; check `tasklist | grep node` and kill all three (npm wrapper, watcher, child) with `taskkill //F //PID …`.

## Backend architecture

- `src/env.js` — Zod-validated env config. Boot fails fast on bad values.
- `src/app.js` — Express factory. Order: JSON body → cookies → request log (dev) → `blockSensitive` middleware → API routes → static design files → 404 → error handler.
- `src/server.js` — Listens, prints banner, handles SIGINT/SIGTERM + Prisma disconnect.
- `src/lib/db.js` — Prisma client **singleton** (HMR-safe via `globalThis.__prisma`). Use `import { db } from '../lib/db.js'`.
- `src/middleware/error.js` — `HttpError` class + handler. JSON for `/api/*`; HTML 401 → 302 to `/login?next=…`; other HTML errors render `views/error.ejs`.
- `src/lib/format.js` — `res.locals.fmt` exposes `rpShort`, `rpFull`, `date`, `dateShort`, `dateLong` to every EJS template + `toNumber()` helper for Prisma Decimal → number.

### Route mounting order

Mount **nested admin sub-paths BEFORE** the generic `/admin` router, otherwise Express short-circuits on prefix match and the catch-all handler can swallow nested URLs. Same rule for `/api/paket/:slug/{hotels,days,rooms}` mounting before the public `paketJsonRouter`. **And: `paymentGatewayRouter` MUST mount BEFORE `paymentsRouter`** — the latter has `router.use(requireAuth, requireRole(...))` which intercepts any `/api/payments/*` prefix and 401s the Midtrans webhook before its signature verify ever runs (caught by HTTP integration tests). Current order in `src/app.js`:

```
/api/paket            → paketChildrenRouter (admin JSON CRUD)
/api/paket            → paketJsonRouter (public read)
/admin/paket          → paketAdminRouter
/admin/users          → usersAdminRouter
/admin/jemaah         → jemaahAdminRouter
/admin/bookings       → bookingsAdminRouter
/admin/payouts        → payoutsRouter
/admin/audit          → auditRouter
/admin/notifications  → notificationsRouter
/admin/payment-intents → paymentIntentsRouter   ← 5tt
/admin/incidents       → incidentsRouter         ← stage 13 (crew SOS queue)
/admin                → adminRouter (overview/manifest/bunking/finance)
```

### Sensitive-path block

`src/app.js` defines `SENSITIVE_FILES` + `SENSITIVE_PREFIXES`. Any new top-level file or directory containing secrets/code must be added there — `express.static` would otherwise expose it.

## Auth & RBAC

- **Token**: JWT (HS256, issuer `religio-pro`). Read order: `Authorization: Bearer …` → `rp_session` httpOnly cookie. Cookie defaults `sameSite=lax`, `secure` from env.
- **Helpers**: `src/lib/jwt.js`, `src/lib/auth.js`, `src/lib/audit.js` (append-only writer — **never updates or deletes**).
- **Middleware**: `requireAuth`, `requireRole(...roles)`, `optionalAuth` in `src/middleware/auth.js`. Use `requireAuth` *before* `requireRole(...)`.
- **Async routes**: wrap with `asyncHandler(fn)` from `src/lib/asyncHandler.js` (Express 4 doesn't catch rejected promises).
- **Rate limit**: `src/middleware/rateLimit.js` with a pluggable store (`src/lib/rateLimitStore.js`). When `REDIS_URL` env is set → Redis store (atomic `INCR + PEXPIRE` via `MULTI`, multi-instance safe). Unset → in-memory fixed-window bucket (single-instance only). **Fails open on store errors** — a Redis outage logs a warning but allows the request through, so a flaky cache never locks legitimate users out of login. Login: 10/min/IP, register: 5/min/IP. Store kind logged at first use (`[rateLimit] store = Redis ...`). Closed cleanly on SIGINT/SIGTERM via `stopRateLimit()`.
- **JSON API** (`/api/auth/*`): `POST /register` (public, creates JEMAAH only), `POST /login`, `POST /logout`, `GET /me`. Login uses generic "Email atau password salah" regardless of email existence.
- **HTML login** (`src/routes/authWeb.js`): `GET /login`, `POST /login` (form-encoded), `POST /logout`. After login, redirects by role:
  - `AGEN` → `/agen`
  - `JEMAAH` → `/saya`
  - `OWNER` / `SUPERADMIN` / `MANAJER_OPS` / `KASIR` → `/admin`
  - others → `/`
- **HTML register** (`src/routes/jemaahPortal.js`): `GET /register`, `POST /register` (form-encoded, JEMAAH role only, rate-limited 5/min). Creates `User` + linked `JemaahProfile` in one transaction, signs JWT, and redirects to `/saya?welcome=1`. The `/api/auth/register` JSON endpoint still exists alongside.
- **Anti-escalation**: SUPERADMIN cannot create/edit `OWNER` users (`guardEscalation` in `src/services/userAdmin.js`). OWNER role option is disabled in the user form for non-OWNERS.
- **Audit**: every LOGIN attempt (success and failure) and LOGOUT writes an `AuditLog` row with IP, user-agent, and `actorEmail` snapshot so deleted-user history survives.
- **CSRF protection**: `src/middleware/csrf.js` runs after cookie + body parsers, before any route. Double-submit cookie pattern — mints a 32-byte hex token in non-httpOnly cookie `rp_csrf`, requires it back on POST/PUT/PATCH/DELETE via either `X-CSRF-Token` header OR `_csrf` body field. Bypasses GET/HEAD/OPTIONS, the Midtrans webhook (signed by upstream), and `/api/health`. **Every form POST in `views/` MUST include `<input type="hidden" name="_csrf" value="<%= csrfToken %>">`** (exposed via `res.locals.csrfToken`). **Every page that uses `fetch()` for state-changing calls MUST load `<script src="/shared/csrf.js"></script>`** before the inline JS — that script monkey-patches `window.fetch` to auto-attach the header from the cookie. Missing either → 403 `CSRF_FAILED` (JSON for `/api/*`, plain text for HTML routes).

## Database

- Prisma 6.x, `mysql` provider (MariaDB-compatible).
- Migrations live in `prisma/migrations/`. Never edit applied migrations — use `prisma migrate dev` to create new ones.
- **AuditLog is append-only** — never write code that updates or deletes rows from it. Same for `Payment` history (refund = new row, not edit).
- Money: `Decimal(15, 2)` for IDR amounts; `Decimal(15, 6)` for exchange rates. Non-IDR amounts also store `amountIdrEq` snapshot.
- Identity: 4 profile tables (`AgentProfile`, `JemaahProfile`, `StaffProfile`, `CrewProfile`) one-to-one with `User`. The 8-role RBAC enum is on `User.role`.
- Agent URL slug: `AgentProfile.slug` (e.g. `ahmad-w`) drives the `?a=` query param on auto-generated paket pages. Booking captures both the FK (`agentId`) **and** a snapshot (`agentSlugCap`) so reattribution audit survives slug renames.
- **Lead model** (`Lead`) is pre-booking only (status `COLD`/`WARM`). When converted, status becomes `CONVERTED` and `convertedBookingId` points at the new booking. `LOST` is terminal.
- **Room model** (`Room`) belongs to a paket; capacity defaults from kelas (QUAD=4, TRIPLE=3, DOUBLE=2, VVIP=1) but is override-able. Composite unique on `(paketId, roomNo)`. `Booking.roomId` is the assignment FK (nullable; cancellation sets it to null automatically).
- **JemaahDocument** — 1-per-type per jemaah (`@@unique([jemaahId, type])`). 8 doc types (PASSPORT/VISA_UMROH/MANASIK_CERT/HEALTH_CERT/VACCINE_MENINGITIS/MARRIAGE_CERT/FAMILY_CARD/OTHER), 5 statuses (PENDING/SUBMITTED/VERIFIED/REJECTED/EXPIRED). `upsertDoc` auto-stamps `submittedAt`/`verifiedAt` on status transitions (only on transitions — re-saving the same status does NOT re-stamp). Optional file attachment (5mm): `filePath`/`fileName`/`fileSize`/`mimeType`/`fileUploadedAt`; file lives at `private/docs/<jemaahId>/<docId>__<sanitised-basename>.<ext>`. One file per doc — re-upload replaces the prior file on disk.
- **PaymentIntent** (5pp) — gateway-side state, separate from `Payment` so an intent can fail (EXPIRED/CANCELLED/FAILED) without ever producing a money movement. Lifecycle: `CREATED → PENDING → SETTLED` (happy path) or terminal failure. `paymentId` 1:1 FK fills in only on first SETTLED transition — the guard against double-credit (Midtrans retries webhooks on failure). `orderId` format `PI-<intentId>` is the join key with Midtrans.
- **PaketCrew** (5oo) — M2M between `Paket` and MUTHAWWIF users. Composite PK `(paketId, userId)` makes assign-twice naturally idempotent (upsert). Assignment to ARCHIVED / soft-deleted paket is permitted (admin may want to keep historical record) but `listAssignedPaket` filters them out of the crew dashboard.
- **AttendanceMark** (5ww) — one per `(bookingId, paketDayId)` (`@@unique`), recording who showed up to each itinerary day. `present` Boolean + optional `notes`; `markedByUserId/At` stamps which crew last touched it. Re-marking is upsert — DB rows never duplicate. **No audit log** for these — high-volume per-trip operation; the row itself is the trail. Cascade delete on Booking + PaketDay for cleanup safety.

### Seed credentials & demo data

`prisma/seed.js` is idempotent. Seeded users:
- `owner@religio.pro` / `owner12345` — OWNER
- `kasir@religio.pro` / `kasir12345` — KASIR
- `ahmad@religio.pro` / `ahmad12345` — AGEN, slug `ahmad-w`

Seeded content (all belonging to one paket, `ramadhan-aqsa-2026`):
- 4 hotels (Madinah/Mekkah/Aqsa/Petra), 4 prices (Quad→VVIP, Double featured), 7 itinerary days
- 12 rooms (M-401..408 on lt 4 wing Selatan/Utara; M-501..504 on lt 5 wing Selatan/Eksekutif)
- 5 demo bookings `RP-DEMO-0000{1..5}` for `ahmad-w` (mixed statuses including 2 LUNAS) with Payment rows synced to paidAmount via `gatewayRef=DEMO-PAY-…`
- 6 demo leads (3 COLD + 3 WARM, mixed sources, with `phone` prefix `0888-DEMO-…` for idempotency)

## Public paket landing + booking (step 4)

- `GET /p/:slug` renders `views/paket.ejs` from DB. `?a=<agentSlug>` resolves to an `AgentProfile`; unknown slugs render the page in "no-agent / Kantor Pusat" mode.
- `GET /api/paket/:slug` returns the same data as JSON.
- `POST /api/booking` — public, anonymous, 8/min/IP rate limit.
- **Agent lock-in invariant**: `Booking.agentId` is set only when the slug resolves; `Booking.agentSlugCap` *always* snapshots the slug from the URL — even if invalid. A future audit can prove which URL the visitor came from, even if the slug is later deleted or reassigned.
- **Booking number scheme**: `RP-YYYY-NNNNN`. Generated by counting existing prefix-matched rows; collisions retry up to 5 times (uniqueness enforced by `bookingNo @unique`).
- **`paket.ejs` hotel render quirk**: the landing template explicitly renders only Madinah + Mekkah hotels in the "hotels" section. Hotels in other cities (AQSA, PETRA, ISTANBUL, JAKARTA, etc.) are stored fine but won't appear on the public landing page without a template change.
- **Authenticated self-booking (5t)**: both `GET /p/:slug` and `POST /api/booking` use `optionalAuth`. When the visitor is a logged-in JEMAAH, the form pre-fills name+phone from their `JemaahProfile`, and `createBooking` reuses that profile (no new row spawned) + sets `Booking.jemaahUserId` directly. A "Booking untuk orang lain?" link sends `forceAnonymous=1` in the form body to opt out of the link and spawn a fresh profile for the third-party jemaah.
- **Admin walk-in booking (5w)**: `GET /admin/bookings/new` + `POST /admin/bookings` (4 admin roles incl. KASIR) wraps the same `createBooking` service with an `adminCreator = {id,email,role}` param. When set, the function **forces `loggedInUser = null`** (the admin session is the actor, not the jemaah) — `selfBooked` stays false and `jemaahUserId` stays null so the jemaah can still later register + claim the booking themselves via `/saya`. Audit row marks `after.adminCreated = true` with the admin's actor email/role.
- **Separation of duties for money flow**: roles are split deliberately. `KASIR` records incoming payments + creates walk-in bookings, but **cannot** issue refunds (5m) or disburse komisi payouts (5x) — those need OWNER/SUPERADMIN/MANAJER_OPS. Don't extend the KASIR role to outbound money without a deliberate decision; the current split mirrors how cash-handling controls work in physical offices.
- **Notification enqueues are fire-and-forget**: every `enqueueNotification` and event-helper call inside `booking.js` / `payment.js` / `payouts.js` is wrapped in try/catch. A failure to insert the notif row (DB hiccup, bad payload, missing template) logs to console but never aborts the transaction that triggered it. The notif row becomes the audit trail of "we tried to tell the jemaah" — even when the actual send fails, the queue + admin viewer surface the gap. **Do not** add notif writes inside the same `$transaction` as money-state writes — the right priority order is "save money state first, attempt notify second".
- **Komisi rate immutability (5u/5v/stage 14)**: when `Paket.komisiRate`, `AgentProfile.komisiRateOverride`, OR `AgentPaketKomisi.rate` changes, **existing `Komisi` rows are NOT recomputed**. The rate at the moment of the LUNAS transition is locked in via the `Komisi.amount` snapshot. This is the right behaviour — historical earnings reflect the contract that was in force when the agent earned them — but means a "global rate change" admin should bump expectations, not historical numbers. Form helper text spells this out for all three surfaces (paket-edit "Komisi agen (%)", user-edit "Override komisi (%)", and the per-agen matrix panel).
- **3-state Zod schema for nullable form fields** — when a form field needs to support "explicit clear" (empty input → `NULL` in DB) vs "leave unchanged" (field not in body), the preprocessor must distinguish three input states: `undefined` (key absent → no DB write), `null` (key present, value empty → clear to NULL), and a real value. Naive `(v) => v === '' ? undefined : Number(v)` collapses the first two, so empty input fails to clear. See `komisiOverridePct` in `src/services/userAdmin.js` for the pattern: `z.union([z.number()…, z.null()]).optional()` paired with a preprocessor that maps the three cases distinctly.

## Agen Portal (`/agen`)

`requireAuth + requireRole('AGEN')`. Single-page with 4 client-switched tabs (state preserved in `?tab=` query param):

- **Leads Pipeline** — 4-column kanban. `Cold` + `Warm` from `Lead` rows (`status IN ('COLD','WARM')`). `Hot/DP` from `Booking` rows (`status IN ('PENDING','BOOKED','DP_PAID','PARTIAL')`). `Lunas` from `Booking` rows with `status=LUNAS`. KPI strip on top. Inline lead create/edit/delete via fetch; **Promote to Booking** modal on WARM cards calls `POST /api/leads/:id/convert` which reuses the public `createBooking()` and sets `Lead.status=CONVERTED + convertedBookingId`.
- **Marketing kit** — list of ACTIVE paket; each card shows the auto-generated link `/p/<slug>?a=<agent.slug>` with copy-to-clipboard button.
- **Wallet & Komisi** — totals + transaction list (komisi grouped by status PENDING/EARNED/PAID/CANCELLED) + read-only payout history table (last 20 `KomisiPayout` rows for this agent, with payoutNo + method + reference for bank-statement cross-check).
- **Analitik** — funnel (5 stages from Cold to LUNAS) + lead source breakdown + 30-day sparkline. Date range filter (`from`/`to` query params, defaults to 30-day window).

All `/api/leads/*` endpoints are scoped to the logged-in agent's profile via `resolveAgent` middleware in `src/routes/leads.js` (ownership enforced by `loadOwnedLead`). Cross-agent access returns 403.

## Jemaah Portal (`/saya`)

`requireAuth + requireRole('JEMAAH')`. Anonymous public bookings each create a fresh `JemaahProfile`, so a returning jemaah usually has multiple profile records floating around. The portal stitches them back together via a **claim** flow.

- **`GET /register`** — public HTML form. Creates JEMAAH user + linked profile; auto-logs-in.
- **`GET /saya`** — dashboard. Shows claim form + list of bookings where `Booking.jemaahUserId = req.user.id`.
- **`POST /api/saya/claim`** — body `{bookingNo, phone}`. Matches `bookingNo` exact + `phone` after stripping spaces/dashes/parens. Generic 404 on any mismatch (deliberate — guards against bookingNo enumeration). 409 if already claimed by a different user. Idempotent if same user.
- **`GET /saya/bookings/:id`** — read-only booking detail (no admin tools). Ownership via `jemaahUserId` filter.
- **`GET /saya/profile`** — form for editing the jemaah's own canonical profile (the one linked via `JemaahProfile.userId`). Fields are the same set admin can edit on `/admin/jemaah/:id/edit`; validation reuses `JemaahSchema` from `src/services/jemaahAdmin.js`.
- **`POST /api/saya/profile`** — JSON update for the form above. Audit row uses `actor.role=JEMAAH` as the self-edit signal (no explicit `via` flag needed). Form includes a "Preferensi notifikasi" panel (5jj) with two channel toggles (`notifWa`, `notifEmail`); the route normalises unchecked HTML checkboxes to explicit `false` before calling `updateMyProfile` so opting out actually persists (an unchecked checkbox is absent from `req.body` — without the normaliser the `notifPref` preprocessor reads it as "no change").
- **`POST /api/saya/documents`** + **`DELETE /api/saya/documents/:id`** — jemaah self-submits or removes their own document tracking. Restrictions in the next section.
- **`GET /saya/paket`** (5dd) — browser for ACTIVE paket with a per-card `alreadyBooked` badge based on the jemaah's existing non-cancelled bookings. "BOOK SEKARANG" links to `/p/:slug` where 5t's pre-fill kicks in; the CTA label flips to "BOOK LAGI" when a prior booking exists. Single batched query for the user's bookings (no N+1).
- **`POST /api/saya/bookings/:id/request-cancel`** (5ff) — jemaah submits a cancellation request with a reason. Sets `Booking.cancelRequested = true` + `cancelRequestedAt` + `cancelRequestReason`, but **does NOT change `status`** — admin must still approve via `cancelBooking`. Refuses if booking is CANCELLED/REFUNDED or a prior request is already pending (409 `ALREADY_REQUESTED`). When admin runs `cancelBooking`, the three request fields are cleared in the same transaction (implicit approval — no separate "approve request" endpoint). Admin surfaces: amber banner at the top of `/admin/bookings/:id` info panel, plus a "REQ CANCEL" badge under the status pill in the manifest table.
- **`GET /saya/notifications`** (5ll) — read-only inbox of the jemaah's own notifications. Filtered strictly by `Notification.recipientUserId = req.user.id` (capped at 50, newest first). Channel badge + status pill (PENDING/SENT/FAILED/SKIPPED) + body preview + error message when not SENT. The page links back to `/saya/profile` so an opted-out jemaah seeing rows with `status=SKIPPED, error="recipient opted out of WA notifications"` has a clear path to flip the toggle. Admin/system notifs (e.g. `CANCEL_REQUESTED` fan-out to admin emails) deliberately enqueue **without** `recipientUserId`, so they never leak into any jemaah inbox. **Unread badge (5rr)**: dashboard topbar shows a gold count pill next to the "Notifikasi" link via `countUnreadForUser`. Opening the inbox calls `markAllReadForUser` *after* fetching the rows, so rendered unread rows get a gold left-border + dot affordance on first visit, then the badge clears for next render. `Notification.readAt` is null until first view; admin/system rows (no `recipientUserId`) are never counted.

### Self-submit document rules (5s)

The jemaah-side `submitMyDoc` in `src/services/jemaahPortal.js` enforces tighter rules than the admin's `upsertDoc`:

- **Status is inferred from input, never set directly.** `refNumber` filled → `SUBMITTED`; empty → `PENDING`. Jemaah can never set `VERIFIED` or `REJECTED` — those are staff verdicts.
- **Re-submitting a `VERIFIED` doc resets the verdict**: status drops to `SUBMITTED`, `verifiedAt`/`verifiedById` are cleared. This handles the re-review workflow (e.g. paspor renewed, new ref number).
- **`deleteMyDoc` refuses to delete `VERIFIED` docs** (409 `DOC_LOCKED`) — staff sign-off is a soft lock; only admin can remove a verified record.
- Auth signal in audit: `actorRole=JEMAAH` is sufficient — no `selfSubmit: true` flag needed (though the implementation does add it to `after` for explicit grep).

### Document file uploads (5mm)

Files attach to an existing `JemaahDocument` row (1 file per doc, re-upload replaces). Flow:

- **Storage**: `private/docs/<jemaahId>/<docId>__<sanitised-basename>.<ext>`. The `private/` dir is in `SENSITIVE_PREFIXES` (`src/app.js`) — `express.static` returns 404 for any direct request, files are only reachable via authenticated download routes. **Any new private dir must be added to that prefix list** before being used for uploads, otherwise it leaks.
- **Multer two-step**: `src/middleware/docUpload.js` writes incoming files to OS temp via multer disk storage; the route handler then calls `moveUploadedFile()` in `src/lib/docStorage.js` to relocate them into the per-jemaah dir. This split keeps multer agnostic of `jemaahId/docId` (only known after auth + param resolution).
- **Limits**: 8 MB cap (`MAX_DOC_BYTES`), mime allowlist `PDF / JPG / PNG / WEBP / HEIC` (`ALLOWED_MIME`). Both are enforced in multer's `fileFilter`/`limits` AND re-checked in the service before move — defence in depth.
- **Filename rule**: `sanitiseBasename()` strips path separators, diacritics, and non-portable chars; caps to 100 chars; falls back to `"file"` on empty. Applied to both the on-disk filename AND the `fileName` column used in `Content-Disposition`. **Never trust the user's raw filename for either path or download header.**
- **Status side-effects on upload**: PENDING → SUBMITTED (file presence = "I've submitted it"); VERIFIED → SUBMITTED + `verifiedAt`/`verifiedById` cleared (mirrors `submitMyDoc` re-review behaviour). Other statuses pass through unchanged.
- **Delete coverage**: `deleteMyDocFile` removes the file but keeps the row; `deleteMyDoc` (jemaah) + admin `deleteDoc` both delete the file on disk too (best-effort) so deleting a doc row never leaves an orphan blob. VERIFIED docs refuse both kinds of delete from the jemaah side (`DOC_LOCKED` 409) — only admin can remove.
- **Download routes** (auth + ownership enforced per-request, NOT via static):
  - `GET /saya/documents/:docId/file` — jemaah; ownership = `doc.jemaah.userId === req.user.id`
  - `GET /admin/jemaah/:jemaahId/documents/:docId/file` — admin RBAC + **tuple guard**: rejects with 404 unless `doc.jemaahId === :jemaahId`. The tuple guard prevents enumerating files across jemaah by guessing docIds on a fixed jemaah URL.
- Both responses are `Content-Disposition: inline` with the stored `fileName` — browsers preview PDFs/images directly; users get a sensible "Save as" name.
- **Inline thumbnails (5vv)** — the doc panels (`/saya/profile` + `/admin/jemaah/:id/edit`) render a 56-64px `<img>` thumbnail when `mimeType ∈ {image/jpeg, image/png, image/webp}` (via `isInlineImageMime()` in `src/lib/docStorage.js`); PDF gets `📄`, anything else (HEIC/HEIF) gets `🖼️`. HEIC is deliberately fallback even though it's in the upload allowlist — Chrome/Firefox don't ship HEIC decoders, so an `<img>` tag would render a broken-image square.
- **Server-side cached thumbnails (stage 15)** — uploads now auto-generate a 256-px JPEG thumb at `private/docs/<jemaahId>/thumbs/<docId>.jpg` via `sharp` (`src/lib/docThumbnail.js`). Thumb download routes `/saya/documents/:docId/thumb` + `/admin/jemaah/:jemaahId/documents/:docId/thumb` serve the cache when it exists, **fall back to the full file** when it doesn't (so legacy uploads + non-image mimes keep working without a flag day). Generation is best-effort — sharp throws on a corrupt source don't abort the upload, just log a warning. Thumb is deleted alongside the source on `deleteMyDocFile` / `deleteMyDoc` / admin `deleteDoc` so the thumbs dir doesn't leak. `src` in the views uses `/thumb` for `<img>` (cheap) but `/file` for the click-through `<a href>` + "Lihat" button (full quality). EXIF rotation is honoured (`sharp.rotate()`) so portrait phone photos aren't sideways. Backfill existing uploads with `node scripts/backfill-thumbnails.js` (idempotent — only generates when no cached thumb exists; `--force` to regenerate, `--dry-run` to preview).

### Profile soft-merge on claim (5p.2)

On every successful claim, `claimBooking` runs a soft-merge inside one transaction when the user has their own profile AND the booking points elsewhere. **Order is load-bearing** — re-point booking FIRST so the source's remaining-bookings count is accurate, then merge with knowledge of whether source will be deleted:

1. Re-point `Booking.jemaahId` from the anonymous booking-profile → the user's canonical profile.
2. Count source's remaining bookings (zero ⇒ source becomes an orphan and will be deleted).
3. Run `mergeProfileInto(target, source, { sourceWillBeDeleted })`:
   - **User profile wins** on conflict: copy `nik / passportNo / passportExpiry / birthDate / gender / address / emergencyContact / notes` from source only when the user's field is null/empty.
   - **Defensive @unique check** for `nik` and `passportNo` — excludes both target AND source from the clash query; only a *third* profile counts as a real conflict.
   - **@unique transfer requires source orphan**: when source still has other bookings, we can't take its NIK/passport away (those other booking-profiles would lose the data) — skip the copy, source keeps the value. When source is about to be deleted, NULL its @unique field first (frees the DB constraint), then patch the value onto target.
4. Transfer `JemaahDocument` rows from source → user. **User wins** on `(jemaahId, type)` collision (source duplicate deleted; source unique re-pointed).
5. Delete the source profile if it has zero remaining bookings.
6. Audit `Booking UPDATE` includes `{merged, targetJemaahId, fieldsCopied, docsTransferred, oldProfileDeleted}` summary.

This means a jemaah who has booked 3 paket anonymously, then registers and claims all 3, ends up with a single canonical profile + their docs consolidated — even though the public booking flow keeps creating new profile rows.

After 5t, the merge path mostly handles **legacy** anonymous bookings made before the user registered. New bookings made while logged in already point at the canonical profile (and have `jemaahUserId` set), so the merge becomes a no-op for them.

## Crew Portal (`/crew`)

`requireAuth + requireRole('MUTHAWWIF')`. Read-only workspace for guides/leaders travelling with the jemaah. Login redirect: MUTHAWWIF → `/crew` (added to `redirectForRole` in `src/routes/authWeb.js` alongside AGEN/JEMAAH).

- **`GET /crew`** — dashboard. Lists paket where this user has a `PaketCrew` row, scoped to non-ARCHIVED + non-soft-deleted, sorted by `departureDate` ascending (next trip on top). Each card: title, slug, departureDate, durationDays, booking count, kursi terisi vs kursi total, status badge.
- **`GET /crew/paket/:slug`** — manifest. Returns 404 (generic `NOT_ASSIGNED`) when the crew isn't assigned — anti-enumeration. Includes per-jemaah: `bookingNo`, `kelas`, `paxCount`, `status`, room assignment, jemaah `fullName`/`phone`/`emergencyContact`/`passportNo`/`passportExpiry`, plus `docPills` (5-type curated strip via `pillsForJemaah`). Filters out CANCELLED/REFUNDED bookings — manifest is "who's actually going on this trip".
- **`GET /crew/paket/:slug/export.csv`** (5ss) — offline-friendly CSV snapshot of the same manifest. Built for crew on the road with weak/no signal — open in any spreadsheet app on a phone or laptop. UTF-8 BOM + RFC 4180 quoting (same convention as the admin 5gg export). Filename `crew_manifest_<slug>_<YYYY-MM-DD>.csv`. Same money-stripped column set as the HTML manifest — booking identity, room, and per-curated-doc-type state, **no totalAmount/paidAmount**. Refuses (404 `NOT_ASSIGNED`) when the requesting crew isn't on the paket — the assignment guard is the same as the HTML view.
- **`GET /crew/paket/:slug/attendance`** (5ww) — per-day attendance overview. Lists every `PaketDay` (in itinerary order) with `presentCount` / `markedCount` / `totalActive` so crew sees at a glance which days are unmarked. Active bookings only (CANCELLED/REFUNDED excluded) — matches the manifest definition of "who's actually going".
- **`GET /crew/paket/:slug/attendance/:dayId`** (5ww) — per-day grid: jemaah list + a custom checkbox toggle + free-text notes input per row, POST to `/crew/paket/:slug/attendance/:dayId/:bookingId`. The service `setAttendanceMark` enforces a **tuple guard**: `(dayId, bookingId)` MUST both belong to the assigned paket — cross-paket combinations return 404 (anti-enumeration; same generic 404 as the unassigned case). Each save shows a green confirmation banner, then strips `?ok=saved` from the URL so reloads don't keep flashing it. Admins can audit the resulting marks at `/admin/paket/:slug/attendance` (5zz — see Admin Dashboard sub-pages).
- **Write surface from `/crew`** is deliberately small: attendance marks (5ww) + SOS / incident reports (stage 13). No jemaah / payment / room assignment edits from the portal. Chat from the static `screens/crew-app.html` mockup is a deliberate follow-up.
- **SOS / incidents** — floating ruby pulse button (`partials/sos-fab.ejs`) on every `/crew/*` page opens a modal with type (SOS / MEDICAL / LOST_JEMAAH / SECURITY / LOGISTICAL / OTHER) + optional location + message. POSTs to `/crew/sos` (form-encoded so the no-JS fallback works). `createIncident` in `src/services/incidents.js` enforces MUTHAWWIF role, resolves `paketSlug` to a paketId **only if the crew is actually assigned** (unknown/unassigned slug silently null — the SOS landing is more important than tagging it), and fires a fan-out (`notifyIncidentCreated`) to every ACTIVE OWNER/SUPERADMIN/MANAJER_OPS via **three channels**: EMAIL (queued, retry-with-backoff), WA (queued, same), and Web Push (real-time, stage 17). Subject prefix `[CRITICAL]` for SOS, `[URGENT]` otherwise. Crew dashboard `/crew` shows the last 10 of their own incidents with current admin status (OPEN / ACKED / RESOLVED) so they can see whether tim sudah respon.
- **Incident state machine** is one-way: `OPEN → ACKED → RESOLVED`. No back-transitions and no re-open — follow-ups create a new incident so the timeline stays honest. Admin queue at `/admin/incidents` (OWNER/SUPERADMIN/MANAJER_OPS) sorts `[status asc, createdAt desc]` so OPEN bubbles to top. Resolve requires a `resolution` note (min 3 chars, max 2000). Jumping `OPEN → RESOLVED` auto-stamps `ackedAt`/`ackedById` to the resolving admin — the timeline always records both verbs.
- **Notif fan-out invariants for incidents**: `recipientUserId` omitted (admin-targeted; never bleeds into a jemaah inbox per 5ll). Failure to enqueue is logged but does NOT abort the incident write — the SOS row landing is more important than the notification firing.
- **Web Push (stage 17)** — the third fan-out channel. `pushToAdmins` in `src/services/webPush.js` looks up every `PushSubscription` owned by an ACTIVE admin and calls `web-push.sendNotification` per row. Per-device subscriptions are managed from a toggle bar on `/admin/incidents` (`shared/push-admin.js` handles permission → fetch `/api/admin/push/config` for VAPID public key → `PushManager.subscribe` → POST to `/api/admin/push/subscribe`). The SW (`shared/sw.js`) `push` handler shows a Notification; SOS payloads set `requireInteraction:true` so the toast doesn't auto-dismiss. `notificationclick` focuses an existing tab on the target URL or opens a new one. **Self-cleanup**: 404/410 from web-push means the endpoint is dead — the dispatcher scrubs the row so the table stays bounded. **Fake-mode**: when `VAPID_PUBLIC` is absent, `sendOne` logs to stdout instead of calling web-push — subscribe/unsubscribe DB ops still work, so the full surface is testable without VAPID creds. Boot log `[push] sender = web-push` (real) vs `[push] sender = console (VAPID_PUBLIC absent ...)` (fake). To enable in prod: `npx web-push generate-vapid-keys` → drop `VAPID_PUBLIC` + `VAPID_PRIVATE` + `VAPID_CONTACT` into `.env` → restart. **CACHE_VERSION bump in sw.js is required** when push handler logic changes so installed clients pick it up on next SW activation.
- **Money-stripped on purpose**: `getAssignedManifest` does NOT select `totalAmount` / `paidAmount` / `payments`. Crew often help with logistics but cash collection is a separate role (KASIR) — keeping balances out of the crew UI mirrors the existing separation of duties around outbound money (5x: KASIR can't disburse payouts).

Admin manages assignment from `/admin/paket/:slug/edit`: a "Crew (muthawwif)" panel lists currently-assigned users + a dropdown of every ACTIVE MUTHAWWIF for one-click assign. Wire-up via JSON: `POST /api/paket/:slug/crew` (`{ userId }`) and `DELETE /api/paket/:slug/crew/:userId` — both in `src/routes/paketChildren.js` so they pick up the existing OWNER/SUPERADMIN/MANAJER_OPS gate.

## Mobile experience (stage 13)

`/saya` (jemaah) and `/crew` (muthawwif) are PWA-installable. The shell is a thin layer over the existing EJS views — no separate native build — so any backend change is immediately reflected on the installed app.

- **`shared/manifest.webmanifest`** — `start_url=/saya`, `display=standalone`, gold-on-onyx theme, 3 shortcuts (Booking / Profil / Notifikasi). Linked from every mobile-relevant view via `views/partials/pwa-head.ejs` (also drops apple-touch-icon + theme-color meta).
- **`shared/sw.js`** — minimal service worker. Static assets (`/shared/*`, `/uploads/*`, common file extensions) are cache-first with background refresh; same-origin HTML pages are network-first with cache fallback, then `/shared/offline.html` as last resort. **Deliberately skips `/api/*`** — those carry per-user CSRF cookies and stale cached responses would mislead a balance/queue screen. Bump `CACHE_VERSION` to invalidate every entry on next activation.
- **`shared/pwa.js`** — registers the SW + captures `beforeinstallprompt`. Exposes a tiny click handler so any view can wire a `<button data-pwa-install>` to fire the prompt from a user gesture. iOS Safari doesn't fire that event, so the same file injects an **iOS install hint** card (`#rp-ios-hint`) 1.2 s after load: bottom-fixed dismissable banner with Share-icon SVG + "Tap Share → Add to Home Screen" instruction. Suppression: not iOS, in-app browser (FB/IG/Line/CriOS/FxiOS), already running standalone, or previously dismissed (localStorage `rp_ios_install_hint_dismissed`). `body.has-m-tabs` adjusts bottom offset so the hint sits above the bottom-nav instead of overlapping it. All styling injected inline — no view edits needed; no separate stylesheet. Bumping `CACHE_VERSION` in `sw.js` is required when this script changes so installed clients pick up the new logic on next activation.
- **Mobile bottom-nav** for jemaah — `views/partials/mobile-tabs.ejs` (Beranda / Paket / Notifikasi / Profil) shows only at ≤ 720 px. JS-side active-tab via longest-prefix match (so `/saya/bookings/:id` highlights Beranda). Tokens.css scopes the styles + body class `.has-m-tabs` reserves bottom padding so content isn't covered. The 5 jemaah views carry that class; redundant top-bar nav links hide on mobile (`.topnav-link` `display: none`) since the bottom tab bar covers them.
- **Responsive baseline** lives in `shared/tokens.css` under the `@media (max-width: 720px)` block: `h1/h2/h3` clamp, `.wrap` padding shrink, `table { display: block; overflow-x: auto; }` fallback, `.grid-2 / .form-grid { 1fr }`, `.topbar { flex-wrap }`. Per-view `@media` blocks tighten further (tap target ≥ 44 px, card-per-row layouts for dense tables like crew-attendance-day).
- **Sensitive-path block applies to PWA assets too**: don't drop secrets in `shared/` (it's public + cached aggressively by the SW). Per-user data must come via authenticated routes.

### Offline-friendly attendance (5xx field-use)

Muthawwif mark attendance from a phone in a bus or on a mountain pass where signal drops out. A failed POST mustn't lose the mark — `shared/attendance-queue.js` is an IndexedDB-backed queue (`religio-attendance.queue` store) that captures form submits, drains on `online` event + 20 s tick, and updates per-row sync pills (queued / syncing / ok / error) from the queue state.

- **Server endpoint is unchanged**. The `setAttendanceMark` upsert keyed on `(dayId, bookingId)` is already idempotent — replaying the same payload N times leaves the DB in the same state as sending it once. That's the contract the queue depends on; the smoke `scripts/smoke-offline-attendance.js` explicitly verifies "replay × 3 → 1 row".
- **drain() is single-flight** — concurrent calls short-circuit. Status transitions: `pending → syncing → done` (deleted from queue) or `failed` (incremented `attemptCount`, `lastError` set). The DOM updates from the queue's `onChange` notifier — banner state + per-row sync pill paint from the truth, not from optimistic JS state.
- **No-JS fallback path intact** — the same form posts traditionally, the server returns 302 `?ok=saved`, and the visible green banner fires from a URL query check. With JS, the form is intercepted and the redirect-following fetch path takes over.
- **`navigator.onLine === false` short-circuits the fetch** — no point hitting the network when the browser already knows. Optimistic UI (label "Hadir / Tidak hadir") still flips immediately so the crew sees their input acknowledged.

## Admin Dashboard (`/admin`)

`requireAuth + requireRole('OWNER','SUPERADMIN','MANAJER_OPS')` (KASIR can view some pages, see per-route guards). Tabbed single page (`?tab=`):

- **Overview** — KPIs (revenue LUNAS, potensi Hot, jemaah, komisi earned), status breakdown, top paket, performa agen (with lead conversion %), lead source effectiveness, global funnel, per-paket revenue trend with inline SVG sparklines (5ee — single-query group-by-day, missing days zero-filled so X-axis stays consistent). Funnel, source breakdown, and revenue trend honor the date range filter (`from`/`to`); KPI strip and top-paket panel are always all-time.
- **Paket** — grid of paket cards (`status≠ARCHIVED`) with badges (ACTIVE emerald / DRAFT amber / CLOSED ruby). Links to landing + manifest + edit.
- **Manifest** — per-paket booking table with doc-completion pills (`docPills` computed via `pillsForJemaah(documents)`). Pay button per row opens modal → `POST /api/payments`. Booking number links to detail page. **CSV export** button (5gg) downloads `manifest_<slug>_<YYYY-MM-DD>.csv` via `GET /admin/manifest/:slug/export.csv` — 20 cols (bookingNo, status, kelas/pax, jemaah identity, money, agen, timestamps, doc counts), UTF-8 with BOM so Excel detects encoding correctly, fields wrapped/escaped per RFC 4180. **Print button** (stage 19) opens `/admin/manifest/:slug/print` — A4 landscape worksheet styled for paper (white sheet, room-first row sort grouping kamar together, sign-row at bottom). Service `getPrintManifest` filters CANCELLED/REFUNDED out + attaches doc pills + reports `unassignedRoomCount` for the header warning.
- **Bunking** — per-paket room grid grouped by floor+wing + sidebar of UNASSIGNED bookings. Assign modal filters rooms to matching kelas + slots-left. Capacity check enforced server-side (`assignBookingToRoom` in `src/services/bunking.js`).
- **Finance** — cash by currency (IDR/USD/SAR), receivables, payment ledger 20-row.

### Admin sub-pages (separate URLs, share sidebar via duplicated layout)

- `/admin/paket/{new,:slug/edit}` — full CRUD form. Hotels + Days + Rooms managed as interactive panels via JSON endpoints `/api/paket/:slug/{hotels,days,rooms}` (POST/PATCH/DELETE).
- `/admin/paket/:slug/attendance` (5zz) — read-only attendance audit for crew's 5ww marks. Two tables: per-day (marked/present/rate) and per-jemaah (daysPresent/daysMarked/rate). **Rate formula deliberately divides by `totalDays`, not `daysMarked`** — unmarked days count as not-present, so crew can't boost rate by skipping the form. Cross-linked from the paket-form crumb. OWNER/SUPERADMIN/MANAJER_OPS (mounted under `paketAdminRouter`, so it inherits that gate).
- `/admin/users` + `/:id/edit` — list with filter (search/role/status), create+edit form with role-conditional profile section (Agent/Staff/Crew/Jemaah), reset-password panel, suspend/reactivate buttons. OWNER+SUPERADMIN only.
- `/admin/jemaah` + `/:id/edit` — list with passport-expiry badges (ok/warning <180d/urgent <90d/expired), search + `?expiringSoon=1` filter. Edit form: identity + passport + address + 8-row document tracking panel (inline fetch save/delete). Each doc with a `filePath` shows a "Lihat" download link to `GET /admin/jemaah/:id/documents/:docId/file` (5mm). OWNER+SUPERADMIN+MANAJER_OPS.
- `/admin/audit` + `/:id` — paginated 50/page list with entity/action/actorEmail/date-range filters. Detail shows before/after JSON side-by-side. OWNER+SUPERADMIN only. **Read-only — never expose writes here.**
- `/admin/notifications` — queue viewer with filter (status/channel/type). Per-row "SEND NOW" button for one-off dispatch + a "Proses Queue Sekarang" header button that POSTs to the HTTP trigger. OWNER/SUPERADMIN/MANAJER_OPS.
- `/admin/payment-intents` (5tt) — global cross-booking PaymentIntent list, paginated 50/page. KPI strip + filters (status/search/date range). Search matches order ID OR booking number (cross-table OR). Read-only — per-intent cancel still lives on the booking detail page (5qq) for context. OWNER/SUPERADMIN/MANAJER_OPS.
- `/admin/incidents` (stage 13) — crew SOS queue, paginated 50/page. KPI strip (OPEN / ACKED / RESOLVED counts), status + type filters, OPEN rows highlighted with ruby left-edge. Detail page `/admin/incidents/:id` shows timeline (created → ack → resolved dots) + resolve form (resolution min 3 chars). Ack + resolve POSTs are guarded with the same RBAC. Sidebar link "Insiden lapangan" in the admin rail.
- `/admin/bookings` — **global booking search**, paginated 50/page (4 admin roles incl. KASIR). 7-col KPI strip per BookingStatus, filter bar (q + status + paket + agent + date range). `q` matches `bookingNo` substring OR `jemaah.fullName` contains OR `jemaah.phone` contains; when `q` carries formatting (dashes/spaces) and the digits-only form differs, that's added as an OR clause too — so "0822-3399" finds phones stored without dashes and vice versa. `agentId=NONE` selects walk-in / Kantor Pusat bookings only. **`counts.byStatus` computed WITHOUT the status filter** (mirrors 5tt — KPI strip shows the full distribution within the current text+date scope, so flipping the status dropdown doesn't make the numbers misleading). Rows clickable into `/admin/bookings/:id`. Service: `searchBookings` in `src/services/bookingsSearch.js`. Sidebar link "Cari booking" in Operasional. Mounted BEFORE `/:id` in the bookings router so the empty path doesn't get interpreted as `bookingId=''`.
- `/admin/bookings/:id` — detail page (view: 4 roles, cancel/refund/notes-edit/agent-transfer/intent-cancel: 3 roles, KASIR view-only). Header status badge, info grid (with inline-editable notes textarea), money row with progress bar, payment table (negative refund rows in ruby with `−` prefix), payment-intent panel (5qq — online gateway attempts + per-row stuck-intent cancel for CREATED/PENDING), komisi table, audit timeline (filtered to this booking), cancel + refund + transfer-agen modals.
- `/admin/bookings/new` — walk-in booking creation form (4 admin roles incl. KASIR). Paket + optional agent dropdowns, jemaah name/phone, kelas/pax/notes. Reuses the public `createBooking` service via `adminCreator` param (see "Admin walk-in booking" invariant below). Linked from the sidebar "Operasional → + Booking baru".
- `/admin/payouts` + `/new` + `/:id` — komisi disbursement workspace (OWNER/SUPERADMIN/MANAJER_OPS — **KASIR explicitly excluded** because the kasir who records inbound payments shouldn't also approve outbound payouts; classic separation of duties). List shows outstanding `EARNED` per agent + history of past payouts. Detail page shows the bundled komisi rows with links back to each booking.

### Payment + status machine

`src/services/payment.js` exports `recordPayment()` (transactional) and `transitionStatus()` (forward-only state machine):

- `paid <= 0` → keep status
- `paid >= total` → `LUNAS`
- PENDING/BOOKED + partial → `DP_PAID`
- DP_PAID + partial → `PARTIAL`
- Already PARTIAL/LUNAS/CANCELLED → unchanged

On LUNAS transition, if `booking.agentId` is set, idempotently creates a `Komisi` row (`amount=rate × total`, `status=EARNED`, `earnedAt=now`). The rate is resolved by an **explicit precedence chain** in `payment.js` (most → least specific):

1. `AgentPaketKomisi(agentId, paketId).rate` (Decimal(5,4)) — stage 14 join table; admin sets per-paket × per-agen via `/admin/paket/:slug/edit` "Komisi override per agen" panel. JSON wire `PUT /api/paket/:slug/komisi-overrides` + `DELETE /api/paket/:slug/komisi-overrides/:agentId` in `src/routes/paketChildren.js`; service in `src/services/agentPaketKomisi.js`.
2. `AgentProfile.komisiRateOverride` (Decimal(5,4)?) — set per-agent via admin user form's "Override komisi (%)"
3. `Paket.komisiRate` (Decimal(5,4), default `0.06`) — set per-paket via paket edit form's "Komisi agen (%)"
4. `DEFAULT_KOMISI_RATE = 0.06` in `payment.js` — defensive fallback only; schema defaults keep real rows populated.

The matrix row is fetched with one `findUnique` keyed on `(agentId, paketId)` only when a komisi is about to be created — no extra DB hit on payments that don't transition to LUNAS, no join in the booking-load query.

### Cancel booking

`src/services/bookingAdmin.js → cancelBooking()` is transactional:
- `status=CANCELLED`, `cancelledAt`, `cancelReason` set
- `Paket.kursiTerisi.decrement(paxCount)` — seats released back to quota
- `Booking.roomId = null` — auto-unassign from room
- `Komisi.updateMany({EARNED → CANCELLED})` — PAID komisi stays (already disbursed)
- Payment rows **NOT touched** — refund is a separate flow (see below)

### Refund (`src/services/refund.js`)

Workflow is strict: **cancel first, refund after**. `issueRefund()` only runs on `status=CANCELLED` bookings (returns 409 otherwise). Behaviour:
- Creates a **new** Payment row with negative amount and `status=REFUNDED` (Payment is append-only — never mutates the original PAID rows).
- Decrements `Booking.paidAmount`. When it reaches 0, transitions `Booking.status` to `REFUNDED` (terminal — no further refunds possible).
- Partial refunds are repeatable: call multiple times until `paidAmount = 0`.
- Refund amount cannot exceed current `paidAmount` (409 with formatted message).
- Komisi is NOT re-touched here (cancel already handled `EARNED → CANCELLED`; `PAID` stays as-is).
- `KASIR` cannot issue refunds (`OWNER/SUPERADMIN/MANAJER_OPS` only) — more restrictive than payment recording.
- `getFinanceSummary` sums `PAID + REFUNDED` together so cash-by-currency reflects net position (refunds reduce the bucket).

### Payment gateway — Midtrans Snap (5pp)

Online payment via Midtrans Snap (hosted checkout). `src/lib/midtrans.js` is the HTTP client + signature verifier; `src/services/paymentGateway.js` is the lifecycle (createPaymentIntent → handleMidtransNotification); `src/routes/paymentGateway.js` exposes the public surface.

- **Flow**: jemaah clicks "Bayar online" on `/saya/bookings/:id` → `POST /api/payments/intent` creates a `PaymentIntent` + asks Midtrans for a Snap token → jemaah redirected to Snap hosted page → on completion, Midtrans calls `POST /api/payments/midtrans/webhook` → handler verifies signature → on `settlement`, materialises a Payment row via `recordPayment`.
- **Webhook delegates to `recordPayment`** — the gateway handler NEVER touches `Booking.paidAmount`/`status` or `Komisi` directly. It builds the right args and defers to `recordPayment` (in `src/services/payment.js`), which is the single source of truth for money math + status transitions + komisi creation. This keeps gateway-paid and admin-recorded payments converging on identical post-state. After `recordPayment` succeeds, the handler also fans out an admin email via `notifyPaymentSettledAdmin` (5yy) — non-blocking, never aborts the webhook ack to Midtrans.
- **Idempotency**: Midtrans retries webhooks on non-200. The handler guards on `intent.paymentId != null` before calling `recordPayment` — duplicate `settlement` payloads return `NOOP` without double-crediting. Composite invariant: `(intent.status === 'SETTLED' && intent.paymentId != null) ⇒ Payment already exists`.
- **Signature verification** (`verifyMidtransSignature`): `SHA512(order_id + status_code + gross_amount + server_key)`, compared with `crypto.timingSafeEqual`. Rejects malformed payloads early. Route returns 401 `BAD_SIGNATURE` without revealing whether the intent exists.
- **Status frozen at terminal**: once `intent.status ∈ {SETTLED, EXPIRED, CANCELLED, FAILED}`, later webhooks only snapshot `gatewayPayload`/`gatewayStatus` for audit — they don't re-transition status. Prevents a stale `cancel` event from reverting a successful settlement.
- **Active-intent guard**: `createPaymentIntent` refuses if there's already a `CREATED`/`PENDING` intent for the booking, unless caller passes `replaceActive=true` (which marks the old one CANCELLED first). The UI button passes `replaceActive=true` so re-clicking "Bayar online" doesn't pile up dangling intents. The 5uu sweep job auto-EXPIRES intents whose `expiresAt` has passed without admin intervention, so abandoned Snap sessions naturally self-clear without needing manual cancel (5qq) for every dead session.
- **Status → enum map** (`mapMidtransStatus`): `settlement` / `capture+accept` → SETTLED; `pending` → PENDING; `deny`/`failure` → FAILED; `cancel` → CANCELLED; `expire` → EXPIRED. `payment_type → PaymentMethod` map (`mapMidtransMethod`): `credit_card → CARD`, `bank_transfer/echannel → VA`, `qris → QRIS`, `gopay/shopeepay/dana/linkaja → EWALLET`, anything unrecognised → TRANSFER (with raw type captured in `Payment.notes`).
- **Fake mode** (default when `MIDTRANS_SERVER_KEY` is absent — i.e. local dev): `createSnapTransaction` returns `{ token: "fake-snap-<orderId>", redirect_url: "/payments/midtrans/fake?order_id=..." }`. The local route `GET /payments/midtrans/fake` builds a valid (signature-correct) webhook payload and invokes the handler in-process, then redirects back to `/saya/bookings/:id?paid=ok`. Lets the full intent → webhook → Payment loop run in smoke without external creds. Refuses to operate in real mode (returns 403 `NOT_FAKE_MODE`) so the local handler can't be exploited in production.
- **Production wiring**: set `MIDTRANS_SERVER_KEY` + `MIDTRANS_CLIENT_KEY` + `MIDTRANS_PRODUCTION=true` in `.env`, register `https://<your-domain>/api/payments/midtrans/webhook` in Midtrans dashboard. The Snap client switches between `app.sandbox.midtrans.com` (default) and `app.midtrans.com` (production) based on `MIDTRANS_PRODUCTION`.
- **Admin viewer + stuck-intent cancel (5qq)** — `/admin/bookings/:id` shows a "Payment Intents" panel listing every intent ever created for the booking (newest first), with order ID, status pill, gateway raw status, amount, and a cross-reference to the resulting Payment when SETTLED. CREATED/PENDING rows render a "CANCEL" button → `POST /admin/bookings/:id/intents/:intentId/cancel` → `cancelStuckIntent` flips status to CANCELLED. Use when a Snap session is dead but Midtrans never sent a webhook (e.g. jemaah closed the tab) — without this the active-intent guard would block them from starting fresh. **Refuses on terminal status** (`SETTLED/EXPIRED/CANCELLED/FAILED` → 409 `INTENT_NOT_CANCELLABLE`); to reverse a settled payment, use the refund flow instead. Same RBAC as cancel/refund (OWNER/SUPERADMIN/MANAJER_OPS — KASIR view-only).
- **Global viewer (5tt)** — `/admin/payment-intents` is a paginated cross-booking list (50/page) for ops investigation ("why didn't this user's payment go through?"). KPI strip on top shows counts per status; filters: status, text search (matches orderId OR bookingNo substring via Prisma relation OR), date range on `createdAt`. **`countsByStatus` is computed WITHOUT the status filter** — KPIs always reflect the full distribution within the current search+date scope, so flipping the status dropdown doesn't make the KPI numbers misleading. Sidebar link in the admin dashboard rail. Same 3-role gate.
- **Jemaah-side live polling (5xx)** — `/saya/bookings/:id` injects the latest non-terminal intent via `getActiveIntentForJemaahBooking` (scoped to `booking.jemaahUserId`). When present, the view renders a gold-bordered status card above the "Bayar online" panel — order ID, amount, status, gateway raw status, "Lanjut bayar" link to `snapRedirectUrl`. Client-side JS polls `GET /api/payments/intent/:id` every 15 s (max 20 ticks ≈ 5 min) via `setTimeout` chain (not `setInterval`, so slow responses can't overlap). On `SETTLED` the card turns green + auto-reloads the page so the Payment table picks up the new row. Terminal `EXPIRED/CANCELLED/FAILED` flips card red and stops polling. Network blips silently retry on the next tick. Polling NEVER returns a terminal intent — `getActiveIntentForJemaahBooking` filters them out, so the card naturally disappears on reload after settlement.

### Komisi disbursement (5x)

`src/services/payouts.js → createPayout({agentId, method, reference, notes})` bundles all of one agent's `EARNED` komisi into a single `KomisiPayout` row and flips them to `PAID` atomically:

- **Payout number scheme**: `PO-YYYY-NNNNN` — mirrors `bookingNo` (`RP-…`) generation: count prefix-matched rows, retry on `@unique` collision.
- **Snapshot at creation**: `KomisiPayout.amount = sum of EARNED komisi at the moment of write`. Never recomputed even if child rows are later edited.
- **Idempotency guard**: refuses with 409 `NO_EARNED_KOMISI` if the agent has zero EARNED komisi (prevents empty payouts).
- **`Komisi.payoutId` is `onDelete: SetNull`** — if a payout row is later deleted, the komisi rows stay PAID but become "unbundled" (no payout reference). This is graceful degradation, not data loss.
- Audit `KomisiPayout UPDATE` row includes `{payoutNo, agentSlug, amount, method, komisiCount, komisiIds[]}`.

### Booking notes

`updateBookingNotes()` is no-op if the value didn't actually change — avoids polluting `AuditLog` with redundant rows. Trims whitespace; empty string is stored as `null`. Inline form on `/admin/bookings/:id`, same RBAC as cancel.

### Transfer booking between agents (5q)

`transferBookingAgent({bookingId, toAgentId, reason, includeEarnedKomisi})` in `src/services/bookingAdmin.js`:
- **`Booking.agentSlugCap` is NEVER mutated** — that's the historical URL trail (which `?a=…` slug the visitor first arrived through), kept as audit evidence even when the active agent rotates. Only `Booking.agentId` changes.
- Active bookings only (rejects CANCELLED/REFUNDED with 409). No-op if `toAgentId === current agentId`.
- `toAgentId = null` means transfer to **Kantor Pusat** (no agent).
- Komisi handling by status:
  - `PENDING` → re-points to new agent. **Deleted** when target is Kantor Pusat (KP doesn't earn komisi).
  - `EARNED` → **stays with original agent by default** (they earned it). Caller can opt-in with `includeEarnedKomisi=true` to transfer — admin discretion only.
  - `PAID` → never touched (already disbursed).
  - `CANCELLED` → never touched (history).
- Audit `Booking UPDATE` includes `{transfer: true, reason, agentSlugCap (emphasised unchanged), komisi: {pendingMoved, pendingDeleted, earnedMoved, earnedKept}}`.
- Reason required (min 3 chars). RBAC: OWNER/SUPERADMIN/MANAJER_OPS only.

## Maintenance jobs

Four ops jobs, all sharing the same pattern: pure service in `src/services/`, thin CLI wrapper in `src/jobs/` that disconnects Prisma and exits, plus an OWNER-only HTTP trigger under `/api/admin/jobs/*` for manual runs.

| Job | CLI | HTTP trigger | Service | What it does |
|-----|-----|--------------|---------|--------------|
| expire-docs | `npm run job:expire-docs` | `POST /api/admin/jobs/expire-docs` | `expireOverdueDocuments` | `JemaahDocument` rows with `expiresAt < now` and non-EXPIRED status → EXPIRED + audit |
| expire-intents (5uu) | `npm run job:expire-intents` | `POST /api/admin/jobs/expire-intents` | `expireStaleIntents` | `PaymentIntent` rows with `expiresAt < now` and status IN (CREATED, PENDING) → EXPIRED + audit |
| send-notifications | `npm run job:send-notifications` | `POST /api/admin/jobs/send-notifications` | `processPendingNotifications` | Dispatch all PENDING notifs + FAILED rows whose backoff window elapsed (5nn) |
| send-daily-digest (S27) | `npm run job:send-daily-digest` | `POST /api/admin/jobs/send-daily-digest` | `buildDigestWithAttention` + `notifyDailyDigest` | Build yesterday's activity summary + needs-attention rollup + enqueue one EMAIL per ACTIVE OWNER |
| send-weekly-digest (S33) | `npm run job:send-weekly-digest` | `POST /api/admin/jobs/send-weekly-digest` | `buildWeeklyDigest` + `notifyWeeklyDigest` | Build previous Mon-Sun summary + week-over-week comparison + top paket + enqueue one EMAIL per ACTIVE OWNER. Monday 07:05. |
| prune | `npm run job:prune` | `POST /api/admin/jobs/prune` | `pruneRetentionWindows` | Weekly bounded-growth sweep — see "Data retention" below |

All four are **idempotent** — re-running on an empty queue is a no-op. Terminal statuses are always skipped (e.g. `expire-intents` never touches SETTLED/CANCELLED/FAILED — the 5pp "terminal frozen" invariant).

### Data retention (pruning policy)

The `prune` job in `src/services/retention.js` bounds growth on operational tables. **Defaults are conservative; tune via env vars** (`RETENTION_NOTIF_SENT_DAYS` etc.). What gets touched:

- **Notification** — SENT + SKIPPED rows older than 90 days, plus FAILED rows that are terminal (`nextRetryAt=null` OR `attemptCount >= 5`) older than 180 days.
- **JobRun** — rows older than 90 days. `/api/health` only needs the latest successful run per job; older rows are observability noise.
- **PaymentIntent** — terminal-failed rows (EXPIRED / CANCELLED / FAILED) older than 365 days. **SETTLED intents are NEVER pruned** — they tie 1:1 to a Payment row.

**Never pruned** (compliance, financial, append-only):
- `AuditLog`. If volume eventually becomes a problem, archive to cold storage (S3 etc.) — never delete in place.
- `Payment`. Append-only invariant.
- `Booking`, `Komisi`, `KomisiPayout`, `Lead`, `Incident`, `AttendanceMark`. Trip + financial + ops history.

The sweep itself writes a single audit row per run (`entity=Retention`, `entityId=YYYY-MM-DD`) **only when something was actually deleted** — no-op runs don't pollute the audit log. Per-row deletes are intentionally NOT audited; that would defeat the bounded-growth purpose.

**Job-run logging + `/api/health` freshness**. Both the CLI scripts and the HTTP triggers route through `runJob(name, fn)` in `src/lib/jobRunner.js` — writes a `JobRun` row on start, patches it on finish with `ok` + `durationMs` + derived counters (`scanned`, `affected` from `expired`/`sent`, `errors`). `/api/health` calls `getJobFreshness()` which returns the latest successful run per known job + an `ok` flag derived from `age <= 2 × EXPECTED_INTERVAL_MS[name]`. Aggregate `status` flips to `"degraded"` when DB is down OR any job is stale — external uptime monitors can alert on that single field. **Tests deliberately bypass `runJob` and call services directly**, so test fixtures never pollute the freshness log.

Production deployment artifacts live in `deploy/` (see `deploy/DEPLOYMENT.md`):
- `deploy/crontab.example` — drop into `/etc/cron.d/religio-pro`
- `deploy/systemd/religio-{expire-docs,expire-intents,send-notifications}.{service,timer}` — systemd alternative (preferred on modern hosts; uses `OnUnitInactiveSec` to prevent overlap, sandboxing flags for least-privilege)
- `deploy/logrotate.example` — `/etc/logrotate.d/religio-pro`

**Set `NOTIF_WORKER_DISABLED=true` in production `.env` when cron/systemd drives the notif queue** — otherwise the in-process worker AND the scheduler both dispatch, doubling delivery.

Daily for docs (expiry granularity is days), 10-min for intents (Snap sessions ~1h, "stuck for a few min" is fine), 2-min for notifs (near-real-time delivery). The in-process notif worker (5cc) handles notif dispatch automatically when the dev server is running — system cron is only needed for production deploys with `NOTIF_WORKER_DISABLED=true`.

**Audit signature for system jobs**: `actorEmail: 'system'` + `actorRole: null` (Role enum has no SYSTEM member). The job-specific marker goes in the `after` payload — e.g. `autoExpired: true` for both expire-* jobs. This distinguishes automated transitions from manual admin actions (5qq stuck-intent cancel sets `adminCancel: true` instead).

## Notifications (5y)

`src/services/notifications.js` is a queue-based, channel-pluggable notification subsystem. Layout:

- **Model** `Notification(type, channel, status, recipientEmail/Phone/UserId, subject, body, payload, relatedEntity, relatedEntityId, sentAt, error, attemptCount, nextRetryAt, lastAttemptAt, readAt)` — durable queue + dispatch log. `readAt` (5rr) is independent of dispatch: a SKIPPED row can still be "read" by the jemaah seeing it in their inbox.
- **`enqueueNotification()`** — writes a row in `PENDING`. **Never throws**; failed inserts are logged to console and the caller continues. Recipient-missing for the chosen channel auto-marks the row `SKIPPED` with a reason — visible in the admin viewer, not silently dropped.
- **Per-channel opt-out (5jj)** — `enqueueNotification` accepts an optional `recipientUserId`. When present, it looks up that user's `JemaahProfile.{notifEmail, notifWa}` booleans (default `true`); if the channel is opted out, the row is created with `status=SKIPPED` + `error="recipient opted out of <CHANNEL> notifications"` + `sentAt=now`. The opt-out check runs *before* the recipient-missing check, so an opted-out user with a valid phone/email still gets a clear "opted out" reason in the queue viewer. All notif helpers (`notifyBookingCreated`, `notifyPaymentReceived`, `notifyRefundIssued`) thread the userId via `booking.jemaah?.userId ?? booking.jemaahUserId`. Admin-side notif types (e.g. `notifyCancelRequested`) skip this — opt-out is jemaah-only, never gates admin alerts.
- **Per-type opt-out** — `JemaahNotifPref(jemaahId, type, enabled)` composite-PK table; absence = enabled (default opt-in). Same `enqueueNotification` lookup pulls the per-type row alongside the per-channel booleans. **Per-type wins for the SKIPPED reason text** ("opted out of PAYMENT_RECEIVED notifications" — more actionable than "opted out of WA notifications" when both apply). UI exposes 5 jemaah-relevant types only (`BOOKING_CREATED`, `PAYMENT_RECEIVED`, `BOOKING_LUNAS`, `REFUND_ISSUED`, `DOC_VERIFIED`); admin/agent types (`CANCEL_REQUESTED`, `PAYMENT_SETTLED_ADMIN`, `PAYOUT_CREATED`) are deliberately not toggleable from `/saya/profile`. `setMyNotifTypePrefs` upserts diff-only and writes an audit row only when at least one type actually changed value.
- **`recipientUserId` is also the inbox key (5ll)** — same field doubles as the filter for `/saya/notifications`. **Admin-targeted notifs MUST omit `recipientUserId`** (set it to null) so they never appear in any jemaah's inbox. The composite index `[recipientUserId, createdAt]` keeps the per-user list query cheap as the table grows. Rule of thumb when adding a new notify helper: if the recipient is the jemaah, pass `recipientUserId`; if it's an admin/agent/system, leave it out.
- **`dispatchNotification()`** — looks up a per-channel sender, runs it, persists result (`SENT` / `FAILED` / `SKIPPED`). Every dispatch (success or fail) increments `attemptCount` and stamps `lastAttemptAt`. SENT and SKIPPED are terminal (`nextRetryAt=null`).
- **Retry with exponential backoff (5nn)** — when dispatch returns FAILED, the row stays as status=`FAILED` but gets `nextRetryAt = now + BACKOFF_MS[attemptCount-1]`. Schedule: **1 min → 5 min → 30 min → 2 h → 12 h** (`MAX_ATTEMPTS = 5`). After the 5th failed attempt, `nextRetryAt=null` and the row is **terminally FAILED** — the queue worker stops picking it up. `processPendingNotifications` query is `status=PENDING OR (status=FAILED AND nextRetryAt<=now AND attemptCount<MAX_ATTEMPTS)`; composite index `[status, nextRetryAt]` keeps it cheap. **FAILED is therefore not a terminal status by itself** — the combination of `status=FAILED + nextRetryAt=null` (or `attemptCount>=MAX`) is what makes it terminal. The admin "SEND NOW" button (relabelled "RETRY" once `attemptCount>0`) **resets `attemptCount=0` + clears `nextRetryAt`** before dispatching, so an operator can give an exhausted row a fresh budget without DB surgery.
- **Sender table** `SENDERS = { CONSOLE, EMAIL, WA }` — default is `defaultConsoleSender` (logs `[notif:CHANNEL] → recipient · type`). Production adapters live in `src/lib/senders/` and are wired via `bootstrapNotifSenders()` (5kk) — see next bullet.
- **Production adapters (5kk)** — `bootstrapNotifSenders()` in `src/lib/notifBootstrap.js` registers real senders **per-channel** based on env presence: `FONNTE_TOKEN` wires WA (Fonnte HTTP API, built-in `fetch`); `SMTP_HOST + SMTP_FROM` wires EMAIL (nodemailer). Missing env = stay on console default (correct for dev/smoke). Called from **both** `server.js` (boot) and `src/jobs/send-notifications.js` (CLI cron), so the CLI doesn't silently fall back to console when system cron runs the queue. The bootstrap is idempotent — repeated calls are safe. **Fonnte phone normalisation**: stripped to digits, leading `0` swapped to `62`; numbers already starting with `62` pass through. This is the only place phone format matters — DB phones can stay in any common ID format. Fonnte quirk: returns HTTP 200 even on logical failure, so success requires the response body to have `status: true`; otherwise we surface `body.reason` as the SMTP/WA `error`.
- **Template engine (5bb)** — body + subject come from file-based JSON templates at `src/notifications/templates/<TYPE>__<CHANNEL>.json` with `{{var}}` placeholders. Service helpers (`notifyBookingCreated`, etc.) just compute a `vars` object and call `renderTemplate(type, channel, vars)`. Templates are cached in-memory after first read; edit + restart dev to reload. **Missing template files throw** (loud failure beats silent empty body). Missing `vars` keys render as empty strings (defensive). To localise per language later, rename to `<TYPE>__<CHANNEL>__<locale>.json` and add a locale param to `renderTemplate`.
- **Event hooks (non-blocking)** — these wrap the enqueue in try/catch so notif failure cannot abort the originating DB write:
  - `createBooking` → `notifyBookingCreated` (email + WA to jemaah)
  - `recordPayment` → `notifyPaymentReceived` (WA to jemaah)
  - `issueRefund` → `notifyRefundIssued` (WA to jemaah; body adapts to partial vs full refund — full means booking moved to REFUNDED, partial means it stays CANCELLED)
  - `requestCancelByJemaah` → `notifyCancelRequested` (email to **every ACTIVE OWNER/SUPERADMIN/MANAJER_OPS with an email** — fan-out, one notif row per admin so each can be retried independently)
  - `handleMidtransNotification` (on SETTLED) → `notifyPaymentSettledAdmin` (5yy — email fan-out to same admin set; lets ops know real money arrived without polling `/admin/payment-intents`. NEVER fires on duplicate webhooks — guarded by the `intent.paymentId` idempotency check upstream)
  - `createPayout` → `notifyPayoutCreated` (WA to agent)
- **CLI + HTTP trigger** — `npm run job:send-notifications` (cron-friendly) and `POST /api/admin/jobs/send-notifications` (OWNER) both call `processPendingNotifications({limit:100})`. Re-running on an empty queue is a no-op. Per-row `POST /admin/notifications/:id/send` dispatches one immediately, useful for testing.
- **In-process worker (5cc)** — `src/lib/notifWorker.js` runs the same `processPendingNotifications` on a `setInterval` (default 30 s) inside the dev server process. Started from `server.js` after `app.listen`. Override with `NOTIF_WORKER_INTERVAL_MS=<ms>` env var; disable entirely with `NOTIF_WORKER_DISABLED=true` (production deploys that prefer system cron should set this). A `running` flag guards against tick overlap when processing > interval; ticks log only when work was actually done, so the idle case stays quiet. `stopNotifWorker()` runs on SIGTERM/SIGINT before `server.close()` to drain cleanly. **Retries (5nn) ride this same worker for free** — every 30 s tick re-checks any FAILED row whose `nextRetryAt` has elapsed, so the in-process loop doubles as the retry scheduler. With backoff floors at 1 min, the 30 s cadence is plenty granular.

Production cron example (every 2 minutes for near-real-time delivery):
```cron
*/2 * * * * cd /path/to/travel && npm run job:send-notifications >> /var/log/religio/notif.log 2>&1
```

For real WhatsApp delivery, **Fonnte** (Indonesian provider, easier than Meta WhatsApp Business API) is the wired adapter — drop `FONNTE_TOKEN` (device token from Fonnte dashboard) into `.env` and restart. For email, the wired adapter is **nodemailer** — set `SMTP_HOST` + `SMTP_FROM` (+ optional `SMTP_USER/PASS/PORT/SECURE`). When wired, boot logs `[notif] WA sender = Fonnte` / `[notif] EMAIL sender = SMTP host:port`; if those lines are missing, you're still on console default.

## Analytics

`src/services/analytics.js` exposes `getAgentFunnel(agentId, opts)`, `getLeadSourceBreakdown(agentId, opts)`, `getDailyActivity(agentId, opts)`. Pass `agentId=null` for global (admin) view. `opts = {from, to}` filter on `createdAt`; `resolveRange()` handles defaults (last 30 days), swap (from > to), and invalid date fallback. `getDailyActivity` caps at 366 days to keep SVG sparklines readable.

## Static design package

Seven HTML files plus one shared stylesheet. The whole system pivots on `shared/tokens.css`.

- `index.html` — the **Hub**. Curated entry point with live in-tile previews (miniature versions of each downstream screen, scaled with `transform: scale()`).
- `design-system.html` — Foundations: color tokens, type scale, spacing, components. This is the visual contract; every other screen consumes it.
- `screens/landing.html` — Public marketing page.
- `screens/paket-detail.html` — Auto-generated sales landing per-package. **The dynamic version at `/p/:slug` is the canonical one** — this static file is kept as a design reference.
- `screens/admin-dashboard.html` — Static mockup for the HQ dashboard (1440px desktop). The dynamic `/admin` is built on this.
- `screens/agen-crm.html` — Static mockup for agent CRM (1440px desktop). Dynamic `/agen` mirrors structure.
- `screens/crew-app.html`, `screens/jemaah-app.html` — Mobile app mockups (iOS 393×852). Not yet wired to backend.

### The token contract

`shared/tokens.css` defines:
- CSS custom properties for the **Onyx + Gold + Cream** palette (`--ink-*`, `--gold-*`, `--cream-*`), jewel-tone semantics (`--emerald`, `--ruby`, `--sapphire`, `--amber`), and currency hues.
- Typography stack: Cormorant Garamond (display), Plus Jakarta Sans (body), JetBrains Mono (mono/eyebrow), Amiri (Arabic) — loaded from Google Fonts via `@import`.
- Spacing (`--s-1`…`--s-10`), radius (`--r-xs`…`--r-full`), shadows, hairlines, motion easings, and layout container width.
- Component primitives: `.btn` (+ `--primary`/`--ghost`/`--outline-gold`/`--danger`/sizes), `.badge` (+ semantic variants), `.card`, `.input`, `.label`, `.eyebrow`, `.brand-mark`, `.ornament-corners`, dividers, and a custom scrollbar.

**Always reuse these tokens and primitives** instead of introducing new color/spacing values or duplicating button/badge styles. Per-page `<style>` blocks should only define page-specific layout, not redefine the palette.

### Path conventions

- Root-level files (`index.html`, `design-system.html`) link to `shared/tokens.css`.
- Files in `screens/` link to `../shared/tokens.css`.
- Dynamic EJS templates link to `/shared/tokens.css` (Express serves the same file).
- Hub previews reference downstream screens via `screens/<name>.html`; in-screen navigation back to the hub uses `../index.html`.

### Body label convention

Every page sets `<body data-screen-label="NN Name">` (e.g. `"02 Admin Dashboard"`). This is the canonical screen identifier — preserve it when adding new screens, and increment numerically.

### In-tile preview pattern (Hub)

`index.html` embeds scaled-down versions of each screen inside `.tile__visual > .preview > .preview-canvas.pv-*`. Each `.pv-*` block is a self-contained mini-mock built from the same tokens, then shrunk via `transform: scale(0.42)`. When a screen's hero/key surface changes meaningfully, update the corresponding `.pv-*` preview in the hub to match.

## Conventions / invariants

- **Number-input step on Rupiah fields**: use `step="1"` (not `step="100000"`). Cicilan/komisi/payment amounts are often non-round (e.g. `5170000` = 62M/12). Browser blocks form submit with "please enter valid number value" on mismatch — saved data is fine, but UX breaks.
- **MySQL enum sort order** follows declaration order, not alphabetical. For UI lists where "ACTIVE first" matters (paket list, dropdown defaults), do the rank-sort in JS after the Prisma query rather than relying on `orderBy: { status: 'asc' }`. See `STATUS_RANK` in `src/services/adminDashboard.js`.
- **Form-encoded vs JSON endpoints**: browser-facing admin sub-pages use form POST + redirect-after-success. JSON endpoints (under `/api/*`) return errors via the global errorHandler which serializes to JSON. Don't mix: an `/admin/*` route that returns JSON errors will show raw JSON in the browser.
- **Ownership checks** in nested CRUD services (`loadOwnedHotel`, `loadOwnedDay`, `loadOwnedRoom`, `loadOwnedLead`) match the resource's parent FK against the request scope (paketSlug or req.user.agent). 404 if not found, 403 if wrong owner.
- **Audit invariant**: every state-changing service call must write to `AuditLog`. Never delete or update audit rows. Tools to read: `/admin/audit` viewer + audit timeline on booking detail page.
- **`audit()` actor.role is the Prisma `Role` enum** — passing a custom string like `'SYSTEM'` will be rejected by the DB and the audit write fails **silently** (the writer catches errors and only logs to console). For system actors (cron, jobs, internal callers) **omit `role` entirely** — `actorEmail: 'system'` + `actorRole: null` is the convention. See `src/services/expireDocs.js` for the pattern.
- **Skip-audit-on-no-op**: when an update sets a field to its current value, prefer to short-circuit before writing (e.g. `updateBookingNotes` in `src/services/bookingAdmin.js`). This keeps the audit log focused on real state changes; idempotent calls become free.
- **Payment rows are append-only** like AuditLog: refunds create a new row with `amount < 0` and `status=REFUNDED`, never mutate the original `PAID` row. UI must handle negative amounts (ruby colour + `−` prefix) and the `REFUNDED` status badge.
- **Prisma JSON path filters use MySQL syntax**, not Postgres. `where: { after: { path: ['merged'], equals: true } }` (array form, Postgres) is **rejected** at runtime — MySQL needs `path: '$.merged'` (single string with `$.` prefix). Easy to hit when grepping audit logs by JSON content.
- **Anonymous booking always creates a new `JemaahProfile`** (no phone-based dedup at creation time, because phone isn't unique). Dedup happens later, on claim — see `claimBooking` in `src/services/jemaahPortal.js`. Do not add phone-based dedup to the public booking flow without addressing family-shared-phone collisions.
- **`Booking.agentSlugCap` is immutable** — the historical URL-of-origin trail. `Booking.agentId` rotates via `transferBookingAgent`, but `agentSlugCap` stays put forever (even when the original agent's slug gets renamed/deleted). This means "who currently services this booking" and "where this booking originally came from" are two separate facts, and both are queryable.
- **`router.use('/saya', ...)` does NOT match `/api/saya/*`** — Express prefix matching is literal. The `/saya` prefix only covers `/saya`, `/saya/profile`, etc. The `/api/saya/*` paths need `requireAuth + requireRole(...)` applied **inline** per route (see `requireJemaah` spread in `src/routes/jemaahPortal.js`). Otherwise `req.user` is undefined and the route 500s with "Cannot read properties of undefined (reading 'id')".
- **HTML checkbox "unchecked" is absent from `req.body`** — when paired with a 3-state Zod preprocessor (`undefined → no change`), this means an unchecked toggle never persists as `false`. For opt-out semantics where unchecking *must* save `false`, normalise at the route layer **before** validation: `notifWa: req.body?.notifWa ?? false`. The 3-state preprocessor is still right for JSON callers (PATCH semantics), but form POSTs need this fix-up. See `/api/saya/profile` for the pattern.
- **Uploaded files MUST live under a dir in `SENSITIVE_PREFIXES`** — `private/` is blocked from `express.static`, so files are only reachable via auth-checking download routes (5mm). When introducing a new directory that holds user-uploaded or private content, add it to `SENSITIVE_PREFIXES` in `src/app.js` **before** wiring uploads, or `express.static` will serve it. The existing public `uploads/` dir (static design package) is the counter-example — it's deliberately public; never put real user data there.
- **Server-side filename sanitisation is non-negotiable** — `sanitiseBasename()` in `src/lib/docStorage.js` is applied to both the on-disk filename AND the `Content-Disposition` header for downloads. Never let the user's raw `originalname` reach either path: it can contain `../`, control chars, or homoglyphs that confuse the filesystem or trick a downloader. Mime allowlist + size cap are also enforced in two places (multer + service) — keep both layers in sync when adding new types.
- **`recordPayment` is the single source of truth for money math** — admin-recorded payments, gateway webhook settlements (5pp), and any future automation MUST defer to `recordPayment` in `src/services/payment.js` for creating Payment rows + bumping `Booking.paidAmount` + transitioning `Booking.status` + creating `Komisi` on LUNAS. Never inline these calculations elsewhere. The gateway webhook handler in particular looks tempting to special-case (Midtrans tells us the amount + method!) but bypassing `recordPayment` would let gateway-paid bookings skip the LUNAS-triggered komisi creation. Same rule for refunds: always go through `issueRefund` (`src/services/refund.js`).
- **Webhook handlers MUST be idempotent** — external systems (Midtrans, future Fonnte delivery callbacks, etc.) retry on non-200. Always design the handler so the same payload arriving N times produces the same end state as arriving once. The 5pp pattern: guard on a "I already did this work" sentinel (`intent.paymentId != null`) before invoking the side-effect, and return a NOOP result if it's already done. Pair this with signature verification (constant-time compare via `crypto.timingSafeEqual`) so untrusted callers can't poke the handler.
